Verification commands referenced below, from this project's conventions:

- `ansible/scripts/run-molecule test --all`, run from a role directory — read the SCENARIO RECAP, not the exit code
- `python3 -m unittest discover --start-directory .github/tests`, from the repository root
- `pre-commit run --all-files`
- `openspec validate --all`

**This change's subject is role behaviour, so Molecule is the mechanism that observes it** — the second row of this project's test table. Four of the delta's seven scenarios are static reads of committed files and belong in `.github/tests`; the other three are behaviour.

**The effect is invisible in host state, which is what makes the channel a design question rather than a detail.** A fetched index and an unfetched one leave exactly the same packages installed, so nothing on the host distinguishes them. `ansible.builtin.apt` returns `cache_updated` independently of `changed`, and that return is the only place the distinction exists — so the role's apt tasks are registered and the **assertions sit in the same play as the invocation they characterise**. Registers do not cross plays: Molecule runs converge and verify as two separate `ansible-playbook` invocations, which this repository states in its own comment in `image_prune`'s `absent-heartbeat-key` converge. So 1.2 and 1.3, which characterise the **converge's own** role run, assert in `converge.yml`; 1.4, which characterises an invocation the test itself makes, asserts in `verify.yml` beside it. A marker file would bridge the gap and is refused for its own reason: its content differs between the converge and the idempotence run, so the task writing it reports `changed` and breaks the idempotence check this change is otherwise careful to preserve. `design.md` Decision 6 carries the full reasoning.

**Assert the rule, not the run.** Molecule runs the converge play twice — converge, then idempotence — and on the second run the first apt task *correctly* does not fetch, the index being minutes old. An assertion reading "the first task fetched" fails against a correct implementation on that pass. What holds on both runs is the requirement's own sentence: a task fetches exactly when the recorded fetch time is outside the bound.

**Section 1 comes first on purpose** — the tests are derived from the delta before the implementation exists.

**This change depends on `narrow-the-molecule-trigger-to-what-it-reads`**, which writes `docs/change-queue.md` entry 65 — this change — along with 66 and 67. **Observed 2026-09-11: that change merged as pull request #142, and this branch has been rebased onto that merge**, so entry 65 — this change — is present here and the numbers resolve. The dependency is therefore discharged rather than pending, and it is recorded because a reader of the archived record should be able to see why the two changes are ordered as they are.

## 1. Derive the tests from the delta

- [ ] 1.1 In `ansible/roles/hardening/molecule/default/converge.yml`, add a pre-task recording the instance's **last-fetch time** before the role runs — the mtime of `/var/lib/apt/periodic/update-success-stamp`, falling back to `/var/lib/apt/lists`, and to **the epoch where neither exists**, which is the module's own third branch (`cache_time = 0`) and the case the delta's never-fetched scenario turns on. `stat` reports a missing path as `exists: false` rather than failing, so all three branches are expressible in one pre-task. This is the antecedent every assertion below is stated against, and recording it before rather than reasoning about it afterwards is what makes them hold on the idempotence pass. **Record the instant of the comparison alongside the mtime**: the assertion runs minutes after the module made the same comparison, index age only grows, and a recorded time within minutes of the bound at role-run time can be outside it by assertion time. Unreachable in a `molecule test` sequence, where the ages are months or minutes; reachable in a hand-driven re-converge, which is exactly when someone would be debugging.
- [ ] 1.2 Establish *A second install task in the same converge does not re-fetch*: in the same play, after the role, assert the **second** role apt task reported `cache_updated: false`. True on both runs and the change's central claim. Pair it with the presence half by leaning on the scenario's existing UFW and fail2ban assertions rather than inventing one — `ansible.builtin.apt`'s return carries no presence field, and `package_facts` would be a second mechanism for a thing already asserted.
- [ ] 1.3 Establish *An install on a host with no record of a fetch still fetches*: assert the **first** role apt task's `cache_updated` equals whether 1.1's recorded time was outside **the role's own bound variable** — not the hour `design.md` Decision 2 chose. The assertion is self-referential on purpose: the delta makes no normative claim about the value, so nothing here guards the default, and stating that plainly is better than implying a check that does not exist. **The role's variable is in scope for this assertion only because `converge.yml` invokes the role through `roles:`**, which exposes its defaults to the play; under `include_role` it would need `public: true`, and without it the assertion fails on an undefined name that points at the expression rather than at the cause — the shape this capability's *absent required input is reported by name* requirement exists to avoid. On the first converge that time is the image's build date and the task fetched; on the idempotence run it is minutes old and it did not. One assertion, correct on both, and it is the requirement's sentence rather than a description of one run.
- [ ] 1.4 Establish *An index older than the bound is refreshed* **in `verify.yml`, on the role invocation that is already there** — not by adding one to the converge play. Immediately before that invocation, set the index's recorded fetch time to outside the bound; then assert its first apt task reported `cache_updated: true`. Without this assertion a change that simply stopped fetching would pass everything else here, so it is what makes the bound a bound rather than a disabling.

      **Placement is not a matter of taste, and the obvious choice is a trap.** A bound-overridden invocation in the converge play costs four fetches, not one: Molecule runs that play **twice**, and the bound is a single variable per role, so both apt tasks fetch on both passes. That returns four of the six fetches this change exists to remove, in the very scenario every figure in `design.md` is computed from. `verify.yml` runs **once**, after the idempotence pass, and already carries a third role invocation for the web-CIDR path — `converge.yml`'s own header records that it was kept out of converge precisely to keep the idempotence check scoped to one state. Reusing it costs one fetch and touches neither the idempotence check nor that header's claim.

      **Back-date by removing the *contents* of `/var/lib/apt/lists` and then setting that directory's mtime to a fixed far-past timestamp, in that order.** Two details, each of which turns a fixture error into something that reads as a broken mechanism. The order: deleting the contents sets the directory's mtime to *now*, which reads as fresh and skips — so back-date after emptying, never before. And the directory itself must survive, along with its `partial/` subdirectory: removing and recreating it loses the mtime the fixture is setting, and a missing `partial/` makes apt fail with `E: List directory /var/lib/apt/lists/partial is missing`, which names apt rather than the fixture. And the choice of path is not free either — `design.md` Decision 1 records why the other two forms fail. Do **not** back-date `/var/lib/apt/periodic/update-success-stamp`: `get_cache_mtime()` prefers it, `cache.update()` never writes it, so it would pin the read value in the past and every later task would report `cache_updated: false` however much it fetched. Do not back-date the lists directory without emptying it either: `cache_updated` is true only where the mtime **moved**, and a re-fetch that resolves entirely to *not modified* writes nothing and moves nothing.

      Back-dating rather than overriding the bound is deliberate, and the claim for it is narrow: `verify.yml` includes the role without `public:`, so the role's bound variable is not in scope for a task placed before it and the back-date distance is a literal. It therefore catches an **absurd** default — one longer than the distance chosen — and not a merely wrong one. Nothing here guards the default's value, as 1.3 also says.

      The back-dating task sits in `verify.yml`, so whether it reports `changed` is irrelevant to Molecule's idempotence check, which re-runs `converge.yml` alone — which is why this placement escapes the objection a back-dating fixture would face in the converge play.
- [ ] 1.5 Establish three scenarios in `.github/tests`, as static reads of committed files. First the **positive** obligation, which nothing else covers: a qualifying own-role apt task — unpinned, not installing from a source written earlier in the same file — that declares **no** bound fails. Without it the requirement regresses silently in the one direction the change exists to establish, and `image_prune`'s single bound is asserted by nothing at all. Then the two refusals — *A pinned-version install declares no bound* and *An install from a source the same run added declares no bound*: a task naming an exact version, or installing where an earlier task in the same file writes a destination under `/etc/apt/sources.list.d/`, and also declaring a bound, fails. **Key the second on the destination, not on the module**: this repository's only instance writes its source with `ansible.builtin.get_url`, so a check looking for `apt_repository` matches nothing and passes vacuously — a green check establishing nothing. **Enumerate the roles from committed content, or exclude the Galaxy-installed role by name** — `ansible/roles/geerlingguy.docker/` is gitignored, so a bare glob over `ansible/roles/*/tasks/` sees it in a provisioned working tree and not in continuous integration, and a check that disagrees with itself between the two is worse than no check. State in the docstring that the same-run-source rule reaches a source written in the same file and not one written by another file or an earlier role, so the bound of the check is recorded rather than assumed.
- [ ] 1.6 Establish *The bound is a variable, not a literal per task* and *A test fixture play is not held to the bound* in the same module, under **the same role enumeration 1.5 uses** — its globs reach `ansible/roles/*/tasks/` and `ansible/roles/*/molecule/*/` the same way, and the gitignored Galaxy role carries a scenario with a literal `cache_valid_time`, so a check without the exclusion disagrees with itself between a provisioned tree and continuous integration: the role task files take the bound from a role variable and a literal fails, while `ansible/roles/*/molecule/*/prepare.yml` is outside the check entirely. The second half is not decoration — ten prepare plays declare a literal `cache_valid_time: 0`, and a check drawn without that boundary fails ten committed files that `design.md` Decision 5 deliberately exempts.

## 2. The bound, as a variable with a default

- [ ] 2.1 Add the staleness bound to `ansible/roles/hardening/defaults/main.yml` as a variable defaulting to one hour, **and a row for it in that role's `README.md` variables table** — the table documents every role variable, and nothing in `.github/tests` enforces that, so an omission here is invisible. Comment it against `design.md` Decision 2: why a bound rather than an unconditional re-fetch, why an hour, and the two cases that must never take one — a pinned version, and a source the same run added.
- [ ] 2.2 Add the same to `ansible/roles/image_prune/defaults/main.yml`, and its row to that role's `README.md` variables table, named consistently so a reader sees the two are one judgment. They are two variables and therefore two places; that is the price of roles being independently consumable, and the delta's scenario says "one place per role" rather than one place, so the wording and the implementation agree.

## 3. The three tasks, their registers, and the one that is left

- [ ] 3.1 In `ansible/roles/hardening/tasks/main.yml`, give the `ufw` install the bound, keeping `update_cache: true`, and **register its result** so section 1's assertions can read it. The option is bounded, not removed: a task reaching an index older than the bound still refreshes.
- [ ] 3.2 Do the same for the `fail2ban` install in the same file — the task that demonstrates the change, running seconds after 3.1's and today re-downloading the index it just fetched.
- [ ] 3.3 In `ansible/roles/image_prune/tasks/main.yml`, do the same for the `curl` install — the bound, but **no register**. Decision 6 admits a register into a production role file only as the channel an assertion needs, and no `image_prune` scenario asserts on it; an unused register would be an edit to a production file justified by a rationale that does not reach it. What covers this role's bound instead is 1.5's positive static check. Leave the comment block above it alone; it records why the role may not assume an HTTP client, which this change does not touch.
- [ ] 3.4 Record, beside the pinned platform image in `hardening`'s and `image_prune`'s scenario definitions, that the bound's saving depends on that image's recorded apt-fetch time being older than the bound — `design.md` Decision 1 carries the observation and the digest it was taken against. The pin-bumping commit is the one place this dependence can be noticed, and it is the one place it is not currently written down; a pin to a freshly built image would make the bound inert and the failure would name a package, not the pin. A comment, not a check — the existing image-pin assertion already reads those files.
- [ ] 3.5 Leave `ansible/roles/tailscale/tasks/main.yml` alone and **comment why, at the task**. Three reasons, the first being the one that survives the others lapsing: the role writes `/etc/apt/sources.list.d/tailscale.list` and then installs from it in the same run, so an index fetched earlier in that converge is inside any bound and predates the source — the package would resolve to no candidate while every other install succeeded. Then: the install is pinned to an exact version; and the role carries no scenario by which any of this could be observed, which `docs/change-queue.md` entry 3b owns.

## 4. Verification

- [ ] 4.1 `ansible/scripts/run-molecule test --all` from `ansible/roles/hardening`, reading the SCENARIO RECAP and confirming it names both scenarios. Before starting, bring this working tree's namespace to the project's initial state — `ansible/scripts/run-molecule destroy --all`, and remove `.molecule-home/tmp/` — since Molecule does not clear it itself.
- [ ] 4.2 The same from `ansible/roles/image_prune`, confirming the RECAP names all four scenarios. While iterating, run individually with `-s <name>`: `--all` stops at the first failure and silently skips every scenario sorting after it.
- [ ] 4.3 **Record the measurement this change exists for**, for both roles. `design.md` predicts `hardening` goes from six fetches to one, **plus one forced by 1.4's bound-0 invocation** — so two in the implemented scenario, and a reader comparing prediction to measurement should expect that. Count the fetches in each run and record them, and say which two runs are being compared: a local run and the degraded continuous-integration run `design.md` quotes are not comparable.
- [ ] 4.4 `python3 -m unittest discover --start-directory .github/tests`, green.
- [ ] 4.5 `pre-commit run --all-files`, green. Provision first — a fresh working tree carries no Galaxy content, and `ansible-playbook --syntax-check` fails on the missing external role in a way that reads as a broken dependency rather than an unprovisioned tree.
- [ ] 4.6 `openspec validate --all`, green.

## 5. The record

- [ ] 5.1 Delete `docs/change-queue.md` entry 65, which is this change, at the archive. The branch is rebased onto the merge that wrote it, so the entry is present and this is an ordinary deletion — the earlier concern, that it would be performed against a base predating the entry, no longer applies.
- [ ] 5.2 Record the single-`apt`-task refactor named in `proposal.md`'s Non-Goals as its own queue entry: collecting each role's installs into one task would cut fetches further, and is a larger change to two roles that moves where a failure surfaces.

## 6. The observation

- [ ] 6.1 The effect is a reduction in work, observable in the run's own output rather than on the host — which is the whole reason section 1 reads `cache_updated` rather than host state. Record the before and after fetch counts, naming the runs compared.
- [ ] 6.2 Confirm the host state is unchanged, which is the other half of the claim: the same packages installed, the idempotence pass still reporting no change, and no scenario's assertions relaxed to reach green.

## 7. Archive

- [ ] 7.1 Once the effect is confirmed, bring the branch back to the freshly fetched trunk, commit this change's specification record there, and open the pull request that carries it.
