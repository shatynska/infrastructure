## 1. Establish the credential set before touching anything

- [x] 1.1 Re-read the live Environment state with `gh api repos/shatynska/infrastructure/environments` and record it in this change rather than trusting `handoff.md`'s table; verify the reading names `main-production`, `production` and `staging`, and reports whether `main-staging` exists yet
- [x] 1.2 Re-read the secret **names** on `production` and `staging` with `gh api .../environments/<name>/secrets`; verify the counts are 15 and 6 and that the name lists match `handoff.md`, and treat any addition since as part of the set to carry over
- [x] 1.3 Establish that each of the three SSH private halves is a **working** key — not that it equals what GitHub stores, which nothing outside GitHub can establish: verify `ssh-keygen -y -f ~/.ssh/shatynska-platform` equals `deploy_apps[platform].public_key` in `ansible/inventory/group_vars/production.yml`, and verify `ssh -o BatchMode=yes -i ~/.ssh/shatynska-ansible-ci-main-production root@<production host> true` and the staging equivalent each authenticate over the address the converge reaches the host on
- [x] 1.4 Recover `PLATFORM_ACME_EMAIL` and `PLATFORM_DEPLOY_HOST` by reading them off the running platform stack on the production host; verify each recovered value against what the deployed service is actually using rather than against memory
- [x] 1.5 Read `TF_API_TOKEN` from `~/.terraform.d/credentials.tfrc.json`, the `app.terraform.io` entry; verify a token is present there and do **not** reissue it — `docs/change-queue.md` entry 75 records it as being on the workstation and it is, which an earlier draft of `design.md` denied
- [x] 1.6 Confirm that **no value needs reissuing**, the Tailscale OAuth pair included. A client secret is shown once at creation, which is why an earlier draft of this plan created a replacement client; the operator holds both halves of the existing pair, so it is carried over like the other twenty. Verify the pair is in hand before section 3 begins. *(Done 2026-09-13 — see `credential-audit.md`. This is what removed the replacement client, the old client's revocation and its blast-radius question from the change entirely.)*

## 2. Derive the tests before implementing

- [ ] 2.1 Commit the approved plan, so the derived tests map to that baseline; verify the commit contains every planning artifact and no implementation edit
- [ ] 2.2 Dispatch the independent test author against this change's delta specs with the `.github/tests` row of `AGENTS.md`'s test table — test command `python3 -m unittest discover --start-directory .github/tests` from the repository root, test-path glob `.github/tests/*.py`; verify the author either produces tests with a `test-plan.md` mapping scenario to test, or reports that the deltas' scenarios are unreachable by a static read and names what it could not place. **Say which pull request each test rides in**: both pull requests must be green before merging, so a test asserting production's new name cannot travel in the staging one — tests about staging's name go with section 4, everything else with section 7. **And say so in `test-plan.md`, with the mechanic**: both pull requests come off one branch in sequence, so a production-name test committed here rides in the *staging* pull request and turns its required check red before task 7.3 lands. Hold the production-half tests in a commit made only after the staging pull request has merged — the remedy a red canary invites is deleting or weakening the test, and that is the one thing this project's testing rules forbid

## 3. Build `main-staging` before anything names it

- [ ] 3.1 Create the `main-staging` Environment; verify `gh api .../environments/main-staging` returns it rather than a `404`
- [ ] 3.2 Set all six secrets on `main-staging` with `gh secret set --env main-staging`, **piping each value in on stdin** rather than passing `--body` or exporting a shell variable — *Write Credentials Confined to the Gated Pipeline* (`openspec/specs/iac-safety-hardening/spec.md`) forbids a stack's write token being "exported into a shell environment", and re-entry is the one moment it transits a workstation at all; taking the Tailscale pair from the operator's own records and `TF_API_TOKEN` from the workstation credentials file; verify by re-reading `gh api .../environments/main-staging/secrets` and confirming the name list equals staging's from 1.2, counting six
- [ ] 3.3 Confirm `main-staging` carries **no** protection rules, matching `staging`'s deliberate ungated design; verify the `protection_rules` array in the API response is empty

## 4. Flip staging, as the canary

- [ ] 4.1 Set `github_environment: main-staging` in `terraform/stacks/main-staging/pipeline.yml`, leaving `target_environment: staging` untouched, and rewrite the **whole comment block** above the field — both its `staging` requires NO reviewer paragraph and its entry-75 commitment paragraph, which the flip falsifies rather than merely dates; verify the file's two environment fields now spell different words and no paragraph in it still says the move is owed
- [ ] 4.2 Add the declared `target_environment` as a third source to **all three** name sets that collapse — `environment_identifiers()` in `.github/tests/test_a_second_environment.py`, the set built inline in `.github/tests/test_host_converge_workflow.py`'s workflow sweep, and the one inside `test_the_gate_names_no_environment` in `.github/tests/test_environment_agnostic_pipeline.py` — and update the first's docstring, which states two sources; verify **all three** still recognise the words `production` and `staging` after the flip. Note that the first is a shared function with **four** consumers, not one: `environment_identifiers()` is read twice inside its own module and twice more in `.github/tests/test_host_configuration_names_its_environment.py`, which imports it. Repairing the function repairs all four, so the count of *sets* is three and the count of *places that lose their subject* is five — confirm the larger number, since it is the one a reader checking coverage will count. Without this the sets collapse to the two stack names: the first makes the assertion tracing to *Write Credentials Confined to the Gated Pipeline* (`openspec/specs/iac-safety-hardening/spec.md`) go green by losing its subject, the second stops reporting a hardcoded environment name in `host-converge.yml` — the one workflow that actually consumes the Ansible group — and the third stops reporting a gate step conditioned on a literal `production` or `staging`, which is what *Each Stack Declares Its Own Pipeline Configuration* forbids by name. Note that the third sits in the same module as a sweep that already reads the group and explains why: one of that module's two sets learned this lesson and the other did not. **Establish that three is all of them by sweeping rather than by trusting this list**: grep `.github/tests` for every construction that reads the stack directory listing or a declared `github_environment`, and confirm each one is among the three. Counting from memory is how the third came to be missed, and a fourth would go green by losing its subject with no signal at all
- [ ] 4.3 Move `SECOND_ENVIRONMENT_GITHUB_ENVIRONMENT` to `"main-staging"` in `.github/tests/test_a_second_environment.py` and rewrite **both** stale commentaries, leaving **no `entry 75` literal** in either so that task 7.9's triage stays true: the constants block, whose sentence "three of these read `main-staging` and the fourth reads `staging`" the flip falsifies, and the separate docstring saying `github_environment: staging` stands because GitHub cannot rename an Environment — the second carries no `entry 75` literal, so no entry-75 needle reaches it; verify `python3 -m unittest discover --start-directory .github/tests` passes from the repository root
- [ ] 4.4 Correct `terraform/stacks/main-staging/versions.tf`'s comment that the apply job "declares `environment: staging`"; verify it names the Environment the stack now declares, and note that this edit makes `REAL_KEEPER_PROSE` in `.github/tests/test_terraform_stacks_are_the_iterated_unit.py` stale one pull request early — it quotes this line and claims HEAD, and task 7.8 is where it is corrected, because correcting it here would make it quote a `main-production/versions.tf` that has not moved yet
- [ ] 4.5 Open the pull request for staging's flip alone and let continuous integration run; verify the required checks report green before merging
- [ ] 4.6 **Operator observation after the merge:** confirm staging's apply runs under `main-staging` and still does **not** wait for a reviewer; verify from the run's own Environment label rather than from the workflow file
- [ ] 4.7 **Operator observation, and the one that makes staging a real canary:** dispatch `host-converge.yml` for the **staging** stack — the workflow takes a `stack` input, so no trigger has to be manufactured — and confirm it runs under `main-staging`, does not pause, and reaches the host. Verify this exercises `main-staging`'s converge key, its vault password, and its Tailscale pair — every one of them re-entered by hand, and none of them exercised by staging's apply, which touches neither the tailnet nor SSH. **It belongs here rather than after production's observations**, because a re-entry error found on a dispatchable staging run costs a re-run, and the same error found at task 8.3 costs a spent production approval on a trigger that cannot be re-raised

## 5. Sweep the tree for every spelling, not for the queue entry

- [ ] 5.1 Sweep with each of these needles separately, **keeping `entry 75` as one of them** rather than replacing it: `entry 75`, `` `production` Environment ``, `` `staging` Environment ``, `environment: production`, `environment: staging`, `--env production`, `--env staging`, the bare backticked `` `production` `` and `` `staging` ``, and the **unbackticked** `production Environment` and `staging Environment`, which is the only needle that reaches `platform-deploy.yml`'s header comment above its `on:` block; verify the hit list is triaged in full before section 7 begins, and note that a comment wrapping the phrase across two lines is invisible to every line-based needle — `platform-deploy.yml`'s Tailscale comment is one such site
- [ ] 5.2 Read `docs/bootstrap-a-new-host.md` **and `README.md`** end to end rather than at named line numbers, and **enumerate** every place either creates, names or populates these Environments so that task 7.6 can correct them — section 5 triages and section 7 edits, so that no half-done correction sits between the two pull requests — §3.2's Environment-creation step, its `gh secret set --env` block, its secret-inventory table, the stage-4 approval narrative and the §0.3 storage rows; verify the runbook, followed literally by a new operator, creates Environments named for their stacks
- [ ] 5.3 Leave the three sites that are **not** this change's: `docs/bootstrap-a-new-host.md`'s application-repository stage and `ansible/roles/deploy_user/README.md`, both of which describe a *different* repository's own `production` Environment, and `docs/review-2026-09-08-host-readiness.md`, a dated record of what was true then; verify each is excluded deliberately and say so where the exclusion is not self-evident

## 6. Build `main-production` before anything names it

- [ ] 6.1 Verify `main-production` still carries its required reviewer by re-reading `protection_rules` in the API response, and add the reviewer if it does not — this is the one claim about this change that has already gone stale once
- [ ] 6.2 Confirm each carried-over value against the source it comes from **before** writing it, rather than after: and check the partition adds to fifteen rather than to a number carried over from `handoff.md`'s cross-stack tally — **8** from the password manager (`ANSIBLE_VAULT_PASSWORD`, `HCLOUD_TOKEN`, `PLATFORM_DEADMANSWITCH_URL`, `PLATFORM_GRAFANA_ADMIN_PASSWORD`, the three `PLATFORM_POSTGRES_*` and `PLATFORM_SLACK_WEBHOOK_URL`), **2** off the running platform stack (`PLATFORM_ACME_EMAIL`, `PLATFORM_DEPLOY_HOST`), **2** workstation SSH halves (`ANSIBLE_SSH_PRIVATE_KEY`, `PLATFORM_DEPLOY_SSH_KEY` — the third SSH half is staging's converge key, already written at 3.3 and not among production's fifteen), **1** workstation `TF_API_TOKEN`, and **2** Tailscale values from the operator's own records. Verify the operator has an actual source in hand for every one of the fifteen names read at 1.2 — this is the password-manager check `handoff.md` says to make before touching anything, and it is a task rather than a preamble because seven of these values are exercised by nothing in section 8
- [ ] 6.3 Set all fifteen secrets on `main-production` with `gh secret set --env main-production`, piping each value in on stdin for the reason task 3.3 gives, taking the **two** SSH values from the workstation files established in 1.3, the Tailscale pair from the operator's own records, and `TF_API_TOKEN` from the workstation credentials file; verify by re-reading the secret list and confirming it equals production's from 1.2, counting fifteen — and read each secret's `updated_at`, since a name list alone cannot tell a write from a name that was already there
- [ ] 6.4 Rewrite the **repository-scoped** `TF_API_TOKEN` from the same workstation value, so the two copies stay identical as *HCP Terraform Access via a Static Token, Unsplit by Privilege* (`openspec/specs/iac-state-management/spec.md`) obliges; verify both exist, and note that this is the one secret whose omission from an Environment falls back to the repository copy and passes silently
- [ ] 6.5 Re-read `main-production`'s protection rules a second time, after populating; verify the reviewer survived the fifteen writes

## 7. Flip production, the documentation and the test literals

- [ ] 7.1 Set `github_environment: main-production` in `terraform/stacks/main-production/pipeline.yml`, leaving `target_environment: production` untouched, and rewrite the two comment **blocks** — the one above `github_environment` and the one above `target_environment` that asserts the two fields *now spell the same word for different reasons*, which the flip makes false; verify no paragraph in the file still says the move is owed
- [ ] 7.2 Set `environment: main-production` in `.github/workflows/platform-deploy.yml`, rewrite the comment above it that says renaming is not a matter of editing this line, and correct **every** comment in the file naming the old Environment — the header comment above `on:` as well as the later ones — including the Tailscale one that wraps `` `production` `` and `Environment` across a line break, which no line-based needle finds; verify the equality between that literal and the stack's declaration still passes
- [ ] 7.3 Move `PROD_GITHUB_ENVIRONMENT` in `.github/tests/test_environment_agnostic_pipeline.py` and `GATED_DEPLOY_ENVIRONMENT` in `.github/tests/test_ci_configuration.py` to `"main-production"`, and rewrite the entry-75 commentary in both modules plus `test_the_external_service_names_are_retired.py` into the past tense, leaving **no `entry 75` literal** in any of them so that task 7.9's sweep stays true; verify the full suite passes
- [ ] 7.4 Correct the docstring in `.github/tests/test_the_external_service_names_are_retired.py` that quotes *Gated Deploy Reuses the Terraform Production Environment* verbatim — **both** quoted passages, the requirement sentence and the scenario *Same approvers gate both kinds of production change*, since this change's delta rewrites each; verify the quotation matches the requirement text the archive will produce, since a stale quotation is a citation this repository has no sweep for
- [ ] 7.5 Delete `docs/naming-conventions.md`'s exception banner and the `(entry 75; see the banner)` annotation on the GitHub Environment row; verify the document no longer claims an exception and that the stack table reads as describing the tree
- [ ] 7.6 Apply section 5's corrections to `docs/bootstrap-a-new-host.md` and `README.md` — the latter including its entry-75 sentence, its write-token line naming prod's and staging's Environments, and its `apply.yml` and `platform-deploy.yml` summaries — and §6.6's `--env` note, which changes to say the argument takes the **stack** rather than being deleted; verify §6.6 still explains which argument is which, and that `README.md` names no Environment this change deletes
- [ ] 7.7 Correct `platform/README.md`, which describes "a `production`-Environment-gated job" the flip falsifies, and state in the commit message that this edit is **also** what raises the deploy task 8.3 observes — a file under `platform/**` is that workflow's only trigger, since it has no `workflow_dispatch`. Verify it is the only `platform/**` change in the pull request, and that `test_shipped_config_reaches_the_container.py` still passes, which it will because a README is not shipped content. **No manufactured edit is needed**: an earlier draft of this plan added a comment-only change to the Compose file for the trigger, not having noticed that a file this change must correct anyway sits under the same path filter
- [ ] 7.8 Correct `terraform/stacks/main-production/versions.tf`'s comment that the gated apply job "declares `environment: production`", and the two passages in `docs/deferred-work.md` naming the old Environments; verify each names the Environment its stack now declares, and correct `REAL_KEEPER_PROSE` in `.github/tests/test_terraform_stacks_are_the_iterated_unit.py`, whose comment says it is quoted verbatim from the two `pipeline.yml` files **and the two `versions.tf` files** as they stand at HEAD — it therefore quotes lines that tasks 4.1, 4.4, 7.1 and this task all rewrite, and it claims HEAD, so it must follow them. Nothing asserts the quotation, so no test turns red; what rots is the claim. **Do not touch `DEFECTS` in the same module**: it is labelled verbatim from `93bef69^` and is deliberately frozen to a historical commit — see task 7.9's fifth bucket
- [ ] 7.9 Re-run every needle from 5.1; verify every remaining hit triages into exactly one of **five** buckets. Three of them are leave-alone: the **environment axis**, which this change keeps by design and which the broad needles legitimately match across the Ansible groups, the `group_vars` files and the `--vault-id` labels; this change's own directory; and the three sites 5.3 excludes. The fourth is **commentary this change corrects**, which the earlier tasks do not reach because it carries no `entry 75` literal and is not a constant — `.github/tests/test_ci_configuration.py`'s three passages describing a production approval request and the `PLATFORM_` prefix's Environment, `.github/tests/test_environment_agnostic_pipeline.py`'s two prose mentions outside its entry-75 commentary, and `docs/change-queue.md`'s two entries describing the deploy job's literal; correct each. The fifth is **quotations frozen to a commit, which SHALL NOT be edited**: `DEFECTS` in `.github/tests/test_terraform_stacks_are_the_iterated_unit.py` is labelled verbatim from `93bef69^` and pairs each defective line with its correction so that a needle is exercised in both directions — rewriting it would leave the test **green** while the fixture stopped being the historical pair it claims to be, which is a check silently weakened with no signal. The keeper tuple in the same module is the same kind. The distinction from `REAL_KEEPER_PROSE`, which task 7.8 does correct, is that it claims HEAD while `DEFECTS` claims a commit. It is **not** an expected-empty sweep, and stating it as one would make it untickable: `environment: production` is a substring of the `target_environment: production` this change deliberately preserves
- [ ] 7.10 Open the pull request for production's half and let continuous integration run; verify the required checks report green before merging

## 8. Observe production, which is the verification that counts

- [ ] 8.1 **Operator observation:** on the merge, confirm the Terraform apply for `main-production` waits for approval under the `main-production` Environment; verify from the pending run's Environment label, and read which workflow the prompt names, since all three render identical text
- [ ] 8.2 **Operator observation:** dispatch `host-converge.yml` manually for the production stack — the merge does not raise it, because that workflow triggers on `ansible/**` and this pull request touches no such file — and confirm it waits under `main-production` and then reaches the host; verify this is what proves `ANSIBLE_SSH_PRIVATE_KEY` was re-entered correctly
- [ ] 8.3 **Operator observation:** confirm the platform deploy raised by 7.7's edit waits under `main-production` and then connects; verify this is what proves `PLATFORM_DEPLOY_SSH_KEY`, which nothing else exercises, and `main-production`'s copy of the Tailscale pair — the same pair having already been proven on staging at 4.7, so a failure here points at this Environment's copy of it rather than at the values
- [ ] 8.4 Re-read `gh api repos/shatynska/infrastructure/environments`; verify it shows four Environments, with the two new ones carrying the right secret counts and `main-production` carrying the reviewer

## 9. Delete the old Environments, which is the irreversible step

- [ ] 9.1 Confirm `gh run list --status waiting` shows no run pending against `production` or `staging`, and re-run any that would be approved later; verify the queue is empty, because a pending run executes the commit it was raised on and GitHub re-creates the Environment it names **with no protection rules**
- [ ] 9.2 Delete the `production` Environment only after every observation in section 8 has been made and 9.1 is clear. **Seven of the fifteen values are exercised by no observation at all** — `PLATFORM_ACME_EMAIL`, `PLATFORM_DEADMANSWITCH_URL`, `PLATFORM_GRAFANA_ADMIN_PASSWORD`, the three `PLATFORM_POSTGRES_*` values and `PLATFORM_SLACK_WEBHOOK_URL` — and a wrong one among them is silent until an alarm fails to arrive or a dashboard refuses a login. They are covered by the soak rather than by a check: leave the deletion as long as the design permits, and treat a platform irregularity in that period as a suspected mis-entry before anything else; verify the API no longer returns it, and record in this change that its fifteen values are now unrecoverable from GitHub
- [ ] 9.3 Delete the `staging` Environment; verify the API no longer returns it
- [ ] 9.4 Re-read the Environment list again some hours later, not immediately; verify exactly `main-production` and `main-staging` remain and neither old name has been resurrected by a late approval or re-run

## 10. Record the change

- [ ] 10.1 Delete entry 75 from `docs/change-queue.md`, and in the same edit check entry 84 for anything this change's completion falsifies — it was rewritten on 2026-09-13 once its key renames were performed, so the caution it now carries is that the remaining work must not become a rotation, which this change does not affect; entry 84 also names this change four times, which is a reference by name rather than by number and is fine to leave; verify the file's remaining entries are untouched and that no other entry cross-references the deleted one
- [ ] 10.2 Run the three verifications this repository owes — `python3 -m unittest discover --start-directory .github/tests` from the repository root, `openspec validate --all`, and `pre-commit run --all-files`; verify all three pass, and report them as proving nothing about GitHub's settings, which section 8 is what proves
- [ ] 10.3 Bring the branch back to the freshly fetched trunk and archive the change, letting `openspec archive` apply the delta texts to the main specifications rather than editing those files by hand earlier; verify `openspec validate --archived` passes, and read each touched capability's `## Purpose` by hand, since `openspec archive` reports `→ 0` for Purposes and silently leaves them stale
- [ ] 10.4 Open the record's own pull request; verify it is the last of this change's pull requests and that every earlier one has merged

## Not tasked, and recorded here instead

**Branch and working-tree removal are deliberately not tasks.** They happen after this change's record pull request merges, which is after the commit that writes this file, so a checkbox for them would be unticked by construction forever — `AGENTS.md` requires them recorded in prose, and this is that record. Once the archive pull request has merged and every other pull request this change opened has merged, remove the branch `rename-the-github-environments` locally and on the remote, and remove the working tree from the repository's main working tree rather than from inside the tree being removed. Read each merge from the pull request's own state rather than from branch ancestry, since a squash or rebase merge leaves no ancestry to read.

**This change reuses another change's working tree.** It runs in `.claude/worktrees/record-the-heartbeat-timing-divergence/`, whose directory name belongs to the change that merged as PR #166; the operator directed the reuse after that merge. The directory name is therefore not a reliable indicator of which change is in progress here, and the removal above refers to this change's branch rather than to the one the directory is named for.
