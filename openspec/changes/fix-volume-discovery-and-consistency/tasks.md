## 1. Baseline

Provisioning first, and a recorded pre-implementation baseline for both suites.
`AGENTS.md`: verification that cannot reach what it needs skips and reports
success, so an unprovisioned run is indistinguishable from a passing one.

- [x] 1.1 Provision the working tree per `README.md` "Local setup": `pre-commit install --hook-type pre-commit --hook-type commit-msg`, the pinned Molecule toolchain from `ansible/requirements-test.txt`, and `ansible-galaxy collection install -r ansible/requirements.yml` plus `ansible-galaxy role install -r ansible/requirements.yml -p ansible/roles`. Verify `molecule --version` runs and `ansible/roles/geerlingguy.docker/` exists. Note the README's own step 5 says `ansible-galaxy install -r ansible/requirements.yml`, which installs to `~/.ansible/roles` where no scenario will find it — use `-p ansible/roles` as CI does. Until every step has been reached, report verification as not run.
- [x] 1.2 Record the pre-implementation baseline for the Molecule suite: `cd ansible/roles/platform_data_volume && molecule test --all` and verify the `default` scenario **passes** on the tree as it stands, so any later failure there is attributable to this change and the working path is known good before the discovery guard touches it.
- [x] 1.3 Record the pre-implementation baseline for the CI-configuration suite: `python3 -m unittest discover --start-directory .github/tests` from the repository root, and verify it passes on the tree as it stands.
- [x] 1.4 Record the pre-implementation baseline for the static analysis suite: `pre-commit run --all-files`, and verify every hook passes on the tree as it stands.
- [x] 1.5a Confirm the pin this change introduces resolves before a production approval is spent on it: `docker manifest inspect postgres:16.15`. Verify it resolves, and compare its manifest with `postgres:16`'s — task 5.3 deletes a caveat that asked exactly this of every tag in the file, and `16.15` is the one tag the file will carry that production has never run. Observed 2026-09-07: both resolve, to the identical manifest.
- [x] 1.5 Record the "before" observation the Compose half will be confirmed against: from a tailnet peer, `curl -s http://<GRAFANA_BIND_ADDRESS>:3000/login | grep -o 'appUrl":"[^"]*'`, and verify it reports `appUrl":"http://localhost:3000/"`. A fix cannot be claimed against an effect never observed. Record the value the run itself returns.

## 2. Derived tests (before implementation)

Written from the delta specs by an author other than whoever implements, and
before the implementation exists — `AGENTS.md`, "derive tests". Each task below
names the spec scenario it descends from. Every one of these tests is expected to
**fail** on the current tree; that failure is the point, and it is what group 1's
baseline makes attributable.

- [ ] 2.1 Add `ansible/roles/platform_data_volume/molecule/no-device-discoverable/` (`molecule.yml`, `converge.yml`, `verify.yml`, and `prepare.yml` only if one proves necessary). Base `molecule.yml` on the `default` scenario's — the platform stanza is role-specific — keeping the **same digest-pinned image**, since `iac-cicd-pipeline` requires scenarios naming the same image repository to name the same digest and `.github/tests/test_ci_configuration.py` asserts it. Give the instance a **distinct name** (`platform-data-volume-no-device-instance`): every scenario in this repository names its instance uniquely, and two scenarios of one role sharing a name collide on `create` while a sibling's container is still up. State explicitly whether `test_sequence` is overridden as `ghcr-credential-rejected` does, rather than inheriting the default silently. Verify by running `python3 -m unittest discover --start-directory .github/tests`, which is what enforces the digest pin.
- [ ] 2.2 In that scenario's `converge.yml`, supply **no** `platform_data_volume_device`, letting it default to `""` so on-host discovery runs and finds nothing (a container has no `/dev/disk/by-id/scsi-0HC_Volume_*` entry). Wrap the role include in `block`/`rescue` and write the outcome — `succeeded` or `failed`, plus the failing task's name, action and message — to a JSON marker file on **both** branches, the shape `ansible/roles/deploy_user/molecule/ghcr-credential-rejected/converge.yml` uses and for the reason its comment gives. Verify the marker file exists on the instance after `molecule converge`.
- [ ] 2.3 In that scenario's `verify.yml`, assert the recorded outcome is `failed`, that the recorded action **names** `assert` (a substring test, as `ghcr-credential-rejected/verify.yml:56` does — the recorded action may be short or fully qualified), and that the recorded message names the missing device and the `volume_enabled` toggle — spec scenarios *No device is supplied and none can be discovered* and, for the `""` input itself, *An empty value the role gives a meaning to is not a missing input*: the role must proceed to discovery on an empty device path and fail with the device diagnostic, never report the input as missing. Verify it **fails** on the current tree, and record what the run reports as the failing task: an unfixed role dies on the `set_fact` with `list object has no element 0`, so a test asserting only "the run failed" would pass here and establish nothing.
- [ ] 2.4 Add `ansible/roles/hardening/molecule/absent-ssh-cidrs/`, its `molecule.yml` subject to every obligation 2.1 states — same digest-pinned image as the role's sibling scenario, a distinct instance name, and an explicit `test_sequence` decision. `converge.yml` includes the `hardening` role with `hardening_ssh_allowed_cidrs` deliberately unset, wrapped in the **same `block`/`rescue` plus JSON marker file** 2.2 describes — this is mandatory, not stylistic: without the rescue the converge exits non-zero, Molecule never runs `verify`, and the scenario reports a bare failure instead of the assertion being tested. `verify.yml` then asserts the recorded outcome is `failed`, that the recorded action names `assert`, and that the message names the variable and `ansible/inventory/group_vars/prod.yml` — spec scenarios *A required input is not supplied* and *The check precedes the tasks that consume the input*. Verify it fails on the current tree, where the run instead dies inside the `Allow SSH (22) from the configured CIDRs` loop.
- [ ] 2.5 In that same scenario's `verify.yml`, assert that the failure happened **before** the host was changed — that `ufw` is not installed on the instance after the failed converge — which is what distinguishes a refusal to proceed from a partial application. Verify this assertion is genuinely reached and not vacuous.
- [ ] 2.5a Add `ansible/roles/platform_data_volume/molecule/multiple-devices-discoverable/`, its `molecule.yml` subject to every obligation 2.1 states. Its `prepare.yml` first stands up **two** loop devices the way `molecule/default/prepare.yml` already stands up one — the same `mknod`/`losetup` steps and the same package prerequisites it installs — then creates `/dev/disk/by-id/` and places a `scsi-0HC_Volume_*` symlink to each. Name them so the lexicographically-first is deliberately **not** the one a bare directory read returns first; `/dev` is a tmpfs whose readdir order follows creation order, so this is controllable, and without it the assertion passes by luck and discriminates nothing. `converge.yml` supplies no `platform_data_volume_device`; `verify.yml` asserts the mount resolves to the lexicographically-first of the two — spec scenario *More than one candidate device is attached*. Verify it fails on the current tree, and record which device the unfixed `files[0]` actually selects.
- [ ] 2.5b Verify 2.5a is genuinely order-sensitive: create the two symlinks in the opposite order and confirm the scenario still selects the same device. A test that passes under only one creation order establishes nothing about determinism.
- [ ] 2.6 Add an assertion to `.github/tests/test_ci_configuration.py` that every service in `platform/docker-compose.yml` declares its image by a content digest, or by a tag that names at least two version components, reusing `parse_image_reference` and `CONTENT_DIGEST`. Reject `latest` and other channel tags (`stable`, `edge`, `main`), and reject a tag naming a bare series such as `16` or `v3` — spec scenario *The automated check enforces a floor and says so*. The rule is a **floor on specificity and never a ceiling**: it must not reject a tag for being *too* specific, and in particular must accept `postgres:16.15`, a two-component PostgreSQL release — spec scenario *The check does not reject a correctly pinned release*. Give the test a docstring recording that passing establishes only a necessary condition: a tag specific enough to pass may still be a series under its own publisher's scheme (`traefik:v3.7` would pass and is not a release), and that residue is carried by human review. Verify it fails on the current tree naming `postgres` for its bare `16`, and passes over the seven services already naming a release.
- [ ] 2.7 Add an assertion that the check is a real read of the file and not an enumeration: a temporarily added service declaring a moving tag must be caught with no test edit, and a temporarily removed `image:` key must fail by name rather than be skipped. Revert both fixtures. Verify — an assertion that silently covers only the services that exist today would pass 2.6 identically.
- [ ] 2.8 Add an assertion that `GF_SERVER_ROOT_URL` in `platform/docker-compose.yml` interpolates the same variable the Grafana service's published port uses, rather than naming a literal host or address — spec scenario *The configured base URL is not a literal that ignores where the interface is published*. Verify it fails on the current tree against `http://localhost:3000`, and that it would also reject a different literal, not just the string `localhost`.
- [ ] 2.9 Verify the new tests add no import outside the standard library and `.github/requirements-ci.txt`, and make no network call and spawn no Terraform binary or container runtime, by running the suite's existing `test_the_suite_imports_only_the_standard_library_and_pinned_dependencies`, `test_the_suite_imports_no_network_capable_module` and `test_the_suite_spawns_no_terraform_binary_or_container_runtime` and confirming they still pass. Claim only what those three assert.

## 3. Volume discovery — the defect

- [ ] 3.1 In `ansible/roles/platform_data_volume/tasks/main.yml`, add `platform_data_volume_by_id.files | default([]) | length > 0` as a second `when:` condition on the `Resolve the discovered device path` task. Comment why `default([])` is needed, giving the *undefined-register* reason (a run excluding the `find` task by `--tags`, or a second role invocation, never registers the variable at all) with the skipped-task case as secondary — not a claim about `when:` short-circuit ordering. Verify task 2.3's scenario now passes.
- [ ] 3.2 In the same task, replace `platform_data_volume_by_id.files[0].path` with `platform_data_volume_by_id.files | map(attribute='path') | sort | first`, and comment that `find` guarantees no ordering. Verify the `default` scenario still passes — it supplies the device explicitly, so this path is not taken there and must stay green.
- [ ] 3.3 Run `cd ansible/roles/platform_data_volume && molecule test --all` and verify all three scenarios pass — `default`, `no-device-discoverable` and `multiple-devices-discoverable`. **Read the SCENARIO RECAP, not just the exit code**: `molecule test --all` stops at the first failing scenario, so a red run leaves every scenario sorting after it unexecuted and absent from the recap. Observed on 2026-09-07: with `multiple-devices-discoverable` failing, `no-device-discoverable` never ran. A run is only evidence about the scenarios the recap actually lists. While the tree is red, run each scenario individually with `-s <name>` instead. Molecule's `--continue-on-failure` is not an option here: it applies only with `--workers`, and `--workers > 1` was observed on 2026-09-07 to refuse with `only supported in collection mode (galaxy.yml required)` — these are plain roles, not a collection.

## 4. Assertions for unconditionally-consumed required inputs

- [ ] 4.1 Add an `assert` as the **first** task in `ansible/roles/hardening/tasks/main.yml`, before `Ensure UFW is installed`, checking that `hardening_ssh_allowed_cidrs is defined` and is a sequence, with a `fail_msg` naming the variable and `ansible/inventory/group_vars/prod.yml` as where it is set. Do **not** assert `| length > 0` and do **not** add a default — design.md Decision 3. Verify tasks 2.4 and 2.5's scenario now passes and `hardening`'s `default` scenario still does.
- [ ] 4.2 Add the equivalent `assert` as the **first** task in `ansible/roles/deploy_user/tasks/main.yml`, before `Create the deploy group`, for `deploy_apps`. Same shape, same prohibitions. Verify all three existing `deploy_user` scenarios still pass — each supplies `deploy_apps`, so all three must stay green.
- [ ] 4.3 Do **not** add an assertion to `ansible/roles/tailscale/`. Verify by reading `tasks/main.yml:84` that `tailscale_auth_key` is consumed only under the join guard, so an unconditional assert would break a re-converge of an already-joined host — design.md Decision 3a. This task is complete when the reasoning is recorded and task 7.2's queue entry exists, not when code is written.
- [ ] 4.4 Record in `ansible/roles/hardening/defaults/main.yml`'s existing comment block that the absence of a default is now enforced by an assertion in `tasks/main.yml`, so the two cannot drift. Verify the comment names the assert's task. Do the same in `ansible/roles/deploy_user/defaults/main.yml` for `deploy_apps`, leaving its `ghcr_pull_*` wording alone — those are tolerated-when-absent by an existing requirement.

## 5. Platform stack — pin and root URL

- [ ] 5.1 In `platform/docker-compose.yml`, change `postgres:16` to `postgres:16.15` — the version production is running, read from `platform-postgres-1` on 2026-09-07 — and comment that the pin names the running version so the deploy stays on that PostgreSQL version. Do not claim the image is identical: see design.md Decision 5 on in-place tag rebuilds. Verify task 2.6's assertion now passes.
- [ ] 5.2 Change `GF_SERVER_ROOT_URL` from `http://localhost:3000` to `http://${GRAFANA_BIND_ADDRESS}:3000`, and comment that this is the same value the `ports:` publication already uses, rendered into `.env` by `platform-deploy.yml`. Verify task 2.8's assertion now passes, and that `docker compose -f platform/docker-compose.yml config` with a representative `GRAFANA_BIND_ADDRESS` renders the expected absolute URL.
- [ ] 5.3 Delete the file's header block (lines 1-10): the commit-message paragraph about a past change, and the caveat that the monitoring pins were never confirmed to resolve — all eight services the stack defines are running those exact tags in production and healthy. Verify the file now opens at `services:` and that nothing else in the repository cited those lines.

## 6. Comment placement

- [ ] 6.1 In `ansible/inventory/group_vars/prod.yml`, move the `ghcr_pull_username` / `ghcr_pull_token` comment block (currently lines 58-64) down to sit immediately above the variables it describes, leaving `platform_data_volume_subdirs` with only its own comment. Verify with the `ansible-lint` pre-commit hook — which does lint `ansible/` including `group_vars` — plus a reading of `git diff` confirming that only comment lines moved and the `!vault` block is byte-identical. Do **not** verify with `ansible-inventory --list` (needs `HCLOUD_TOKEN` and the Vault password) or the `ansible-playbook --syntax-check` hook (scoped to `ansible/playbooks/`, run with no `-i`, so it never loads `group_vars` at all).

## 6a. AGENTS.md gains the Molecule test row

Operator-directed, and postdating the plan review: recorded here rather than
queued because a change that adds three Molecule scenarios under a glob the
project's own testing table does not name should be the change that names it.

- [x] 6a.1 In `AGENTS.md`'s "Testing" section, change "**two** test commands" to three and add a Molecule row to the table — subject "the behaviour of an Ansible role on a host", command `molecule test --all` run from each role directory, glob `ansible/roles/<name>/molecule/<scenario>/`. Verify by reading the section as a dispatching agent would: it must now be possible to dispatch a test author for a role scenario without being told the pair by hand, which is what this change had to do.
- [x] 6a.2 Re-point the paragraph that begins "The second exists because" — with three rows, "the second" no longer identifies the `.github/tests` suite. Name that suite explicitly. Verify no other sentence in the section counts or ordinally references the rows.
- [x] 6a.3 Add a paragraph distinguishing the Molecule row from the `.github/tests` row, since they are near-opposites and easy to confuse: Molecule needs a container runtime and asserts what a role *does*; `.github/tests` is a static read that may not spawn one. Note that a `molecule.yml`'s image pin is therefore asserted by `.github/tests` while the behaviour that scenario exercises is asserted by Molecule. Verify the paragraph names `ansible/requirements-test.txt` and `ansible-verify.yml`.
- [x] 6a.4 Record the `molecule test --all` abort behaviour in that section: it stops at the first failing scenario, so later scenarios are neither run nor listed in the SCENARIO RECAP, and a verification claim must read the recap rather than the exit code. State the remedy (`-s <name>` per scenario while red) and that `--continue-on-failure` is unavailable, with its reason — it needs `--workers`, and `--workers > 1` requires collection mode. Verify the reason is stated, not just the conclusion, so nobody re-derives it.

## 7. Follow-on work recorded, not folded in

- [ ] 7.1 Add a `docs/change-queue.md` entry for design.md Decision 2's rejected alternative — whether `platform_data_volume` should refuse to proceed when discovery matches more than one device rather than picking deterministically. State that it is a policy decision about what else may legitimately be attached to the host, not a defect. Verify the entry names this change as its source.
- [ ] 7.2 Add a `docs/change-queue.md` entry for `tailscale_auth_key`: reporting a conditionally-consumed required input needs the join condition named once rather than restated, the existing expression carries a `POLARITY` warning that reading it wrongly silently stops a host joining the tailnet, and the role has no Molecule scenario to regress against. Verify the entry states both the work and why it is not folded in here.
- [ ] 7.2b Add a `docs/change-queue.md` note under entry 4 (`promote-molecule-to-a-required-check`): `molecule test --all` aborts at the first failing scenario, so a red run silently leaves later scenarios unexecuted. This does not weaken the "consecutive green runs" evidence — a green run did execute everything — but it means a red run establishes less than its recap appears to, which matters when reading the run log that entry keeps. Record that Molecule's own remedy is unavailable to this repository: `--continue-on-failure` applies only with `--workers`, and `--workers > 1` refuses with `only supported in collection mode (galaxy.yml required)` (observed 2026-09-07). The remedy that would work is a CI matrix over *scenarios* rather than roles, which also parallelises the suite's longest role — adjacent to the workflow reshaping entry 4 already names as its real remaining work. Verify the note is attached to entry 4 rather than replacing its existing content.
- [ ] 7.3 Reconcile `docs/change-queue.md` entry 3 with what this change moves. Strike its `platform/docker-compose.yml:1-4` bullet, since task 5.3 removes the lines it cites, leaving the entry itself and its other bullets in place. Then re-point — or drop the line numbers from — its citation of `ansible/roles/deploy_user/tasks/main.yml:124-148`, which task 4.2 shifts by inserting a new first task; that block is named in the entry as load-bearing and must not be lost to a stale reference. Verify no remaining citation in that entry points at the wrong lines.

## 8. Verification

- [ ] 8.1 Run `pre-commit run --all-files` and verify `terraform fmt`, `tflint`, `terraform validate`, `gitleaks`, `ansible-lint` and `ansible-playbook --syntax-check` all pass, against the group 1.4 baseline.
- [ ] 8.2 Run `molecule test --all` for every role carrying scenarios — the set `ansible-verify.yml` discovers — and verify all pass, so the branch head is green before `build`'s review is dispatched. Confirm each role's recap lists every scenario that role has, for the abort reason task 3.3 records; a role whose recap is short of its scenario count has not been fully verified even though the command reported a result.
- [ ] 8.3 Run `python3 -m unittest discover --start-directory .github/tests` and verify it passes.
- [ ] 8.4 Run `openspec validate fix-volume-discovery-and-consistency --strict` and verify it reports the change valid.
- [ ] 8.5 Verify no file outside proposal.md's Impact/Code list was modified, other than this change's own artifacts under `openspec/changes/fix-volume-discovery-and-consistency/`, which the Impact list does not enumerate and is not expected to. Reconcile the diffstat against the merge base line by line with that list.
