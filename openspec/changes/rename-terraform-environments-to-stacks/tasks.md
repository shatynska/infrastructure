## 0. A note on what this task list may contain

Branch and working-tree removal happen after this change's record has merged, which is after the commit that writes this file — a task for them would be unticked by construction forever. They are recorded in prose in the archive group instead. This is the repository's own convention (`AGENTS.md`, "Task lists, archived records, and disclosing what was not done"); the archive step itself is a task and is unaffected.

## 1. Provision this working tree before any verification result is claimed

- [x] 1.1 Record the baseline on trunk before anything moves: `python3 -m unittest discover --start-directory .github/tests` from the repository root, and `terraform init -backend=false && terraform validate` in each of `terraform/environments/prod/` and `terraform/environments/staging/`. A later run is a comparison against this, not an assertion on its own. Measured on trunk `74ea5b9` in a throwaway checkout of it: **712 tests, green**; both directories valid in this working tree. The figure this change was drafted against was 601 on trunk `032f923`, and it is stale by three of main's own continuous-integration changes — re-baselining is why it is measured here rather than carried forward.
- [x] 1.2 Confirm `python3` resolves PyYAML at the version `.github/requirements-ci.txt` pins (6.0.1). The suite parses every workflow with it; a tree without it does not fail loudly, it fails as an import error that reads like a broken test.

  This change needs no Molecule namespace and no Ansible collections: it touches no role. That is stated so that a reader does not go looking for the provisioning step `AGENTS.md` describes for Molecule and conclude it was skipped.

## 2. Move the directory

- [x] 2.1 `git mv terraform/environments terraform/stacks`. Verify the two `pipeline.yml` files, both `.terraform.lock.hcl` files and both `terraform.tfvars` moved with it, and that nothing under `terraform/modules/` moved.
- [x] 2.2 Re-run `terraform init -backend=false && terraform validate` in `terraform/stacks/prod/` and `terraform/stacks/staging/`. **This is the step that needs the untracked state `git mv` did not move**: `terraform/environments/prod/.terraform/` is gitignored and stayed behind, so the moved directory has no provider directory until `init` re-creates it. Verify both report valid.
- [x] 2.3 Re-point any local `direnv` arrangement by hand. **Checked, and nothing needed re-pointing.** This working tree carries no `.envrc` at all; the main checkout's repository-root `.envrc` is a bare `export HCLOUD_TOKEN=…` naming no path, so the move does not reach it; there is no `.envrc` under either stack directory; and `direnv` is not installed on this machine. The check is what makes that a fact rather than an assumption. Original text: The repository-root `.envrc` is prod's read-only-token file and `terraform/environments/staging/` held staging's; neither is tracked, so neither is in the diff and review cannot catch a stale one. Verify by `direnv status` or by confirming `HCLOUD_TOKEN` is set in each moved directory. Not performed by CI and not observable from it.
- [x] 2.4 Remove the abandoned `terraform/environments/` tree if `git mv` left an empty directory or an untracked `.terraform/` behind. Verify `terraform/environments` does not exist.

## 3. The four discovery bodies and the workflows

Apply design.md decision 2's rule: rename the word where it names the unit the pipeline discovers, plans, applies, drift-checks and converges; leave it where it names a GitHub Environment, the `environment` label, or the environment axis.

- [x] 3.1 `pr-validation.yml`, `apply.yml` and `drift.yml`: in the shared discovery body, `environments_root` becomes `stacks_root`, and every refusal message, comment and step name naming the mechanism follows. **The three bodies stay byte-identical** — `.github/tests` asserts it, and that assertion is what makes running one copy evidence about all three. Verify by diffing the three extracted bodies against each other before running the suite.
- [x] 3.1a The **changed-path resolution body** is duplicated too, across `pr-validation.yml` and `apply.yml`, and the same test class asserts those two identical — a pull request and the merge of that pull request must resolve the same set. It carries the `terraform/environments/*/*` case statement and the `ENVIRONMENTS` input. Edit both together and diff them the same way.
- [x] 3.2 `host-converge.yml`: the same rename of `environments_root` to `stacks_root` in its deliberately different fourth body, leaving `inventory_root` and `group_vars_root` alone — those enumerate `ansible/inventory/`, which entry 62 renames and this change does not. Its refusal messages follow the rename, including the one reporting a name that "yields an environment name that is not letters, digits, underscores and dashes". Design.md decision 2a settles which of this body's six derived surfaces are the stack and which are the environment; read it before editing, because this is the one body where both are true at once.
- [x] 3.3 All four workflows: the matrix key `environment` becomes `stack`, so every `matrix.environment.*` becomes `matrix.stack.*`; the `discover`, `affected` and `planned` job outputs named `environments` become `stacks`; `ENVIRONMENTS` and `ENVIRONMENT_NAME` become `STACKS` and `STACK_NAME`; and `REQUESTED_ENVIRONMENT` — `host-converge.yml`'s carrier for the dispatch input 3.4 renames — becomes `REQUESTED_STACK`. Every `needs.<job>.outputs.<name>` reader moves with the output it reads; `.github/tests` asserts that agreement for `apply.yml` only, so `drift.yml` and `pr-validation.yml` are checked by eye.
- [x] 3.4 `host-converge.yml`'s `workflow_dispatch` input `environment` becomes `stack`, description included. This is the one operator-facing rename in the change: a dispatch typed against the old input name silently converges everything rather than one host, because an unrecognised input is not an error.
- [x] 3.5 Leave four things untouched: `github_environment`; every `environment:` job key; every GitHub Environment name; and **`TARGET_ENVIRONMENT` / `target_environment` and the `--vault-id` label**, which name the Ansible group the converge play targets and are governed by *Host Configuration Names the Environment It Targets* (`openspec/specs/iac-host-configuration/spec.md`) — see design.md decision 2's fourth keeper and decision 2a. `TARGET_ENVIRONMENT`'s assignment becomes `${{ matrix.stack.name }}` and gains a comment saying why its name and its source differ; renaming the variable itself would break `ansible/playbooks/host-baseline.yml`, `.ansible-lint` and `.pre-commit-config.yaml`. Verify by grepping the four workflows for `environment` afterwards and confirming every surviving occurrence is one of those four.
- [x] 3.6 Keep both narrowing bodies free of `${{ }}`, taking every input through `env:` — the constraint that lets `.github/tests` extract and execute them. A rename is the easiest place to reintroduce an expression by accident.
- [x] 3.7 **`bash -n` over all 47 expression-free `run:` bodies in the four workflows: 0 failures. `shellcheck -S warning` over the same: 0 findings.** Two further bodies carry Actions expressions and are not shell until interpolated, so neither tool reads them. `actionlint` is **not** on PATH here and was not run — disclosed rather than claimed; `docs/change-queue.md` entry 6 owns adding it. Original text: Verify every `run:` body this change edits still parses: `bash -n` over each, and `shellcheck -S warning` where it is available. `actionlint` is on no path in this environment (`docs/change-queue.md` entry 6 owns adding it); disclose that rather than claiming the workflow was linted.

## 4. `.github/tests`

- [x] 4.1 Update every literal the suite asserts: the path, `environments_root`, the matrix key, the job outputs and the shell-variable names. Seven pre-existing modules carry one or more of them, measured after the rebase: `test_environment_agnostic_pipeline.py`, `test_a_second_environment.py`, `test_planned_environment_apply_stage.py`, `test_host_converge_workflow.py`, `test_ci_configuration.py`, `test_host_configuration_names_its_environment.py` and `test_the_suite_is_triggered_by_what_it_reads.py` — the last being one main added while this branch sat unrebased. Not every occurrence in them is an offence: `test_host_configuration_names_its_environment.py`'s subject is the environment axis, which this change keeps. **The discovery locator keys on the `terraform/environments` path literal, not on the step name** — `test_environment_agnostic_pipeline.py` records that a name-based locator was tried and was wrong — so it is the path in the locator that must move, and renaming step names alone moves nothing it reads. Verify by running the suite; a locator left behind fails loudly rather than skipping, which is what the suite was built for.
- [x] 4.2 Update the scratch trees the discovery tests build, which create `terraform/environments/<name>/pipeline.yml` directly. A scratch tree left under the old path makes discovery refuse an empty root and the test then passes for the wrong reason — the refusal it asserts is the one it would get from a correct implementation reading an absent directory.
- [x] 4.3 Update `.github/dependabot.yml`'s two Terraform directories and the assertion comparing that list against the set of directories carrying a lockfile. A directory this list omits is uncovered rather than partially covered.
- [x] 4.4 Do **not** rename the test modules or their test methods. `test_environment_agnostic_pipeline.py` and its siblings assert nothing about the word in their own names; renaming them churns several hundred identifiers and changes what no single one checks. Recorded against entry 62 in `docs/change-queue.md` rather than left implicit.

## 5. Path filters, documentation and `direnv` examples

- [x] 5.1 `pr-validation.yml`'s changed-files step: the `terraform/environments/**` filter becomes `terraform/stacks/**`. The workflow-level `paths:` filter on `apply.yml` is `terraform/**` and needs no edit — verify rather than assume.
- [x] 5.2 `README.md`, `docs/bootstrap-a-new-host.md`, `AGENTS.md`, `.envrc.example` and `ansible/.envrc.example`.
- [x] 5.3 `ansible/inventory/prod.hcloud.yml`, `ansible/inventory/staging.hcloud.yml` and the two `group_vars` files, which cite the path in comments. The **filenames** stay `prod` and `staging`: entry 62 renames them together with the Hetzner resources.
- [x] 5.4 `ansible/roles/hardening/README.md`, `ansible/roles/platform_data_volume/README.md`, `ansible/roles/platform_data_volume/tasks/main.yml` and that role's `molecule/default/prepare.yml`, each of which cites the path. No role behaviour changes; verify the Molecule scenarios are untouched by the diff.
- [x] 5.5 `docs/deferred-work.md`, whose entries quote the path and `matrix.environment`. Task 7.4 adds to it; this is the sweep of what is already there.

## 6. Specifications and the one change in flight

These are plan artifacts rather than implementation, so 6.1 and 6.2 were performed while this change was drafted and are ticked here.

- [x] 6.1 The **six** delta files under this change's `specs/`: **twenty-two MODIFIED requirements** selected by design.md decision 2's rule, applied across all nine capabilities in `openspec/specs/` rather than only the ones carrying the path, plus a `## Purpose` section on `iac-repo-foundations`, `iac-state-management` and `iac-data-volumes`. Verified with `openspec validate`: the change is valid.
- [x] 6.2 The sweep of `apply-host-configuration-through-a-gated-workflow`'s delta, performed in the plan commit, is **dropped**, and what it was for is folded into 6.1 instead. That change archived first and unswept, as `3282bd0`, so its record is now at `openspec/changes/archive/2026-09-11-apply-host-configuration-through-a-gated-workflow/` and an archived record is not edited. Rebasing re-applied the edit into the archive path through git's rename detection — silently, since the rename detection resolved what would otherwise have been a modify/delete conflict — and it was reverted. Its `ADDED` requirement *Host Configuration Is Applied by a Gated Workflow* is now live in `openspec/specs/iac-cicd-pipeline/spec.md`, so it is the twenty-second `MODIFIED` requirement in 6.1; its body is the text this branch had already swept, lifted from the pre-archive delta before that file was restored and verified byte-identical, unswept, to what archiving merged. See design.md decision 4.
- [x] 6.3 Verify no requirement and no scenario was renamed: the three requirement titles carrying *Environment* are unchanged, every `#### Scenario:` line matches the one in `openspec/specs/`, and the delta files contain no `## RENAMED Requirements` section. A scenario rename is refused by `openspec validate` outright, which is how this constraint was found.

## 7. The queue entries this change leaves behind

- [x] 7.1 `docs/change-queue.md`: delete entry 61, and update entries 62, 63 and 64 where they quote `terraform/environments/` or describe it as still to be renamed. Correct entry 60 while there: it says `.github/tests` "locates a discovery body by step name", and the suite locates it by the path literal the body contains — its own docstring records that a name-based locator was tried and was wrong. That inaccuracy is what entry 60 partly rests its "a script removes the indirection" argument on.
- [x] 7.2 Add to entry 62 the three renames this change deferred to it and why: the requirement titles carrying *Environment*, deferred here rather than taken; the scenario titles, which `openspec validate` refuses to see renamed inside a `MODIFIED` block at all, so that entry needs a mechanism rather than an edit; and the `.github/tests` module and method names. Add also that entry 62 owns **re-deriving `target_environment` and the `--vault-id` label**, which equal the stack name today only because the two coincide at one tenant.
- [ ] 7.3 **Do not merge this change without its implementation.** This change's delta specs oblige `terraform/stacks/` throughout. A merge of this branch in its plan-and-tests state would put that text on the trunk over a tree still holding `terraform/environments/`, together with forty failing tests, and the delta half of that is detected by nothing — `.github/tests` reads `openspec/` only for the citation-form check and never compares a cited path against the tree, and `openspec validate` checks structure rather than paths. This is a constraint on how this change ships, not a note for another change's operator, so it stays here rather than going to `docs/change-queue.md`.
- [x] 7.4 `docs/deferred-work.md`: disclose that the `.github/tests` module and method names, and the requirement and scenario titles, were deliberately left in the old vocabulary, with entry 62 named as their owner. This is the half of the rename a reader of the tree would otherwise read as an oversight.

## 8. Verify

- [x] 8.1 **Run: 783 tests, green.** It reached 762 with one failure first, and that failure was a defect in a derived test rather than in this change: `test_the_plan_comment_heading_and_the_comment_locator_agree_on_the_stack_root` harvested the plan heading out of its double-quoted shell assignment without unescaping, so it compared a backslash-bearing string against the bare YAML `body-includes` locator — two surfaces that agree in the file. It was returned to its author rather than edited here, repaired in the read with the assertion left byte-identical, and the four tests that establish the repaired read still tells two headings naming different paths apart are what take the count from 762 to 766. Original text: `python3 -m unittest discover --start-directory .github/tests` from the repository root. Compare against task 1.1's baseline of **712 green on trunk**: the expected end state is **783 tests, green** — the baseline plus this change's own seventy-one, being forty from the first derived pass, ten for the requirement added on rebase, four for the read repair, three for the widened sweep, ten for the stack-directory coverage, and four for the widened keeper needles. A count below 783 is a module that failed to import, not a suite that got smaller.
- [x] 8.2 `terraform init -backend=false && terraform validate` in `terraform/stacks/prod/` and `terraform/stacks/staging/`. **Both report valid.**
- [x] 8.3 `pre-commit run --all-files`. **All six hooks pass** — `terraform fmt`, `tflint`, `terraform validate`, `gitleaks`, `ansible-lint`, `ansible-playbook --syntax-check`. The syntax check failed on first run for an unprovisioned reason rather than a defect: `geerlingguy.docker` is gitignored and absent from a fresh working tree, so `ansible-galaxy install -r ansible/requirements.yml -p ansible/roles` was run first. A tree that skips that step reports a failure that reads like a broken role.
- [x] 8.4 **Zero occurrences survive outside `openspec/`** except two that are correct — but the first statement of this result was narrower than it read, and the correction is the useful part. The grep this task names is keyed on the path *with* its `terraform/` prefix and on an all-caps, case-sensitive identifier list, so three things escaped it and were found by code review rather than here: `Environments` in an `apply.yml` summary line (wrong case), `environments/prod` in a `tftest.hcl` comment and a queue entry (no prefix), and an "an stack" left by a line-scoped rename. All are fixed, and the sweep test is being widened so the *class* closes rather than the instances. What the grep establishes is therefore narrower than "the sweep is complete": it is that no occurrence of the prefixed path or the listed identifiers survives. The two correct survivors: `.github/tests/test_terraform_stacks_are_the_iterated_unit.py`, which must name the old vocabulary in order to assert its absence, and `TOKEN_ENVIRONMENT_NAMES` in `test_ci_configuration.py`, which names shell environment variables. Inside `openspec/`, the delta specs and the archived records are expected to carry it — the deltas apply at archive, and an archived record is history. Original text: Grep the whole tree, excluding `openspec/` wholesale — which is what `old_root_occurrences()` in the derived suite prunes, and is the exclusion this task is checked by — for the union of 3.3's rename list and the path: `terraform/environments`, `environments_root`, `ENVIRONMENT_NAME`, `ENVIRONMENTS`, `REQUESTED_ENVIRONMENT`, `matrix.environment`, `inputs.environment`, and the bare job outputs named `environments`. Every surviving occurrence SHALL be one this change decided to keep — a `github_environment`, an `environment:` job key, a GitHub Environment name, or a `target_environment` handle. The grep is what proves the sweep was complete rather than mostly complete; a stale occurrence in a comment or an `::error::` message fails nothing on its own.

## 9. Ship

- [ ] 9.1 Open the pull request once 8.1–8.4 pass and the code review has cleared, and let continuous integration run.
- [x] 9.1a **Checked: no drift issue is open.** `gh issue list --state all` returns exactly one, #22 *"Terraform drift detected: environments/prod"*, already CLOSED — so the rename orphans nothing and there is nothing to close by hand. Recorded rather than assumed, which is what this task asks for. Original text: Before or at the merge, list open drift issues — `gh issue list --state open --search 'Terraform drift detected'` — and close by hand any titled with `terraform/environments/`. `drift.yml` matches its dedup key on the whole title, so an issue opened under the old title is matched by nothing after the rename: a drifting stack opens a second issue and no clean run ever closes the first. Unlike the plan-comment effect, this one does not correct itself. If none is open, say so — the check is what makes the absence a fact rather than an assumption.
- [ ] 9.2 Wait for the operator's confirmation that it merged and that the deploy is healthy. Nothing ships from a local machine and no deploy command is run here.
- [ ] 9.3 **Confirm the effect**, which for this change is that the pipeline still finds both stacks under their new path: read the merge's `apply.yml` run and confirm its `discover` job emitted a matrix naming `prod` and `staging`, and that each stack's plan job ran under its own read-only secret. This observation is performable, so no waiver applies. A merge touching only `terraform/stacks/` paths raises production's approval request — that is the pipeline working, and it is approved or cancelled on its own merits, not on this change's.

## 10. Archive

- [ ] 10.1 Archive this change once 9.3 is confirmed, per `AGENTS.md`'s `ship` stage. **Check that all three `## Purpose` sections landed** — in `openspec/specs/iac-repo-foundations/spec.md`, `openspec/specs/iac-state-management/spec.md` and `openspec/specs/iac-data-volumes/spec.md`: archived changes in this repository carry a `Purpose` in a delta, but every precedent is a *new* capability, and whether `openspec archive` applies one to an existing capability is not established here. `iac-data-volumes` is the one to check first, being the capability whose Purpose nobody re-reads. Where it did not apply, correct the three lines in the archive commit and say so — that is a correction making the record say what happened, which `AGENTS.md` permits, rather than a change to what was decided.

  Branch and working-tree removal follow the record's own pull request and are not tasks here: remove the branch locally and on the remote, and remove the working tree from the repository's main working tree rather than from inside it.
