## 1. Baseline

- [ ] 1.1 Provision the worktree per `README.md` "Local setup" — `pre-commit install --hook-type pre-commit --hook-type commit-msg`, the pinned Molecule toolchain from `ansible/requirements-test.txt`, `ansible-galaxy install -r ansible/requirements.yml` — and verify by running `molecule --version` and confirming `ansible/roles/geerlingguy.docker/` exists. Until every step has been reached, report verification as not run.
- [ ] 1.2 Record the pre-implementation baseline: run `cd ansible/roles/platform_data_volume && molecule test --all` and verify it fails at `verify.yml`'s subdirectory-ownership assert with `object of type 'dict' has no attribute 'pw_name'`, on the `grafana` loop item. Record the line number the run itself reports rather than carrying one over from another document. A fix cannot be claimed against a failure never observed here.
- [ ] 1.3 Run `python3 -m unittest discover --start-directory .github/tests` from the repository root and verify it passes on the tree as it stands, so any later failure is attributable to this change.

## 2. Derived test (before implementation)

- [ ] 2.1 Add scenario discovery to `.github/tests/test_ci_configuration.py` that globs `ansible/roles/*/molecule/*/molecule.yml` and excludes role directories whose names appear in `ansible/requirements.yml`'s `roles:` list, reading that manifest with the already-pinned PyYAML. Resolve each entry to a directory name the way `ansible-galaxy` does — `name` where given, else the `src` basename with any `.git` suffix and version qualifier stripped. Verify it returns the same eight scenario files on the provisioned tree (where `ansible/roles/geerlingguy.docker/molecule/default/molecule.yml` is present) as on a tree with that directory removed — spec scenario "Installed Galaxy content is not held to this repository's pinning obligation".
- [ ] 2.1a Verify the exclusion never fails open: an entry the resolution cannot name, and a missing or unparseable `ansible/requirements.yml`, must fail the check identifying the entry or the file rather than yielding an empty or partial exclusion set. Verify with a temporary malformed manifest and a temporary `src:`-only entry, reverting both.
- [ ] 2.2 Verify the exclusion is derived, not hardcoded: temporarily add a second `roles:` entry to `ansible/requirements.yml` naming an existing role directory, confirm discovery drops it, then revert. A hardcoded `geerlingguy.docker` string would pass 2.1 and fail this.
- [ ] 2.3 Add an assertion that every discovered scenario declares each platform's image with an `@sha256:` digest, and verify it **fails** on the current tree naming all eight scenarios — spec scenario "Every scenario's platform image is pinned by digest". Check every entry in a scenario's `platforms:` list, not only the first.
- [ ] 2.4 Add an assertion that a discovered scenario declaring no `platforms:`, or a platform with no `image:`, fails by name rather than being skipped, and verify it with a temporary fixture — spec scenario "A scenario declaring no platform image fails rather than being skipped".
- [ ] 2.5 Add an assertion that scenarios naming the same image repository name the same digest, and verify it fails on a temporary fixture where one scenario's digest differs — spec scenario "Scenarios sharing an image repository agree on its digest". It is scoped to a shared repository, so a scenario on a different image must not fail it.
- [ ] 2.6 Verify discovery is genuine and not an enumeration: add a scenario file at a new temporary role path, confirm the assertions in 2.3–2.5 cover it with no test edit, then remove it. Asserting that the glob "is not a fixed list" does not establish this and is not an acceptable substitute.
- [ ] 2.7 Verify the new code adds no import outside the standard library and `.github/requirements-ci.txt`, and makes no network call and spawns no Terraform binary or container runtime, by running the suite's existing `test_the_suite_imports_only_the_standard_library_and_pinned_dependencies`, `test_the_suite_imports_no_network_capable_module` and `test_the_suite_spawns_no_terraform_binary_or_container_runtime` and confirming they still pass. Those three are what the requirement at `openspec/specs/iac-cicd-pipeline/spec.md` names; claim no broader property from them.

## 3. Pin the platform image

- [ ] 3.1 Replace `image: geerlingguy/docker-ubuntu2204-ansible:latest` with `image: geerlingguy/docker-ubuntu2204-ansible:latest@sha256:0172e3b586fc316491b5ac080e6b24c5f66cde985c27c625025f13f66a8cd21f` in the eight scenario files this repository owns, and verify `grep -rl 'ubuntu2204-ansible:latest$' ansible/roles/ --exclude-dir=geerlingguy.docker` produces empty output. The exclusion is needed because the installed Galaxy role's own scenario is not this change's to edit.
- [ ] 3.2 Add a brief comment beside the pin in one scenario — or extend the existing shared-rationale pointer the other scenarios already use for `ANSIBLE_ROLES_PATH` — recording that the image publishes no version tag, so a digest is the only exact form, and that refreshing it is manual. Verify by reading the file that a reader learns why the digest is there without opening `design.md`.
- [ ] 3.3 If the Docker driver rejects the combined `repo:tag@sha256:…` form at `create`, fall back to the bare `repo@sha256:…` per design decision 1, moving the tag into 3.2's comment. Verify by observing a successful `molecule create` in whichever form is adopted; record which one was used and why.
- [ ] 3.4 Verify the assertions from group 2 now pass: `python3 -m unittest discover --start-directory .github/tests`.

## 4. Fix the verify assertion

- [ ] 4.1 In `ansible/roles/platform_data_volume/molecule/default/verify.yml`, guard the owner and group name comparisons as `(item.stat.pw_name | default('')) == item.item.owner or (item.stat.uid | string) == item.item.owner` and the `gr_name`/`gid` equivalent, leaving `exists`, `isdir` and `mode` untouched. Verify by reading the diff that nothing asserted was removed or weakened.
- [ ] 4.2 Update the assertion's surrounding comment to record why the guard is there — that a declared numeric owner has no `passwd` record for every uid, so `stat` returns no `pw_name` and the unguarded access raised before the uid comparison could run. Verify the comment names the mechanism, not just the symptom.
- [ ] 4.3 Run `cd ansible/roles/platform_data_volume && molecule test --all` and verify the scenario now passes, and that the ownership assert is genuinely reached — not skipped — for both the `prometheus` and `grafana` fixtures.
- [ ] 4.4 Verify the assertion still discriminates: temporarily change one fixture's declared `owner` in `converge.yml` to a value the role will not produce, confirm the scenario fails on that assert with the intended `fail_msg`, then revert. A guard that makes a broken assert unreachable would pass 4.3 identically.

## 5. Records and conventions

- [ ] 5.1 Widen `AGENTS.md`'s Testing-table "Subject" column so the `.github/tests` row covers static assertions over configuration the pipeline executes, not only workflows/`dependabot.yml`/`.pre-commit-config.yaml`. Verify by re-reading the row as a test author would: it must make clear where an assertion over a `molecule.yml` belongs.
- [ ] 5.2 Add a `docs/deferred-work.md` entry recording that the digest has no automatic refresh mechanism, why Dependabot does not cover it, and what would make revisiting worthwhile. Verify the entry states what is deliberately not done rather than describing work merely waiting.
- [ ] 5.3 Update `README.md`'s Molecule section with the one-line manual refresh procedure and where the digest comes from. Verify by following the stated procedure and arriving at the digest now pinned.
- [ ] 5.4 Correct `docs/change-queue.md` entry 8's stated cause (uid `65534` resolves to `nobody`; the failing fixture is `grafana`, `472/472`) and its stale `verify.yml:189` line reference, and note against entry 4 that its two named blockers are closed by this change pending its merge. Verify entries 5 and 8 are left in place — they are deleted at archive, not now.

## 6. Verification

- [ ] 6.1 Run the full suite the way CI does — `molecule test --all` per role across all five roles this repository owns that carry scenarios — and verify every scenario passes. Record the outcomes as **local** results in this change's own artifacts; they are not entry 4's evidence, which `docs/change-queue.md` requires to come from runs on pull requests.
- [ ] 6.2 Run `pre-commit run --all-files` and verify `ansible-lint`, `ansible-playbook --syntax-check` and `gitleaks` pass on the tree.
- [ ] 6.3 Run `python3 -m unittest discover --start-directory .github/tests` from the repository root and verify the whole suite passes, including the assertions added in group 2.
- [ ] 6.4 Verify no file outside this change's stated Impact list is modified: `git diff --stat` against the branch point. `AGENTS.md` is on that list (task 5.1); the installed `ansible/roles/geerlingguy.docker/` is gitignored and must not appear at all.
- [ ] 6.5 After the pull request is opened and `ansible-verify.yml` has run on it, record that run's per-role outcome and duration in `docs/change-queue.md` entry 4's baseline table, as a third data point toward promotion. This is the evidence entry 4 asks for; task 6.1's local run is not a substitute for it.
