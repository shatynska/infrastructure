Verification commands referenced below, from this project's conventions:

- `python3 -m unittest discover --start-directory .github/tests`, from the repository root
- `ansible/scripts/run-molecule test --all`, from a role directory — read the SCENARIO RECAP, not the exit code
- `pre-commit run --all-files`, which carries `terraform fmt`, `tflint`, `terraform validate`, `gitleaks`, `ansible-lint` and `ansible-playbook --syntax-check`
- `openspec validate --all`

Sections 1–5 land through this change's first pull request. Section 6 is the `ship:confirm` observation, and one half of it cannot be made on this change's own pull request — `design.md` Decision 3 says why, and section 6 records what is observed where. Section 7 is the archive. Removing the branch and the working tree happens after this record's own pull request merges, which is after the commit that writes this file, so those steps are recorded in prose in `proposal.md` and in `AGENTS.md` rather than as tasks here.

**Order matters between sections 1 and 2, and in one direction only.** The scans must be running in `.github/tests` before the filter stops selecting the suite on inventory changes. Landing them in one pull request makes the ordering internal, but do not reverse it within the branch: a commit that narrows the filter while the inventory scan still lives in `ops_user`'s verify play is a commit at which a pull request editing `ansible/inventory/group_vars/prod.yml` is scanned by nothing.

## 1. The repository-scope scans move to `.github/tests`

- [x] 1.1 Establish the file set the relocated scans read, per `design.md` Decision 5: the repository's tracked files, enumerated with `git ls-files`. Three behaviours are decided there and all three belong in this helper rather than being rediscovered by whoever writes the scans:
      - Where the enumeration fails — no `git`, or not a repository — the assertion **fails** and names the enumeration as the cause; it does not skip. This is the one place `.github/tests`' existing `require_external_tools` skip-guard must not be copied, and the reason belongs in the docstring: a guarded skip is right for an assertion whose absence leaves another check standing, and wrong for a scan for committed credential material, where a skip reports success for a property nothing examined.
      - The helper returns **bytes**, and the scans match bytes. Decoding every tracked file as text makes an undecodable file — none is tracked today, and nothing prevents one — a hard failure that examined nothing, which is this decision's own failure mode reached from the other side. The markers are ASCII; nothing is lost.
      - A listed path **absent from the working tree fails, naming that path**. A sparse or interrupted checkout cannot establish a property over the committed tree, and it is a state to hear about rather than to scan around.

      Put it, and the three relocated scans, in `.github/tests/test_ci_configuration.py` rather than in a new module. That module is where this repository's conventions record the suite's own constraint assertions as reading — no network call, no credential, no container runtime, no Terraform binary — and the enumeration exception the delta bounds is exactly the kind of thing those assertions must be able to see. A new module would put the one subprocess this suite admits outside the checks that police subprocesses; widening those assertions across modules is `docs/change-queue.md` entry 45's, not this change's.

      Write this helper first; 1.2 through 1.4 all consume it.
- [x] 1.2 Relocate the GitHub/GHCR token-marker scan out of `ansible/roles/deploy_user/molecule/default/verify.yml:419`. It greps for `gh[pousr]_[A-Za-z0-9]{20,}` and `github_pat_[A-Za-z0-9_]{20,}`; the reimplementation reads 1.1's file set, and its failure message names the offending files as the sibling refusals in that module do. Delete the task and its `assert` from the verify play, leaving a comment at the deletion point recording where the assertion went and why — the scenario's own header still lists it, and a reader finding the header without the task must not conclude the check was dropped.
- [x] 1.3 Relocate the `ghcr_pull_token` literal scan from the same file, at line 457. **This is the task where a casual reimplementation loses the property that matters**, and the source comment says so in as many words: both scenario converge files assign the token as a YAML folded scalar, so the assignment line is bare and the `lookup('env', ...)` that makes it acceptable sits on following lines. It is already Python — move it as nearly as-is as the new home permits, keeping the continuation-absorbing loop rather than rewriting it against a YAML parser, and carry across the comment explaining why the two scenario files are deliberately *not* excluded by path. Its file set changes from its own `rglob("*.y*ml")` over the repository to 1.1's tracked files filtered to the same extensions — the YAML restriction is the scan's subject rather than an optimisation, since what it matches is a YAML assignment. Delete the task and its `assert` from the verify play and leave a comment at the deletion point, exactly as 1.2 does; a scan copied rather than moved leaves the suite asserting what `.github/tests` now asserts, on a subset of the pull requests.
- [x] 1.4 Relocate the operator private-key scan from `ansible/roles/ops_user/molecule/default/verify.yml:631`, **widening its root from `ansible/inventory/` to 1.1's file set** per `design.md` Decision 4. Leave the scan at line 618, over the role's own directory, where it is. The assembled-pattern technique carries over unchanged and for the same reason: a file searching for the private-key delimiter matches itself if the pattern is spelled literally, and that is as true in a Python test module as in a verify play. Adjust the surviving `assert` so it no longer references the removed register, and correct the scenario header, which currently describes a scan over both roots.
- [x] 1.5 Assert the negative case for all three scans against `tempfile` fixtures rather than by introducing a marker into the working tree. A real token literal committed even briefly trips `gitleaks` in `pre-commit` and risks being left behind; a fixture makes the red state permanent evidence rather than a one-off observation, which is the argument task 3.4 makes for the premise check and it applies here too. Because the scans read tracked files, the fixture must exercise the matcher over a supplied file list rather than over the repository — so write them to take that list as a parameter, which 1.1's helper supplies in production. **Assemble the fixture payloads at run time, from halves, for the same reason 1.4's search pattern is assembled**: a literal `gh[pousr]_…` or private-key delimiter written into `.github/tests/test_ci_configuration.py` is itself a tracked file, so the relocated scans would match their own module — and `gitleaks` in `pre-commit` would very likely block the commit that introduced it. The pattern being assembled is not enough on its own; the payload it is tested against must be too.
- [x] 1.6 Confirm the scans are clean on the current tree before relying on them. Only one widens — 1.4's private-key scan, from `ansible/inventory/` to every tracked file — so it is the one that could inherit a pre-existing hit; 1.2's keeps the repository root it had and 1.3's keeps its YAML restriction. During planning the token-marker and private-key patterns were each run over all 448 tracked files and matched nothing. Re-run all three and record the tracked-file count in this task's completion note, since it will have moved; a hit is then a real finding rather than something the widening dragged in.
      **Performed 2026-09-11.** 449 tracked files; the token-marker and private-key patterns each matched nothing, and no `ghcr_pull_token` assignment carries a committed literal. The count moved by one from planning's 448 because the derived-test module had landed by then.
- [x] 1.7 **Widen the suite's own spawn audit to admit the enumeration binary.** `.github/tests/test_ci_configuration.py` carries `SPAWNABLE = {"bash", "sh"}`, read by two assertions — one over the module itself and one over every module in the suite directory — and 1.1's tracked-file enumeration adds a third spawnable name. Without this the suite goes red on the commit that introduces the helper, naming the module rather than the cause, which reads as a broken test rather than as an audit doing its job. Add the name **with the reason in place**: it is admitted because it is present wherever the repository was checked out, which is the same bound the delta puts on the exception, and not as a general relaxation. This task was identified by the test author, whose first new module tripped the audit; no earlier review round named it.
- [x] 1.8 Run `python3 -m unittest discover --start-directory .github/tests` and confirm green; then run `ansible/scripts/run-molecule test --all` from `ansible/roles/deploy_user` and from `ansible/roles/ops_user` and read the SCENARIO RECAP of each, confirming it names every scenario the role has. `deploy_user` has three and `ops_user` two; a recap naming fewer means the run stopped at a failure and the scenarios sorting after it did not execute.

## 2. Change detection stops naming what the suite does not read

- [x] 2.1 In `.github/workflows/ansible-verify.yml`, replace the `ansible:` filter's single `ansible/**` (line 101) with that positive followed by negations for `ansible/inventory/**`, `ansible/playbooks/**`, `ansible/requirements.txt` and `ansible/.envrc*`. Keep `ansible/**` at the head: `dorny/paths-filter` requires a file to be matched by a positive pattern before a negation can exclude it. Write the comment block against `design.md` Decisions 1 and 2 — what established each exclusion, that `ansible/requirements.yml` and `ansible/requirements-test.txt` are deliberately **not** excluded and that `docker`'s scenario reads the first of them directly, that `ansible/ansible.cfg` is deliberately not excluded and that two scenario comments are wrong about it, and why the polarity is exclusions rather than inclusions. Do **not** touch `pr-validation.yml`'s `ansible:` filter (line 578); that it stays at `ansible/**` is what covers the excluded paths, and the two filters ceasing to be identical is the point rather than a drift.
- [x] 2.2 Correct the aggregating job's message on the correct-skip branch, at `.github/workflows/ansible-verify.yml:287`. *"Nothing under the configuration directory changed, so the suite was skipped rather than run"* is false once a path under `ansible/` can change and be excluded; it must say that nothing the suite reads changed, and it should name the lint tier as what did run over those paths, because that is the reader's next question. The gate body is free of `${{ }}` so `.github/tests` can execute it standalone — keep it that way, and update that module's table of gate outcomes to the new wording.
- [x] 2.3 **Revise the existing assertion that would otherwise survive stating the opposite of this change, and would still pass.** `.github/tests/test_ci_configuration.py:4661` declares `CONFIGURATION_PATHS` including `ansible/playbooks/host-baseline.yml` and `ansible/inventory/prod.hcloud.yml`, and `test_the_change_filter_selects_the_whole_configuration_directory` at `:4677` asserts the `ansible-verify` filter selects **every** one of them. It will keep passing after task 2.1, because its matcher is `any(gh_glob_matches(pattern, path) for pattern in patterns)` over all declared patterns and the positive `ansible/**` still matches each — the negations are invisible to it. That leaves a `SPECIFIED`-labelled check whose claim is false and which can never go red on it, which is precisely the defect this capability exists to refuse. Split the tuple: the paths the suite reads stay and are asserted selected; `ansible/playbooks/host-baseline.yml` and `ansible/inventory/prod.hcloud.yml` move to a new tuple asserted **not** selected. Make the matcher negation-aware — last matching pattern wins, per `dorny/paths-filter` — or the new assertion is as blind as the old one. Rewrite the docstring: its whole-directory argument is now the lint filter's, in `pr-validation.yml`, and it should say so and point there. This is not weakening a test to reach green; the proposition changed in the specification and the assertion is re-pointed at what now holds. Record it as an obsolete-test entry in `test-plan.md`.
      **Performed 2026-09-11, and the negation-blindness was confirmed before it was fixed rather than assumed.** With the narrowed filter declared, the old `any(gh_glob_matches(...))` matcher still reports `ansible/inventory/prod.hcloud.yml` as selected — that is the check passing while asserting the opposite of the requirement. The replacement `_selects()` helper resolves last-match-wins and reports it not selected, while still reporting `ansible/roles/x/tasks/main.yml` and `ansible/requirements.yml` selected. Both tuples are asserted, in opposite directions.
- [x] 2.4 **Assert that the lint tier stays unexcluded**, which nothing does today. `.github/tests/test_ci_configuration.py`'s `test_an_ansible_path_filter_selects_changes_under_ansible` establishes only that `pr-validation.yml` declares *an* `ansible:` entry in its filter — it reads the block as a string and looks for the key. That was adequate while both filters said the same thing; it is not adequate once this change makes the lint tier the compensating coverage for four excluded paths, because narrowing or negating that filter would withdraw the coverage with nothing failing. Add an assertion, using the negation-aware matcher 2.3 builds: `pr-validation.yml`'s `ansible:` filter carries `ansible/**` and **no** negation, and it selects `ansible/playbooks/host-baseline.yml` and `ansible/inventory/prod.hcloud.yml` — the two paths 2.3 asserts `ansible-verify` does *not* select. The pair read together is the whole safety argument of this change, stated as two assertions that must disagree.
- [x] 2.5 Confirm nothing else in the repository asserts or documents that the Molecule suite is triggered by `ansible/**`. Grep **repository-wide**, excluding only `openspec/changes/archive/` — not just `.github/`, `docs/`, `README.md` and `openspec/specs/`, since `AGENTS.md` and the role-level READMEs under `ansible/` are prose that can carry the claim too. Account for every hit in this task's completion note in one of three classes: `pr-validation.yml`'s lint filter (correct, unchanged); `host-converge.yml`'s merge trigger (correct, unchanged — a converge applies what the playbooks and inventory say, so it is triggered by paths the suite excludes, and that divergence is now deliberate on both sides); and this workflow's own (this change). At planning time the three within `.github/` were `pr-validation.yml:579`, `host-converge.yml:42` and `ansible-verify.yml:101`.

## 3. The premise the exclusions rest on gains a check

      **Performed 2026-09-11**, repository-wide over `*.yml`, `*.yaml`, `*.md` and `*.py`, excluding `openspec/changes/archive/`. Every hit falls in one of the three classes: `pr-validation.yml:579` (lint filter, correct, unchanged); `host-converge.yml:42` (merge trigger, correct, unchanged — a converge applies what the inventory and playbooks say, so it is deliberately triggered by paths the suite now excludes); and `ansible-verify.yml:153`, this change's own, now the positive at the head of the exclusion list. The remainder are this change's own artifacts and the in-flight `apply-host-configuration-through-a-gated-workflow`'s, which describe its own trigger and are not this change's to edit. **No prose in `AGENTS.md`, `README.md` or any role README claims the suite is triggered by the whole directory**, so nothing outside the workflow needed correcting.
- [x] 3.1 Add an assertion to `.github/tests` over every scenario definition this repository authors: **no controller-side read of a repository file** may reach outside the paths `design.md` Decision 1 enumerates. Both halves of that criterion are load-bearing and the task fails without either. *Controller-side*, because a read of the managed node cannot be affected by a filter over this repository — roughly twenty undelegated `slurp` tasks read absolute paths inside the container, and `slurp` has **no** `remote_src` option, so delegation rather than any module option is what puts a read on the controller. *Of a repository file*, because delegated tasks that derive a value from an already-registered fact open nothing — `docker/molecule/default/verify.yml:99` and `:105` and `swap/molecule/default/verify.yml:80` are three, and `image_prune/molecule/default/prepare.yml:58` runs `docker save` into `/tmp` on the controller while reading no repository file at all. A check drawn wider than this criterion is red on roughly twenty-five sites on day one, and the only ways out are to permit them wholesale, which makes it meaningless, or to narrow it by a rule nobody wrote down, which is how the first version of this design went wrong.
- [x] 3.2 Build the route list from `design.md` Decision 1 rather than from delegation alone: a file-reading `lookup('file' | 'template' | 'fileglob' | 'ini' | 'csvfile' | 'first_found' | 'unvault', …)` wherever it appears; controller-resolved inclusion of **variables** (`vars_files`, `include_vars`, `include_role`'s `vars_from`) and of **tasks or plays** (`import_playbook`, `include_tasks`, `import_tasks`, `include_role`'s `tasks_from`); a `src:` on any module that sends a file to the managed node (`copy`, `template`, `script`, `unarchive`, `assemble`) without `remote_src`; any `src:` or path argument on a task that **does** delegate to the controller; and a path in a scenario's provisioner `env:`. **The task/play inclusion route is not optional and not hypothetical** — `platform_data_volume/molecule/multiple-devices-reverse-order/` reads three sibling files through `import_playbook` today, and a check built without it passes them silently. Close the list by **refusing**: an unrecognised construction carrying a path that resolves into this repository fails rather than being passed over, for the same reason Decision 2 makes the filter an exclusion list. Derive the set of authored scenarios the way the existing image-pin and instance-name checks do, excluding installed Galaxy content from `ansible/requirements.yml` rather than from a hardcoded list, so a scenario added later is covered without an edit.
- [x] 3.3 Enumerate the permitted set explicitly, from `design.md` Decision 1's table and from nothing else. **Key each entry on the file, the construction and the target it resolves to — never on a line offset**, per `design.md` Decision 1a: section 1 of this very change deletes three tasks from two of these files, so a permitted set recorded as `file:line` is wrong on the commit that lands it, reporting the surviving role-local scans as violations and permitting whatever text slid onto the recorded numbers. The line numbers below locate each read as of the sweep, for finding it rather than for matching it. Enumerate the **reads**, not the paths they reach: a permitted-path set would silently admit a second read of `ansible/requirements.yml` that nobody looked at, where the point of the enumeration is that a read added later is considered rather than inherited. **Give each entry an expected occurrence count *per file*, and fail a file carrying more than its entry allows** — the identity alone does not separate one read from two, and the natural place to add a second read of `ansible/requirements.yml` is `docker/molecule/default/verify.yml`, where the permitted one already is: same file, same construction, same target, indistinguishable under plain set membership. Per file rather than in total, and the two declaration entries are why: each is one declaration across seventeen `molecule.yml` files, so a total would be seventeen and would go red every time a scenario is added or removed. Every count is one today, and stays one as scenarios come and go. After section 1's three relocations the set is nine reads plus two per-scenario declarations: `deploy_user/molecule/default/verify.yml:563` and `ops_user/molecule/default/verify.yml:618` (role-local private-key scans), `ops_user/molecule/default/verify.yml:666` (delegated `slurp` of the role's own `README.md`), `ops_user/molecule/revocation-steady-state/verify.yml:59` (delegated `ansible-playbook` over its own scenario directory), `swap/molecule/default/verify.yml:71` (`defaults/main.yml` lookup), `docker/molecule/default/verify.yml:92` (`ansible/requirements.yml` lookup), the three `import_playbook` lines at `platform_data_volume/molecule/multiple-devices-reverse-order/{converge,prepare,verify}.yml:8`, and every authored `molecule.yml`'s `ANSIBLE_ROLES_PATH` **and** its `dependency.options.requirements-file`, which resolves to `ansible/requirements.yml` — the second of those was found by the test author, not by either sweep, and omitting it makes the check red on seventeen sites the day it lands. A check that infers the permitted reads rather than listing them cannot refuse one added later, which is the whole point of it.
      **The permitted set was confirmed against the real tree rather than written from the table alone**, and the first attempt was wrong in the dangerous direction: the check detected only 20 reads and passed **vacuously**, missing every `playbook_dir`-based one. The cause was in `_resolve_target` — stripping a variable prefix leaves a leading `/`, which read as an absolute path on the managed node. After correcting it, and after resolving `MOLECULE_PROJECT_DIRECTORY` against the role directory rather than the scenario directory, the check finds all 47 reads across 14 distinct identities, and the nine reads plus two declarations that survive the relocations are exactly the permitted set.
- [x] 3.4 Establish that the check is **capable of red**, and that it ignores the managed-node class. The property is what matters; the method is fixtures rather than commit sequencing, because confirming red "on the tree before section 1 lands" would mean landing section 3 first and is not worth reordering the branch for. Five fixtures:
      - a scenario body reaching an unpermitted path, asserted to name that scenario and that read;
      - a **second** read of an already-permitted path, asserted to be refused too — and pin it to the **hard** case: same file, same construction, same target as the permitted entry, so the fixture exercises the occurrence count rather than passing for free on a different file;
      - a construction the check does not recognise, carrying a repository-resolvable path, asserted to **fail naming it** — this is the refusal-on-unrecognised branch, the design's chief anti-staleness property, and by this task's own argument a branch never seen red could be implemented as a pass-over with nothing noticing;
      - an undelegated `slurp` of an absolute container path, asserted to be passed over;
      - a permitted read shifted to a different line, asserted to still be recognised.

      A check of this kind that has never been seen red establishes nothing, one never seen to *ignore* the managed-node case will be narrowed by whoever meets the twenty-five-site version of it, and one never seen to survive a line shift is the defect `design.md` Decision 1a exists to prevent.
- [x] 3.5 Add the assertion over the filter's *declared* patterns — that `ansible/**` is present as a positive, that each of the four negations is present, and that no negation appears which this change did not establish. State in its docstring that it asserts the declaration and not the behaviour, that reproducing picomatch would need a dependency this suite is forbidden from taking, and that the behaviour is established by observation per section 6.

## 4. Verification

- [x] 4.1 `python3 -m unittest discover --start-directory .github/tests` from the repository root, green.
      **Performed 2026-09-11: 633 tests, OK.** The baseline before this change was 601; the derived tests took it to 629 with 31 failures, and the 4 that outlived the first implementation pass are recorded in 4.2's neighbours below. One of those four was an existing assertion doing its job — `test_the_workflow_names_no_role_literally` caught the new filter comment naming three roles in prose, which is exactly what that check forbids in this workflow; the comment was reworded rather than the check weakened.
- [ ] 4.2 `ansible/scripts/run-molecule test --all` from each of the seven role directories this repository authors, reading each SCENARIO RECAP and confirming it names every scenario that role has. Only `deploy_user` and `ops_user` are touched by this change, but the suite is what the change is about and a claim that it is unaffected is worth more when it was run. Record the per-role recap in this task's completion note. Before starting, bring this working tree's namespace to the project's initial state — `ansible/scripts/run-molecule destroy --all` from each role directory, and remove `.molecule-home/tmp/` — since Molecule does not clear it itself and a tree resumed after a crashed run starts from whatever the crash left.
- [x] 4.3 `pre-commit run --all-files`, green.
      **Performed 2026-09-11**, after provisioning. The first run failed `ansible-playbook --syntax-check` on `geerlingguy.docker` not being found: the Galaxy role is gitignored and this working tree had never installed it. `ansible-galaxy role install -r ansible/requirements.yml -p ansible/roles` fixed it, and this is the unprovisioned-tree case `AGENTS.md` warns about arriving in practice — the failure named a missing role and read as a broken dependency rather than as an unprovisioned machine.
- [x] 4.4 `openspec validate --all`, green.

## 5. Record what this change learned about the suite's cost

- [x] 5.1 Confirm entries 65 and 66 are in `docs/change-queue.md` — `cache-the-apt-index-within-a-converge` and `bound-the-molecule-matrix-with-a-timeout`. Both were identified by the investigation that produced this change and are deliberately outside it; `proposal.md`'s Non-Goals say why. They sit outside this change's own artifacts because a note kept inside one is archived with it.
- [x] 5.2 Add entry 67 to `docs/change-queue.md` for per-role selection, carrying what this change established and did not act on: that `image_prune`'s scenarios converge `docker`, that `ops_user`'s default converges `docker` and `deploy_user`, and that a correct per-role matrix therefore needs the reverse closure of a dependency graph derived from scenario text — which is why it is a change of its own rather than a refinement of this filter. Note that `design.md` Decision 1's table is the sweep such a change would extend.

## 6. The observation

- [ ] 6.1 **Observable on this change's own pull request**: it touches two files under `ansible/roles/`, so the Molecule matrix must have rows and the suite must run. Confirm from the run that all seven `molecule (<role>)` jobs executed and that the aggregating job concluded success. This is the direction whose failure is silent — a filter that wrongly excludes — and it is the one this change can prove.
- [ ] 6.2 **Not observable on this change's own pull request**: that a pull request whose whole `ansible/` footprint is excluded paths starts no container. It needs a diff this change cannot produce. Propose it as the `ship:confirm` observation to be made on the next pull request touching only such paths — what to look at is the `Ansible Verify` run's job list, and what would mean it worked is a `discover` job that succeeded, no `molecule (<role>)` job at all, and an `ansible-verify` job concluding success with the message task 2.2 wrote. Record the pull request number and the observed job list. Where no such pull request arrives within this change's life, say so plainly and name the waivable class rather than reporting the gate as met.

## 7. Archive

- [ ] 7.1 Once the effect is confirmed or the gate waived, bring the branch back to the freshly fetched trunk and commit this change's specification record there, opening the pull request that carries it — the last of this change's pull requests.

## Not performed

- 4.2 — the Molecule suite across all seven authored roles.
  Reason: no container runtime is available in the authoring environment (`docker info` fails), so Molecule cannot create an instance. Reporting this as passed, or running it and reading a skip as success, is the precise failure `AGENTS.md` names — verification that cannot reach what it needs reports success rather than failing. **What this leaves unverified is narrow and is verified elsewhere in this change's own pull request**: the edits to `deploy_user`'s and `ops_user`'s verify plays are deletions of three tasks and one `assert` clause, `ansible-lint` and `ansible-playbook --syntax-check` both pass over them, and the suite runs in full in continuous integration on this pull request because it touches `ansible/roles/**` — which is task 6.1's observation. Read that run's SCENARIO RECAP for `deploy_user` (three scenarios) and `ops_user` (two) before merging; a recap naming fewer means the run stopped at a failure.
