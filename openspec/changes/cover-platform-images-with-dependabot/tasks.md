## 1. Configuration

- [ ] 1.1 Add a `docker-compose` stanza to `.github/dependabot.yml` with `directory: "/platform"` and `schedule: weekly`, carrying an explanatory comment in the form the `terraform` stanza already uses — why the directory is explicit (the fetcher does not recurse and raises on an empty directory, design.md Decision 1) and that the CI suite compares the configuration against the tree. Verify with `python3 -c "import yaml; yaml.safe_load(open('.github/dependabot.yml'))"` and by confirming `docker-compose` appears among the `package-ecosystem` values.
- [ ] 1.2 Add a `groups` entry to that stanza bundling the six monitoring images, with patterns written as **dependency names** — `prom/*`, `grafana/*`, `ghcr.io/google/cadvisor`, `quay.io/prometheuscommunity/postgres-exporter` — not as Compose service names, which Dependabot does not match on and which differ for all six (design.md Decision 4 and its table). No pattern may match `postgres` or `traefik`; in particular do not write `postgres*`, which would pull the shared database into the group. Add no `ignore` stanza (Decision 3) and no `open-pull-requests-limit` (Decision 5). Verified by task 3.4 rather than by inspection.

## 2. Specification

- [ ] 2.1 Do **not** edit `openspec/specs/iac-safety-hardening/spec.md` — the delta is merged into it at archive time, not during build. Instead confirm the delta's MODIFIED block still reproduces the live requirement verbatim apart from the intended edits, by diffing `specs/iac-safety-hardening/spec.md`'s requirement block against lines 87–143 of the live file and checking that every difference is one this change intends. Verify with `openspec validate --changes --strict`.

## 3. Tests

- [ ] 3.1 Add a Compose-file discovery helper to `.github/tests/test_ci_configuration.py` returning every file whose top-level `services:` is a mapping with at least one entry declaring an `image:`, walking via the suite's existing `walked_files()` rather than a fresh `rglob` — it is the only walk in the suite that prunes `.claude/worktrees`, which `docs/change-queue.md` entry 34 records as already turning the terraform assertion red from the main working tree (design.md Decision 2, "Which walker"). Verify it returns `platform/docker-compose.yml` and nothing else, running from the main working tree with another worktree present.
- [ ] 3.2 Exercise the shape rule against a fixture rather than against the tree, following the `compose_fixture` pattern the suite already uses (`tempfile.mkdtemp`, under a path the walker does not prune). Assert that a task-list-shaped `docker-compose.yml` — a YAML sequence, the shape `ansible/roles/geerlingguy.docker/tasks/docker-compose.yml` has — is **not** discovered, and that a services-mapping file under a name containing no `compose` **is**. Without this the shape rule is demonstrated by no file in the committed tree, because `walked_files()` prunes `ansible/roles/geerlingguy.docker` by path before the rule is consulted — an assertion that cannot fail, which is the vacuous pass this suite refuses elsewhere.
- [ ] 3.3 Extend `TestDependabotCoverage` with an assertion that every file the helper discovers is covered on **both** conditions the fetcher applies — its directory is named by a `docker-compose` entry, **and** its name matches the fetcher's filename pattern — reporting a file that fails either as uncovered (design.md Decision 2). Mirror `test_every_terraform_lockfile_directory_appears_in_dependabot_config` including its guard against a vacuous pass on empty discovery. Verify with `python3 -m unittest discover --start-directory .github/tests` from the repository root.
- [ ] 3.4 Add an assertion that the `docker-compose` group's patterns select exactly six of the eight images `platform/docker-compose.yml` declares, and that neither `postgres` nor `traefik` is among them — matching each image's dependency name (the reference with its tag stripped) against the patterns under `*` wildcard semantics. Record in the test's own text that this establishes the committed patterns' intent, not that Dependabot applies those semantics, which is outside this repository. Verify it fails when a pattern is changed to `postgres*` and passes against the committed configuration.
- [ ] 3.5 Rename `test_dependabot_configures_both_required_ecosystems` to `test_dependabot_configures_every_required_ecosystem` and extend it to require all three ecosystems the amended requirement enumerates — the existing name asserts "both" and would be false of three. Verify it fails against a fixture configuration omitting `docker-compose` and passes against the committed one, following the fixture pattern the suite already uses for the Dependabot coverage test. Then grep the repository for the old name and confirm the hits are the expected ones, editing **only** the definition in `.github/tests/test_ci_configuration.py`: this change's own `proposal.md` and `tasks.md` name the test as it stands today and are correct as written, and the two test-plans under `openspec/changes/archive/` (`2026-09-06-close-ci-verification-gaps`, `2026-09-08-open-autoupdate-pr-with-app-token`) are archived records of what those changes did and are never rewritten.
- [ ] 3.6 Confirm each new or changed assertion can fail, recording the failing output before restoring: remove the `docker-compose` stanza and check 3.3 and 3.5 both report; rename `platform/docker-compose.yml` to `platform/stack.yml` and check 3.3 reports it as uncovered on the filename condition rather than passing on its directory; change a group pattern to `postgres*` and check 3.4 reports. An assertion never observed red establishes nothing.

## 4. Documentation

- [ ] 4.1 Delete entry 31 from `docs/change-queue.md`, and update `docs/review-2026-09-08-host-readiness.md` — finding 13 in the table, and entry 31's mention in reading-order step 4 — to record that it landed rather than that it is queued. Verify by grepping both files for `Queue 31` and reading each remaining hit.
- [ ] 4.2 Add to `README.md`'s CI/CD section, alongside the existing note on why `pre-commit` revisions are refreshed by a workflow rather than by Dependabot, a statement that the shared stack's image pins are refreshed by the `docker-compose` ecosystem — **and** that a reviewer of such a pull request owes the question no check can answer: whether the proposed image declares a persistent store the current one does not, per *No Store on This Host Holds Data Requiring Backup*. This is what discharges design.md's first risk; the specification alone does not, because a bump reviewer does not open `openspec/specs/` unprompted. Verify both statements are true of the committed `.github/dependabot.yml` and of that requirement by reading all three.

## 5. Verification

- [ ] 5.1 Run the full CI-configuration suite from the repository root — `python3 -m unittest discover --start-directory .github/tests --verbose` — and confirm it is green **and** that the verbose listing names each assertion added in group 3, rather than reading the exit code alone.
- [ ] 5.2 Run `pre-commit run --all-files` and `openspec validate --changes --strict`, and confirm both pass. State in the completion report that `terraform test` and `molecule test --all` were not run, and why — this change touches no Terraform module and no Ansible role — rather than reporting a suite as passing that was never invoked.
