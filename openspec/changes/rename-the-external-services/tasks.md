## 0. A note on what this task list may contain, and on who performs what

Branch and working-tree removal happen after this change's record has merged, which is after the commit that writes this file — a task for them would be unticked by construction forever. They are recorded in prose in section 9 instead. This is the repository's own convention (`AGENTS.md`, "Task lists, archived records, and disclosing what was not done"); the archive step itself is a task and is unaffected.

**The operator's steps — the ones performed outside this repository, in a web interface — are 2.1, 2.2, 2.3, 7.1, 7.2, 8.2, 8.3, 8.4, 8.5 and 9.1.** They are classified one by one rather than by section, because no section is wholly one or the other: section 7 opens with two interface steps and continues with three that run in this working tree. They are tasks because the change does not work without them.

**Section 7 is placed where it is, and that placement is the single most consequential thing in this list.** From 7.1 until the merge, `main` names workspaces that no longer exist — so 7.1 comes after the commit, the tests, the documents *and* the whole of section 6's verification and code review, which is as late as the mechanism allows. `design.md` decisions 1 and 2 are the authority, and nothing in section 7 may be brought forward for convenience.

**Section 3 is the derived tests and belongs to an author other than whoever implements section 4** (`AGENTS.md`: tests are derived from the approved delta specs, by a different author, after the plan is committed and **before** the implementation). It is placed before section 4 for that reason and not as a preference.

## 1. Provision this working tree before any verification result is claimed

A new working tree carries tracked files only. Verification that cannot reach what it needs skips and reports success, so until this section is complete the correct report is *not run, and why*.

- [ ] 1.1 Confirm `python3` resolves PyYAML at the version `.github/requirements-ci.txt` pins (6.0.1). The static suite parses every workflow with it, and a tree without it fails as an import error that reads like a broken test — which is also why this precedes the baseline rather than following it.
- [ ] 1.2 `pre-commit install --hook-type pre-commit --hook-type commit-msg`, and `ansible-galaxy install -r ansible/requirements.yml -p ansible/roles` — without the latter the `ansible-playbook --syntax-check` hook fails on the gitignored `geerlingguy.docker`, which reads as a broken role and is an unprovisioned tree.
- [ ] 1.3 `terraform init -backend=false && terraform validate` in both stack directories, which is also what creates the provider directory this tree does not carry.
- [ ] 1.4 **Record the baseline, now that 1.1–1.3 make it meaningful**: `python3 -m unittest discover --start-directory .github/tests` from the repository root. On trunk at `bdaaf6b` on 2026-09-12 that is **846 tests, OK**. A later run is a comparison against this, not an assertion on its own.
- [ ] 1.5 **No Molecule namespace is needed and none is taken.** This change adds no role, edits no role and touches no scenario; its Ansible surface is two inventory sources and an example environment file, none of which any scenario reads. Stated rather than omitted, because `AGENTS.md` obliges taking a namespace where verification writes to that shared service and this change's verification does not.

## 2. The operator's steps that open no window

Safe at any point before the pull request: nothing on `main` reads anything these steps touch.

- [ ] 2.1 **Probe the rename control, and clear the name.** An Environment named `main-production` already exists — created by hand 2026-09-12T06:11:52Z, no secrets, no protection rules, no deployments. Rename it to `rename-probe` in Settings → Environments, then delete it. The rename is the probe `design.md` decision 4 describes: it establishes that the control exists and that the name is free, and it establishes **nothing** about secrets or protection rules, because this Environment has neither. **If no rename control exists, stop here** and return to the operator with decision 3's rejected alternative and the question it rests on — whether all twenty-one Environment secret values can be produced by hand.
- [ ] 2.2 **Confirm `main-staging` is free too.** Task 8.2 renames onto both names, and only one of them has been probed. The Environment listing read on 2026-09-12 showed `production`, `staging` and `main-production` and no `main-staging`; re-read it rather than trusting that. A rename that fails on the second name leaves `production` already renamed and the pair half-moved.
- [ ] 2.3 **Create the two new repository secrets**, keeping the old two in place: `gh secret set HCLOUD_TOKEN_MAIN_PRODUCTION` and `gh secret set HCLOUD_TOKEN_MAIN_STAGING`, each holding the same **Read Only** Hetzner token its predecessor holds. Do not pass `--body`, which records the value in shell history. All four coexist until task 8.4, which deletes the old pair.

## 3. The derived tests

Derived from the delta specs by an author other than the implementer. The deltas state GitHub Environment names, which are repository settings no static read can reach — so the honest scope is narrower than the deltas, and it is stated here rather than discovered.

- [ ] 3.1 **`platform-deploy.yml`'s gated job is already asserted**, by `test_ci_configuration.py`'s check that exactly one job in that workflow declares `environment: <GATED_DEPLOY_ENVIRONMENT>`. That constant is the implementer's to move (task 4.10); the test author's job here is to confirm the existing assertion carries the delta and to report that rather than writing a second one.
- [ ] 3.2 **New, and the only genuinely new assertion this change owes**: that no committed file outside `openspec/changes/archive/` names `HCLOUD_TOKEN_PRODUCTION`, `HCLOUD_TOKEN_STAGING`, `infrastructure-prod` or `infrastructure-staging`. It is the only mechanism in this repository that can catch a name this change misses — every other check reads a *declaration*, and most of this change's surface is the prose around one. It is a static read of committed files at repository scope, which is `.github/tests`' own subject. **It must exclude its own module** and any synthetic fixture, or it fails on itself; `test_a_stack_and_its_environment_are_named_separately.py`'s retired-volume-name assertion is the shape to follow.
- [ ] 3.3 Confirm, rather than duplicate, the existing generic assertions: that each stack's declared `read_only_secret` equals its inventory source's credential variable, and that no two stacks declare the same read-only secret or the same GitHub Environment. Both are written over "every stack" and move with the tree.
- [ ] 3.4 **What is not testable, and is not to be stood in for.** No delta scenario about an Environment's protection rules, its secrets, a workspace's state or a Hetzner project can be asserted here: this suite makes no network call, and that constraint is itself asserted by tests in it. Report the gap rather than reaching for the nearest testable thing — `design.md` decision 2 closes on that rule because entry 62 did reach, and reported a probe that did not touch the code path it claimed to.

## 4. The commit

- [ ] 4.1 `terraform/stacks/main-production/versions.tf` and `main-staging/versions.tf`: `workspaces.name` becomes `main-production` and `main-staging`. **Delete the comment block in each saying the workspace name and the directory name are deliberately out of step until entry 63** — that comment is a commitment entry 62 made and this change is what discharges it; leaving it would state a divergence that no longer exists. Keep what survives it: the Execution Mode paragraph, the "no two stacks share a workspace" paragraph, and staging's note that the name is not derived from the directory's. **Where those comments repeat the claim that pushing ahead of the rename makes the next plan propose four creates, correct them** to what the delta to *Remote State Backend* now says — `design.md` decision 2 is the authority and carries the measurement.
- [ ] 4.2 The same two files' `provider "hcloud"` comments: staging's names `HCLOUD_TOKEN_STAGING`, which moves. **Leave the `HCLOUD_TOKEN`-for-prod claim in both files wrong** — it is already wrong, and it is `docs/change-queue.md` entry 70's work. `design.md` decision 9 is the authority, and says why that error is left while 4.1's is corrected; task 5.5 keeps entry 70 quoting the tree accurately.
- [ ] 4.3 `terraform/stacks/main-production/pipeline.yml`: `github_environment: main-production`, `read_only_secret: HCLOUD_TOKEN_MAIN_PRODUCTION`. Rewrite the two comment blocks that explain the present values — the `PRODUCTION`-rather-than-`PROD` paragraph now explains `MAIN_PRODUCTION`, and its pointer to entry 63 goes with the entry. **`target_environment` is untouched**, and the comment saying why the two differ becomes *more* load-bearing once the stack name and the Environment name agree.
- [ ] 4.4 `terraform/stacks/main-staging/pipeline.yml`: `github_environment: main-staging`, `read_only_secret: HCLOUD_TOKEN_MAIN_STAGING`. Its `target_environment: staging` comment says staging's group name did not move while its directory did — still true, and now the clearest demonstration in the repository that the two axes are independent, since after this change every other name for this stack *is* `main-staging`.
- [ ] 4.5 `ansible/inventory/main-production.hcloud.yml` and `main-staging.hcloud.yml`: the `api_token` lookup and every comment naming the variable. The name **must equal** the stack's declared `read_only_secret` (*Dynamic Inventory via the hcloud Plugin, One Source per Stack*, `openspec/specs/iac-host-configuration/spec.md`), and `.github/tests` compares the pair — so 4.3, 4.4 and 4.5 land in one commit or the build fails. Do not touch the `compose:`/`connect_with:` block, the `strict: true` reasoning or the `keyed_groups`: all three are measured findings about the plugin and none is about naming.
- [ ] 4.6 `.github/workflows/platform-deploy.yml`: `environment: production` → `main-production`. **The only literal GitHub Environment name in any workflow**, and one the handoff that opened this change did not name — found by reading the workflows rather than the documents. Leave the comment beside it saying what the line gates.
- [ ] 4.7 `ansible/.envrc.example`: both `export` lines, and the paragraph headed "THE VARIABLE NAMES ARE THE ENVIRONMENT AXIS, NOT THE STACK", which becomes false — after this change the names *are* the stack. Its pointer to entry 63 goes with the entry.
- [ ] 4.8 `.envrc.example` at the repository root: the sentence naming the two variables Ansible reads.
- [ ] 4.9 **Every operator's own `ansible/.envrc`.** Gitignored, so no review and no check can catch a stale one, and nothing in this repository will ever report it. A stale one fails the next *local* converge at inventory parse, naming the source it could not parse rather than the variable. Say so here because saying so is the whole of the mitigation available.
- [ ] 4.10 `.github/tests` literals: `PROD_READ_ONLY_SECRET` and `PROD_GITHUB_ENVIRONMENT` (`test_environment_agnostic_pipeline.py`), `SECOND_ENVIRONMENT_READ_ONLY_SECRET`, `SECOND_ENVIRONMENT_GITHUB_ENVIRONMENT` and `SECOND_ENVIRONMENT_WORKSPACE` (`test_a_second_environment.py`), `GATED_DEPLOY_ENVIRONMENT` (`test_ci_configuration.py`). **Synthetic fixture trees do not move**: several modules build scratch declarations naming `prod`, `staging`, `HCLOUD_TOKEN_STAGING` or `infrastructure-prod` to exercise a reader, and those assert nothing about this repository.
- [ ] 4.11 Delete `WORKSPACE_FORM` in `test_a_second_environment.py`. It is referenced by nothing, and its comment quotes the `infrastructure-<environment>` derivation *Remote State Backend* retired at entry 62 — a dead literal asserting a rule that no longer exists.

## 5. The documents

- [ ] 5.1 `README.md`: the local-setup step naming both Ansible variables, the "Environment variables and secrets" paragraph naming both repository secrets and the two Environments, the `platform-deploy.yml` bullet, and the "adding a stack" list, which names "a GitHub Environment of the declared name" and "a repository secret of the declared read-only name" — both correct as written and worth re-reading against the new values rather than assumed.
- [ ] 5.2 `docs/bootstrap-a-new-host.md`. **It is a runbook: read it end to end rather than grepping it**, because a stale name in it is a command an operator runs. The dense places are §0.4's count table (the HCP workspace row names both workspaces), §1.2's four-token table, §2's workspace creation step, §3.2's Environment creation, §3.3's `gh secret set` block and its secret table, §4.1, §6.0, §6.6, Appendix A's complete secret inventory, and Appendix B and C where they name an Environment. §1.1's `<company>-prod`/`<company>-staging` project names are the Hetzner projects and move with them.
- [ ] 5.3 `docs/deferred-work.md`: the entry naming the OAuth client "scoped to the `production` Environment".
- [ ] 5.4 `docs/change-queue.md` entry 52, which quotes `environment: production` as a literal on the deploy job — still a literal, now a different one, and that entry's point survives the change.
- [ ] 5.5 `docs/change-queue.md` entry 70, which quotes staging's `versions.tf` comment and prod's `read_only_secret` declaration. Both move; entry 70's finding does not. Check the entry still reads as a true statement about the tree this change leaves.
- [ ] 5.6 **Entry 60 needs nothing, and that is recorded rather than assumed.** The handoff that opened this change names entry 60 as quoting secret names in passing. It does not: the only occurrences of either secret name in `docs/change-queue.md` are in entries 63 and 70 (read 2026-09-12). Re-read it before ticking this, and if the handoff turns out to be right, task 3.2's new assertion will say so by turning the build red.
- [ ] 5.7 `docs/naming-conventions.md`: **the banner stays**, because entry 64 remains. Correct nothing else there — it is the target, and this change is part of what brings the tree to it.
- [ ] 5.8 Leave `ansible/roles/deploy_user/README.md` alone. Its `production` Environment is in *an application's own repository*, which this scheme does not reach. Leave `docs/review-2026-09-08-host-readiness.md` alone: a dated review of the tree as it stood.

## 6. Verify and review, before the window opens

**This whole section precedes section 7**, and that is the point of both. Everything here is offline: it reaches no external service, so it can run for as long as it takes — including a fix-and-re-review round — without a window being open anywhere. Any fix that section 7 or 8 later forces brings the change back here.

- [ ] 6.1 `python3 -m unittest discover --start-directory .github/tests` from the repository root. Compare against task 1.4's baseline; a count *below* it is a module that failed to import, not a suite that got smaller.
- [ ] 6.2 `terraform fmt -check -recursive terraform/`, and `terraform init -backend=false && terraform validate` in both stack directories. **`-backend=false` deliberately**: this one must not reach HCP, which at this point still holds the old workspace names.
- [ ] 6.3 `pre-commit run --all-files`, then `openspec validate --all` and `openspec validate --archived`.
- [ ] 6.4 **The tree-wide sweep, which is most of this change's completeness evidence.** Grep the tree, excluding `openspec/changes/archive/`, for `HCLOUD_TOKEN_PRODUCTION`, `HCLOUD_TOKEN_STAGING`, `infrastructure-prod`, `infrastructure-staging`, and — read one at a time rather than swept — `environment: production`, `environment: staging`, `` `production` Environment `` and `` `staging` Environment ``. Every survivor SHALL be one this change decided to keep: a synthetic fixture, a needle a test must name in order to assert its absence, an application repository's own Environment, an archived record, or the Ansible group `production`/`staging`, which is a different name this change does not touch. **The last four needles exist because the first four pass over every document that names an Environment in prose**, which is most of this change's surface.
- [ ] 6.5 Dispatch the code review over this change's diff, and fix what it finds. **Finish this before 7.1.** A review that lands after the workspaces are renamed holds the window open for the length of its own fix round, which is the cost the whole placement of section 7 exists to avoid.

## 7. The rename that opens the window, the plan that checks it, and the push

- [ ] 7.1 **Rename the HCP workspaces.** In app.terraform.io, organisation `shatynska`: `infrastructure-prod` → `main-production`, `infrastructure-staging` → `main-staging`. Settings → General → Name. **The window opens here and closes at the merge (8.3).**
- [ ] 7.2 **Verify each rename preserved its state, by id rather than by appearance.** `main-production` must be workspace `ws-QxowEvdjeZzziZ33` and `main-staging` must be `ws-b2HGxWg9zz2SoCC5`; each must still report four resources, and a state serial no lower than 46 and 4 respectively (read 2026-09-12). An id that is neither of those is a workspace created empty under the intended name. Also read each workspace's **States** view and confirm its history is present — weaker evidence than the id, kept because it is what the interface shows.
- [ ] 7.3 **Put staging's local Terraform token in scope.** `terraform/stacks/main-staging/.envrc` does not exist on this workstation; create it from `.envrc.example`, exporting `HCLOUD_TOKEN` with the same value `ansible/.envrc` exports as `HCLOUD_TOKEN_MAIN_STAGING`, and `direnv allow` it. Without this, `direnv` supplies the repository-root value and 7.4's staging run plans against production's project.
- [ ] 7.4 **The real local plan, and this change's strongest pre-push evidence.** With 7.1 done and section 4 committed but **not pushed**: `terraform init && terraform plan` in each stack directory, under that stack's **read-only** token. Both must report `No changes`. A plan proposing to *create* the server, firewall, volume or SSH key means the `cloud` block points at an empty workspace — **stop there and do not push.** **Two causes produce that one reading**: an empty workspace, and the wrong stack's token in scope, which plans a foreign project and also proposes four creates. Confirm the token in scope is that stack's, per 7.3, before concluding the rename failed. `AGENTS.md` permits this run; `design.md` decision 5 says why it happens here rather than during production's approval wait.
- [ ] 7.5 **Push the branch.** Named as a task because it is the event `design.md` measures the window against, and because nothing else in this list marks the moment the repository stops being the only thing at risk.
- [ ] 7.6 **From here until the merge, nothing else merges to `main`.** `main` still names the old workspaces, and `terraform init` creates an absent workspace rather than failing. What that costs is measured in `design.md` decision 2 and is smaller than the handoff feared — a failed run and an empty workspace, not a silent apply — but the empty workspace takes a name, and task 8.4 is what clears it.

## 8. Ship

- [ ] 8.1 Open the pull request and let continuous integration run. The plan jobs read the **new** repository secrets, created at task 2.3; if they fail on authentication, 2.3 did not happen. Get the pull request reviewed and approved — **8.2's first step is not due yet**, since it opens the second window and the merge is what closes it.
- [ ] 8.2 **Immediately before merging**, rename both GitHub Environments: `production` → `main-production`, `staging` → `main-staging`. **Immediately after**, re-list both: `gh secret list --env main-production` must show **fifteen** secrets and the Environment must still show **one required reviewer**; `gh secret list --env main-staging` must show **six** and no protection rule. A rename that lost either shows up here and nowhere else until a deploy fails. These are the counts read on 2026-09-12; `design.md` decision 4 records that this is the half of the rename claim the probe could not reach.
- [ ] 8.3 Merge. **Read staging's plan and apply output before approving production's.** Both plans must read `No changes`; staging's arrives first, costs nothing, and is produced by the same mechanism production's will use. A staging plan proposing creates means production's will too — do not approve it. The merge also triggers a converge on both stacks, because `ansible/inventory/*.hcloud.yml` changed; unlike entry 62's merge there is no race with the apply, because this change moves no Hetzner label. Wait for the operator's confirmation that it merged, that both applies succeeded and that both converges succeeded — nothing ships from a local machine.
- [ ] 8.4 **After the merge**, delete the two old repository secrets — `gh secret delete HCLOUD_TOKEN_PRODUCTION` and `gh secret delete HCLOUD_TOKEN_STAGING` — and list the HCP workspaces, deleting any named `infrastructure-prod` or `infrastructure-staging`. Any run that reached `main` inside task 7.6's window will have created one; they are identifiable by a resource count of zero, a remote execution mode, and an id that is neither `ws-QxowEvdjeZzziZ33` nor `ws-b2HGxWg9zz2SoCC5`. The nightly drift check runs at 03:00 UTC and Appendix A records it as consistently over four hours late; if it ran in the window, read its failure as this change's own artefact rather than as drift.
- [ ] 8.5 **Rename the two Hetzner projects** to `main-production` and `main-staging`. Cosmetic and unordered. **That a Hetzner API token survives its project's rename is asserted by the queue entry and by nothing this change has measured**, so it is not taken on trust: task 9.1's fifth observation is what establishes it, and it runs after this step for that reason.

## 9. Confirm and archive

- [ ] 9.1 **Confirm the effect**, as five observations, each waiting on the operator. Every one is outside this repository, which is what this change is:
  - **HCP** — workspaces `main-production` and `main-staging` exist, carry ids `ws-QxowEvdjeZzziZ33` and `ws-b2HGxWg9zz2SoCC5`, report four resources each, and show state serials no lower than 46 and 4. No workspace named `infrastructure-*` remains.
  - **Terraform** — the first plan after the merge reported `No changes` for both stacks, and the applies that followed changed nothing.
  - **GitHub** — `gh secret list` shows `HCLOUD_TOKEN_MAIN_PRODUCTION` and `HCLOUD_TOKEN_MAIN_STAGING` and neither old name; `gh secret list --env main-production` shows fifteen secrets, `--env main-staging` shows six; `main-production` requires a reviewer and `main-staging` does not; no Environment named `production`, `staging` or `rename-probe` remains.
  - **A converge reaches both hosts** on the merge, attaching to the renamed Environments and reading the renamed secrets — the one step that exercises all three GitHub renames at once.
  - **Hetzner** — both projects show their new names in the console, and **after** the rename a local `terraform plan` under each stack's read-only token still reports `No changes`. That second half is the point: it is the only evidence this change produces that a token survived its project's rename, which is otherwise an untested claim.

  This change is **not waivable** under either class in `AGENTS.md`: its effect is observable and the five observations above are performable. Nothing in it is a refactor with no externally observable effect — the effect is *entirely* external, which is the opposite case.
- [ ] 9.2 Archive once 9.1 is confirmed. **Read every touched capability's `## Purpose` by hand and correct it in the archive commit**: `openspec archive` does not apply a `Purpose` to an existing capability — it reports `→ 0` in the totals and silently leaves them stale. Entry 61 found it, entry 62 confirmed it. `iac-cicd-pipeline`, `iac-state-management` and `iac-platform-deploy-pipeline` are the three to read here.
- [ ] 9.3 Delete `docs/change-queue.md` entry 63 in the archive commit, and check that entry 64 and the `docs/naming-conventions.md` banner still read correctly against the tree this change leaves — entry 64 is the last of the four and its archiver deletes the banner.

  Branch and working-tree removal follow the record's own pull request and are not tasks here: remove the branch locally and on the remote, and remove the working tree from the repository's main working tree rather than from inside it.
