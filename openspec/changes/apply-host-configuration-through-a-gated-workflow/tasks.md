Verification commands referenced below, from this project's conventions:

- `python3 -m unittest discover --start-directory .github/tests`, from the repository root
- `ansible/scripts/run-molecule test --all`, from a role directory — read the SCENARIO RECAP, not the exit code
- `pre-commit run --all-files`, which carries `terraform fmt`, `tflint`, `terraform validate`, `gitleaks`, `ansible-lint` and `ansible-playbook --syntax-check`
- `openspec validate --all`

Sections 1–6 are repository work and land through this change's first pull request. Section 7 is the operator's, on their own workstation and in repository settings, against credentials that exist nowhere in this repository; the migration order in `design.md` is what sequences it against the merge. Removing the branch and the working tree happens after this record's own pull request merges, which is after the commit that writes this file, so those steps are recorded in prose in `proposal.md` and in `AGENTS.md` rather than as tasks here.

## 1. The inventory chooses its connection address

- [x] 1.1 In `ansible/inventory/prod.hcloud.yml`, add `connect_with` reading an environment variable and defaulting to `public_ipv4`, with a comment stating: that the default is what an operator's first converge needs, because joining the tailnet is what the run itself does; that CI selects `hostname`, which the plugin resolves to the server's Hetzner name — and that CI, not this file, is responsible for making that name resolve, which it does by mapping it rather than by relying on MagicDNS (3.5-0); and that a value the plugin does not recognise fails at option validation rather than falling back. In the same pass, correct the header's account of what adding an environment costs: it says "a variable beside `HCLOUD_TOKEN_PROD`", and the variable's name is no longer free — it must be **the same name** that environment declares as its `read_only_secret`, because a workflow converging it holds the credential under the declared name and must supply it under the name this file reads, and carrying that mapping in workflow text is the environment-naming the pipeline forbids. This is the one place a person adding an environment actually looks; the spec clause is invisible from here. Verify the source still parses against an invalid token — `HCLOUD_TOKEN_PROD=invalid ansible-inventory -i inventory/prod.hcloud.yml --graph` from `ansible/` exits non-zero reporting **`Invalid Hetzner Cloud API Token`**, which is the message that means the plugin was reached (the plugin's *name* appears only when `verify_file` rejects the path — `configure-the-staging-host` 1.2 established that the two read the opposite way round).
- [x] 1.2 Make the same change in `ansible/inventory/staging.hcloud.yml`, whose header already defers to prod's for the shared reasoning; add only what is specific here, which is nothing, so the line is identical. Verify the same way with `HCLOUD_TOKEN_STAGING=invalid`.
- [ ] 1.3 Verify all three address behaviours against the live API, and record each in this task's completion note — nothing in `.github/tests` can reach them, so this task is where the delta spec's address scenarios are established. With valid credentials: `ansible-inventory -i inventory/staging.hcloud.yml --list` still reports `ansible_host` as the public IPv4; the same command with the variable set to `hostname` reports the server's name; and the same command with the variable set to a value the plugin does not accept exits **non-zero**, naming the valid choices, rather than falling back to either address. The third is the delta's *An unrecognised selection fails the run* scenario and is the one a comment cannot establish.
- [x] 1.4 Leave `ansible/.envrc.example` alone, and say why in 1.1's comment: the variable is set by the converge job and by nothing else, and adding it to the file operators copy would invite a workstation to select an address its first converge cannot use.

## 2. Production's read-only secret stops colliding with the write token

- [x] 2.1 In `terraform/environments/prod/pipeline.yml`, change `read_only_secret` to `HCLOUD_TOKEN_PROD` and rewrite the field's comment block. The sentence about `HCLOUD_TOKEN` being "the name prod already uses" is now false, and what replaces it is the rule: every GitHub Environment defines `HCLOUD_TOKEN` as its Read & Write token, an Environment secret shadows a repository secret of the same name, and a gated job reading this field would therefore resolve the write token from a field that says read-only. Name `ansible/.envrc.example`, which already uses this variable for the same token locally.
- [x] 2.2 Retire the committed assertion that pins the old name. `.github/tests/test_environment_agnostic_pipeline.py` sets `PROD_READ_ONLY_SECRET = "HCLOUD_TOKEN"` and asserts prod's declaration equals it; the comment above it justifies the value by that change's "nothing in repository settings changes for N=1", a consequence this change retires by design. Update the constant and rewrite that comment to say what is now true — prod declares a name no GitHub Environment shadows, and the rename is a repository-settings step this change's Migration Plan sequences. This is **not** weakening a test to reach green: the proposition it asserts changed in the specification, and the assertion is re-pointed at the new one rather than deleted. Record it as an obsolete-test entry in `test-plan.md`.

      A second place in the same module rests on the same retired premise and **stays green while becoming untrue**, which is why it needs naming rather than finding. The digest-emitter exemption in the literal-named-credential sweep is justified in its docstring by prod declaring `HCLOUD_TOKEN` as its own read-only secret, and names as a closed escape the very rename 2.1 performs; after it, the exemption matches nothing and `assertLessEqual(len(exempted), 1)` passes over an empty list. Correct the docstring to say the escape was taken deliberately and the exemption is now inert, or remove the exemption — either is honest, and both change no result, which is exactly why neither will be forced by a red run.
- [x] 2.3 Verify nothing else in the repository names the old value where it now means something different. `grep -rn 'HCLOUD_TOKEN' --include='*.yml' --include='*.md' --include='*.py' --include='*.tf'` and account for **every** hit in this task's completion note, in one of four classes: the Environment-scoped write token (correct, unchanged); Terraform's own provider variable and the `.envrc` files that scope it (correct, unchanged — it is a different variable at a different layer); staging's already-suffixed read-only name (unchanged); and prod's read-only name (this rename). `docs/bootstrap-a-new-host.md` and `README.md` both carry hits of the last class and are section 5's and 6's work — this task establishes the list they must cover, so run it before them.
- [x] 2.4 Confirm the derived test for the delta's *A declaration naming the write token's own name is refused* scenario is present and red before 2.1 lands, and green after. It is **not written here**: it comes from a specification delta, so it is the independent test author's, and this task is the reading of it. What it must assert is that no `terraform/environments/*/pipeline.yml` declares `read_only_secret: HCLOUD_TOKEN` — a static read of a committed file, so `.github/tests` is its home, beside the assertions that already read both declarations — and that its failure message names the offending declaration, as the sibling refusals in that module do. The same holds for the delta's *A source's credential variable is the name the environment declares* scenario, which is statically checkable in the same place and against the same two files — and "same name" there means **literal** string equality, not a case-insensitive match: GitHub resolves `secrets[…]` case-insensitively while Ansible's `lookup('env', …)` does not, so a declaration differing from its source only in case would resolve in the workflow and fail in the play, which is precisely the shape a lenient comparison would let through.
- [x] 2.5 Run `python3 -m unittest discover --start-directory .github/tests` and confirm the suite is green with the renamed declaration and with 2.2's retired constant.

## 3. `.github/workflows/host-converge.yml`

- [x] 3.1 Add the workflow with `on: push` (branches `main`, paths `ansible/**`) and `workflow_dispatch` carrying a free-text `environment` input. Head the file with the comment block this repository's gated workflows carry: that no environment is named anywhere in it; that the workflow-level `paths:` filter is permissible **only** because this is not a required status check, with the pointer to `pr-validation.yml` where the opposite rule holds; and that the dispatch input is free text rather than a `choice` because a `choice` would enumerate environments in workflow text. Declare `permissions: contents: read` at workflow level, and again on `converge` — *Least-Privilege Workflow Permissions* obliges each job to declare what it needs, a job-level block **replaces** the workflow-level one rather than adding to it (which `ansible-verify.yml`'s `discover` job comments at length), and an absent declaration falls back to a repository setting that can change with no commit.
- [x] 3.2 Add the `discover` job: no `environment:`, `contents: read`. Its one step enumerates `ansible/inventory/*.hcloud.yml`, derives each environment name from the filename, and reads `github_environment` and `read_only_secret` from `terraform/environments/<name>/pipeline.yml` with the flat-mapping `field()` reader the Terraform workflows use. It refuses, by name and non-zero, on: a source whose environment directory or declaration is absent; a declaration missing either field or carrying a `read_only_secret` that is not a usable GitHub secret name; a source with no `ansible/inventory/group_vars/<name>.yml`; an environment directory carrying a declaration but no source; a dispatched `environment` input naming an environment it did not find, reporting those it did; and an empty result. Be explicit that an **absent or empty** input selects every discovered environment and is not refused — only a non-empty value is matched. Every `push` supplies an empty one, so conflating empty with unresolvable would fail discovery on every merge; this repository distinguishes the two by name everywhere else, and this is the one place the distinction is load-bearing rather than stylistic. Add one refusal the Terraform body has no reason to carry: 3.4b makes a declared `read_only_secret` a **process-environment key** for every later step of the converge job, so refuse a name that would overwrite the runner's own or Ansible's own — `PATH`, `HOME`, `ANSIBLE_CONFIG`, `ANSIBLE_VAULT_PASSWORD_FILE` and their kin. Each case fails closed today (the source never receives its credential and `any_unparsed_is_failed` refuses), so this is not a live hole; it is the same class as the control-character refusal already there — committed data reaching an execution surface — and is refused for the same reason. Carry a comment saying that this body is **deliberately not identical** to the Terraform discovery body — a different root, plus a cross-check that body cannot make — and is therefore outside the identity assertion, which stays covering the three Terraform workflows exactly as it does today. Say in the same breath what it *does* share with them (the `field()` reader, the secret-name validation, the empty-result refusal, the JSON emission), so a reader comparing the two is not misled into thinking the overlap is accidental, and point at the queue entry from 6.4. Take nothing from the event inside the step body, so `.github/tests` can execute it against a scratch tree.
- [x] 3.3 Add the `diff` job: `needs: discover`, no `environment:`, no secret of any kind, `fetch-depth: 0`, writing the push range's `ansible/` diff to `$GITHUB_STEP_SUMMARY`. On a dispatched run there is no range: say so in the summary rather than emitting an empty code fence. Comment that this job must start immediately with no access to the converge credential, citing `platform-deploy.yml`'s `diff` job as the precedent and `design.md` Decision 1 for why a check-mode run cannot take its place.
- [x] 3.4 Add the `converge` job: `needs: [discover, diff]`, `environment: ${{ matrix.environment.github_environment }}`, matrix over discovered environments with `fail-fast: false`, and `concurrency` keyed on the environment with `cancel-in-progress: false`. Comment the cancellation choice: a converge interrupted mid-play leaves a partially-converged host, which `docs/bootstrap-a-new-host.md` §6.3a treats as an exception and which cancelling would make routine.
- [x] 3.4-0 Add `ansible/requirements.txt`, pinning `ansible-core` alone at exactly the version `ansible/requirements-test.txt` pins. Its comment says why a second pip manifest exists rather than reusing the first: the converge job verifies nothing and has no business installing Molecule and its docker driver, and a test-toolchain bump must not move the Ansible that converges production. It also says what obliges the two to agree — a converge applying roles under a different Ansible than the one the Molecule suite verified them under is verified by nothing — and that the agreement is asserted, not intended. Confirm the derived test for the delta's *The converge runs the Ansible the roles were verified under* scenario is present and red before this file exists. It is the test author's, not this task's, and it must assert the scenario's own form rather than the narrower one: that the converge job's install step **names** `ansible/requirements.txt`, and that that file's pin equals `ansible/requirements-test.txt`'s. Asserting only that the two pins agree passes while the job installs from somewhere else entirely — the files agree and the property does not hold. Both halves are still static reads.

      **Corrected at the code-review gate, and the correction is not cosmetic.** `ansible-core` alone is not enough to run this job: `hetzner.hcloud` declares `requests` and `python-dateutil` in its own `requirements.txt`, `ansible-galaxy collection install` installs a collection rather than its Python requirements, and `ansible-core` depends on neither. Both arrive transitively through `molecule-plugins[docker]` in `requirements-test.txt`, so `ansible-verify.yml` and any workstation that has run the local setup already have them — this manifest dropped Molecule and took the dependency with it. Left as it was, the first `ansible-inventory` of every converge would have raised `missing_required_lib("requests")`, at a step whose own error message sends the operator at the two secrets they had just created. Both are pinned here.
- [x] 3.4-1 Provision the `converge` job before any of its Ansible runs, mirroring `ansible-verify.yml`'s four steps and citing it: `actions/checkout`; `actions/setup-python` at the version that workflow uses; `pip install -r requirements.txt`; then `ansible-galaxy collection install -r requirements.yml` and `ansible-galaxy role install -r requirements.yml -p roles` as **two** commands, because `-p` applies to roles only. **The paths are relative to `ansible/`, not to the repository root**, because 3.4-2 sets the job's working directory — copying `ansible-verify.yml`'s `ansible/`-prefixed spellings, which are correct in a job that runs from the root, would look right and resolve to `ansible/ansible/…`. A job-level working directory applies to `run:` steps and not to `uses:` steps, so `actions/checkout` and `actions/setup-python` are unaffected. Comment what each is for in this job specifically: `hetzner.hcloud` is what 3.5's very first command loads, and `geerlingguy.docker` is the play's first role — a runner carries neither, and a run that reaches a missing one fails deep inside a mechanism and reads as a broken mechanism rather than as an unprovisioned job. This is `AGENTS.md`'s provisioning rule reaching a runner rather than a working tree, and the failure mode it warns about is the same one.
- [x] 3.4-2 Name `working-directory: ansible` once, at the `converge` job level, and comment why it is not cosmetic: Ansible loads `./ansible.cfg` from the current directory, so from the repository root it loads **none** — `roles_path = roles` stops resolving, and `any_unparsed_is_failed` reverts to its default, which is the setting `iac-host-configuration`'s "an inventory source whose credential is absent or is rejected SHALL fail the run" clause is false without. A converge run from the wrong directory does not fail; it loses a guard and reports a rejected token as a destroyed server, which is exactly the misdiagnosis `ansible.cfg`'s own comment exists to prevent. Verify by inspection that every path in 3.4-1, 3.5 and 3.6 is relative to `ansible/` and that no step re-declares a directory. 3.4-1 is the one most likely to be wrong, because its commands are copied from a workflow that runs from the repository root.
- [x] 3.4a Give the `converge` job a job-level `env:` block carrying the connection-address selection, set to the plugin's `hostname` choice. This is what makes the whole reachability argument true rather than described — without it the job dials the public IPv4 and the cloud firewall drops it. Comment that it sits on the **job** deliberately: on individual steps, 3.5's derived address and 3.6's dialled address could disagree; at workflow level it would reach the credential-less `diff` job, which connects to nothing.
- [x] 3.4b Export the environment's read-only Hetzner token under the variable name that environment's inventory source reads, as the `converge` job's **first step**, writing a `<name>=<value>` line into `$GITHUB_ENV` with the name from `matrix` and the value from `secrets[…]`. It cannot ride in 3.4a's block and the comment must say why, because the shape looks like an oversight: Actions evaluates expressions in `env:` **values**, not in keys, and this name varies per environment. `apply.yml`'s plan step is not a counter-example — its key is the fixed literal `HCLOUD_TOKEN`, which works only because Terraform's provider reads one name whatever it is planning, and the Ansible sources deliberately do not have that property. Use the heredoc delimiter form rather than a bare `>>` append, with a **unique** delimiter, and say in the comment what that does and does not buy: it stops a value carrying a newline from injecting a second variable only where the delimiter itself cannot occur in the value, which is why GitHub's own guidance asks for a unique one rather than a conventional `EOF`. Note also that the token is in the job's environment from this step onward, inherited by every later step — the trade taken deliberately for single-sourcing, against exporting it twice inside the two steps that need it. Verify by reading 3.5's output on a real run: an inventory that resolves proves the name arrived, and an `any_unparsed_is_failed` refusal proves it did not.
- [x] 3.5 In `converge`, join the tailnet with `tailscale/github-action` using that Environment's OAuth client and `tags: tag:ci`, mirroring `platform-deploy.yml`. Write the SSH private key and the Vault password file under `umask 077`, `mkdir -p ~/.ssh` first — the precedent this mirrors carries it, and a runner's home has no `.ssh` until something makes one. Then, in **one** step, run the preflight and derive what to keyscan: `set -o pipefail`, then `ansible-inventory -i inventory/<environment>.hcloud.yml --list --vault-id <environment>@<file> | jq -er '._meta.hostvars | to_entries[] | .value.ansible_host'`, keyscanning each address into `known_hosts`. Carry `design.md` Decisions 7 and 9 as comments: the step exists because a `!vault` value is decrypted at *first use*, which is inside `deploy_user`, the fourth role — so without it a wrong password leaves a partially-converged host for a reason that has nothing to do with the host; the `jq` is what lets a document carrying decrypted secrets be read at all, narrowing it to one field so it reaches neither disk nor run log; `-e` makes an empty result a non-zero exit and `pipefail` makes a failure on the left of the pipe fail the step. Say why the address is derived here rather than held in a secret: this job has no `PLATFORM_DEPLOY_HOST` equivalent and must not grow one, and the inventory is the only source. Say also what the keyscan does **not** buy — on a runner whose `known_hosts` starts empty, it trusts whatever key is offered, exactly as `StrictHostKeyChecking=accept-new` would; it is chosen for matching `platform-deploy.yml` and for failing at a named step, not for being stronger.

      **Two corrections at the code-review gate, both to claims in the paragraph above.** `ansible-inventory --list` does **not** force a vaulted value to decrypt under the pinned `ansible-core` 2.21.3: it serialises with the `inventory_legacy` profile, which preserves it as `{"__ansible_vault": "<ciphertext>"}`. Verified against 2.21.3 — a wrong `--vault-id` exits 0 with output byte-identical to the right one. So the preflight proved the Hetzner token and nothing about the Vault password, which is the whole reason the step exists. It now also runs `ansible <environment> -c local -m debug -a "msg={{ hostvars[inventory_hostname] | to_json | length }}"`, which templates every vaulted value that host carries and prints a length: wrong password exits 2 naming the undecryptable variable, right password exits 0. That is what reaches prod's unlabelled `1.1` block too. And the derivation now selects from the environment's own **group** rather than from `_meta.hostvars`, which is every server in that Hetzner project: the two coincide today and are not the same set, and the set keyscanned must be the set the play converges. One consequence worth recording: because nothing is decrypted by `--list`, the document piped into `jq` carries no plaintext, so the narrowing is defence in depth rather than the exposure control Decision 7 argued it was.
- [x] 3.5-0 Between deriving each address and keyscanning it, resolve it on the runner and map it, so that nothing downstream depends on MagicDNS: `tailscale ip -4 "$name"` returns that peer's `100.x.y.z` from `tailscaled`'s own netmap — no resolver involved — and the pair is appended to `/etc/hosts`. The keyscan, `ansible_host` and the play all keep using the **name**, so there is still one string and Decision 3 is unchanged; what changes is that a failure now means *the peer is not in the netmap* rather than *the resolver is not configured as we assumed*. Comment why this is not belt-and-braces: `tailscale/github-action@v4` passes `--accept-routes` and neither `--accept-dns` nor its negation, so MagicDNS is probably on and probably is the wrong footing for the mechanism every converge depends on — and this repository's own practice already leans the other way, `PLATFORM_DEPLOY_HOST` being the literal IP precisely to avoid a resolution step. Use `sudo tee -a`, not a plain redirect: the runner's user cannot write `/etc/hosts` directly. Say in the comment what that rests on — passwordless sudo, which GitHub-hosted runners grant and a self-hosted one need not, and the runner's `hosts: files dns` order, which is what makes the mapping take precedence over any resolver. Give this lookup the same bound 3.5a gives the keyscan, for the reason 3.5a states: without it a transient empty would be reported as *the peer is not in the netmap*, which is the one diagnosis this step exists to make trustworthy, and it would be wrong. Fail with an `::error::` naming the environment and the name when the bound is reached — and name **a stale peer of the same name** as a cause in that message, because 5.10 explains why it is the likeliest one and this message is what an operator will be reading.
- [x] 3.5a Make the keyscan wait for tailnet propagation, and make all three of this step's failures speak. The keyscan is this job's **first** contact with the tailnet, and `platform-deploy.yml` carries nine lines saying a new peer propagates with a brief, eventually-consistent delay; its `ping:` mitigation cannot be copied here, because `ping:` takes an address and this job does not know one until after the join. So retry the keyscan, bounded, until it yields a key, failing with an `::error::` naming the environment and the address when the bound is reached. Then: emit an `::error::` naming the environment when the derivation yields no address — that is the group resolving to no host, which `host-baseline.yml`'s guard play has a careful diagnostic for that this step now pre-empts by failing first — and discard `jq`'s own stderr in favour of a fixed message, because a `jq` parse error quotes its input and a Vault-decrypted value is not a registered secret, so nothing masks it. Without the retry an empty scan means *not yet* rather than *unreachable*, and `ssh-keyscan` exits 0 either way; the failure then surfaces later as a connection error or a host-key mismatch, which read as a wedged host and as an attack, and neither reads as propagation.
- [x] 3.6 In `converge`, run `ansible-playbook playbooks/host-baseline.yml -i inventory/<environment>.hcloud.yml -e target_environment=<environment> --vault-id <environment>@<file> --private-key <the key file 3.5 wrote> -e tailscale_auth_key=''`. The `--private-key` is not optional and is easy to omit: the equivalent manual command in §6.3 carries it, and without it Ansible falls back to the runner's default identities, which are none. Carry `design.md` Decision 4's reasoning on the empty auth key as a comment: CI reaches the host *through* the tailnet, so a host not on the tailnet is one this job cannot open a session to at all, and `tailscale up` has no reachable state in which it both connects and needs to run. Name the one residual path — an unparseable `tailscale status` on a reachable host — and point at §6.3a.
- [x] 3.7 Verify statically what can be verified statically: the credential variable name written in 3.4b, the name `ansible/inventory/<environment>.hcloud.yml` reads, and the name that environment's `pipeline.yml` declares are one name; and the address selection set in 3.4a is in scope for both 3.5's derivation and 3.6's invocation, with no step re-declaring it. That the three addresses actually coincide at run time is **observed**, not verified here — this workflow cannot be dispatched until it is on the default branch, so the first run of it is the merge in 7.5, and 7.6 is where that reading happens.
- [x] 3.8 Add no liveness report and no `schedule:` trigger, and say so in the header comment: *Scheduled Workflows Report Their Own Liveness* reaches schedule-triggered workflows, this one is merge- and dispatch-triggered, and adding a report here would be decorative. Verify `python3 -m unittest discover --start-directory .github/tests` stays green — the scheduled-workflow census reads every file under `.github/workflows/` and this file must not enter it.
- [ ] 3.9 Verify the workflow is well-formed before it can be dispatched: `actionlint` over `.github/workflows/host-converge.yml` from a scratch install (it is not in `.pre-commit-config.yaml` — queue entry 6 owns that), and `pre-commit run --all-files` clean.

## 4. `.github/tests`

- [x] 4.1 Confirm the derived tests from this change's `test-plan.md` are in place and red for the right reason before any of section 3 is written — a test that is green before the implementation establishes nothing. Record the baseline run in `test-plan.md`, not here.
- [x] 4.2 After section 3, run `python3 -m unittest discover --start-directory .github/tests` and confirm every derived test is green and no existing test regressed. The discovery-body identity assertion over the three Terraform workflows must still pass **unchanged**: if it needed editing, section 3.2 produced a copy rather than a different discovery, and that is a defect in the implementation rather than in the test.

## 5. `docs/bootstrap-a-new-host.md`

- [x] 5.1 §0.3: add the `ansible-ci` keypair per environment to the keys the operator generates, saying what holds each half — the private in that environment's GitHub Environment as `ANSIBLE_SSH_PRIVATE_KEY`, the public appended to `root`'s `authorized_keys` on that host — and that it is distinct from the operator's own root key so that rotating CI's does not re-key the human. Say plainly that no role manages that file, so revoking this key is an edit on the host, and name the queue entry that would change it.
- [x] 5.2 §5.2/§5.3: state that `tag:ci` must reach each environment's host, not production's alone, since the converge job joins the tailnet for every environment it converges.
- [x] 5.3 §6.3: split the section. The first converge of a host stays exactly as written — manual, from the workstation, on the public address, with the auth key at the prompt — and gains a sentence saying why it cannot be the pipeline's: the host is not on the tailnet and carries no CI key yet. Add what happens afterwards: every later converge is a merge touching `ansible/`, production's waiting for the same approval `apply.yml`'s does.
- [x] 5.4 §6.3's `changed=2` paragraph: it currently says entry 23 owns removing those two `get_url` tasks. Entry 23 is this change and did not. Repoint it at the queue entry created in 6.2, and keep the baseline statement itself intact — it is still true and still the thing an operator must not discount.
- [x] 5.5 §6.3a: add that this procedure now also runs against a failed *pipeline* converge, that the recovery is unchanged (correct the input, re-run — every role is idempotent), and that the re-run may be a dispatched workflow run rather than a workstation command.
- [x] 5.5a The rename reaches five places in this document outside §6, and 2.3's grep is the list. Repair each, and do not treat any of them as cosmetic — this file is the authoritative bootstrap procedure and nothing in `.github/tests` reads it, so a wrong instruction here survives every check this repository has. Stage 1's token-placement table says production's Read Only token belongs in "the repository secret `HCLOUD_TOKEN`"; stage 3.2's step 5 says prod's `pipeline.yml` "needs no change unless you rename things", which is now false in this repository rather than hypothetically; stage 3.3's command block sets `gh secret set HCLOUD_TOKEN` for production's Read Only token, which would **recreate the collision this change exists to remove**; stage 3.3's secret table carries a `HCLOUD_TOKEN` / Repository row whose "Read by" cell also asserts "**Not Ansible** — no workflow runs Ansible", which this change falsifies; and Appendix A's row for the production read-only token names the same repository secret. Stage 3.3's paragraph on an Environment omitting its own `HCLOUD_TOKEN` needs a smaller correction in the same pass: it says the repository fallback "here is **production's read-only token**", and after this change the repository holds no `HCLOUD_TOKEN` at all — so the fallback resolves to empty, which `apply.yml`'s digest guard still refuses because both sides then match. Say that, rather than deleting the paragraph: the guard is unchanged and the reason it still fires is worth a reader's line.
- [x] 5.6 §6.4 and Appendix A: add the new secrets to the inventory table — `ANSIBLE_VAULT_PASSWORD` and `ANSIBLE_SSH_PRIVATE_KEY`, both per environment and both GitHub Environment-scoped; the `staging` Environment's copies of `TAILSCALE_OAUTH_CLIENT_ID` and `TAILSCALE_OAUTH_SECRET`; and the renamed `HCLOUD_TOKEN_PROD`. Say for the Vault password what changes about it: it is no longer only on one machine, which is what makes a second operator possible without handing over a password.
- [x] 5.7 Stage 9 (*Closing the gates*): record that the host layer now reaches production only by merging, and that the operator's workstation retains read credentials and the bootstrap path, per `AGENTS.md`.
- [x] 5.8 Record what happens when an environment is added, at the point in the document where an operator adds one: the merge that brings in a new inventory source touches `ansible/**`, so it triggers this workflow against a host that has not been bootstrapped and is on no tailnet, and that environment's converge row fails at connection. It is loud, it is isolated from the other environments by `fail-fast: false`, and it ends at that host's first manual converge. Say it there so the first person to meet it reads it as expected rather than as a broken pipeline.
- [x] 5.10 Appendix B (*Rebuilding an existing host*): add removing the old machine from the tailnet to the rebuild sequence, at or before 5.3's auth-key step. This change is the **first** consumer of the host's tailnet *name* — §6.3 dials the public IPv4 and `PLATFORM_DEPLOY_HOST` is a literal IP refreshed at 6.4 — and Tailscale deduplicates machine names by suffixing, so a rebuilt host joins as `<name>-1` while the old node keeps the bare name. It keeps it indefinitely, because §6.4 step 1 has the operator disable key expiry on it. The name then resolves to a peer that no longer exists, 3.5-0 succeeds against a corpse, and the converge fails at a keyscan whose message invites exactly the wrong diagnosis. Confirm the suffixing behaviour in the Tailscale admin console before writing it as fact — it is a vendor property, not a repository one, and this is a minute's check. The same appendix's staging paragraph already warns that an invalidated `known_hosts` "presents as the converge failing at connection time rather than as a rebuild artefact"; this belongs beside it, being the same class of trap.
- [x] 5.9 Verify every statement added here against the repository rather than against this plan, and verify `python3 -m unittest discover --start-directory .github/tests` still passes — the citation-form check reads this file.

## 6. `docs/change-queue.md` and `README.md`

- [x] 6.1 Delete entry 23. Its successor entries are below; nothing else in it survives.
- [x] 6.2 Add an entry for **scheduled host drift detection**: a `--check --diff` sweep per environment on a schedule, with an issue reporter and a liveness report, mirroring `drift.yml`. It inherits three things and must say so — the `changed=2` baseline and its three candidate remedies, that the `tailscale` role carries no Molecule scenario to regress a remedy against (entry 3b owns the role's first scenario), and `docs/bootstrap-a-new-host.md` §6.3's paragraph, which changes with it.
- [x] 6.3 Add an entry for **managing `root`'s `authorized_keys` from a role**, so the `ansible-ci` key is revocable and rotatable by converge rather than by hand. It must name what makes it its own change: the file can lock every operator out of a host, so it needs scenarios of its own, and the existing single entry arrives from Hetzner at server creation where `hcloud_server.ssh_keys` cannot be changed without recreating the server.
- [x] 6.4 Add an entry for **factoring the four environment-discovery bodies into one artifact**, naming what has changed since the decision to keep three copies: a fourth near-sibling now exists that is deliberately not identical, so the identity assertion no longer covers the whole family. Record that a script under `.github/scripts/` is likely simpler than a composite action, because `.github/tests` can execute a script directly instead of pulling a body out of a workflow by step name.
- [x] 6.5 Entry 25 (`close-public-ssh-and-manage-sshd-explicitly`): update its block on this change. It is no longer waiting for the host converge to leave the workstation; what it now inherits is that CI reaches the host over the tailnet and therefore does not need the ISP `/24`, and that the `/24` is the last fallback if a converge wedges the tailnet.
- [x] 6.4a Add an entry for **bringing this repository's pip manifests under Dependabot**. `.github/dependabot.yml` configures no `pip` ecosystem, and *Automated Dependency Updates* obliges only `terraform`, `github-actions` and `docker-compose` — so `ansible/requirements-test.txt` and `.github/requirements-ci.txt` are uncovered today, and 3.4-0 adds a third. State that plainly: this change does not create the gap and does not widen its class, but it does put one more pin behind a mechanism nothing watches, and the entry owes a decision on whether the requirement itself should name the ecosystem.
- [x] 6.5a Add a line to entry 3b (`report-an-absent-tailscale-auth-key`), which this change gives a caller it did not have. That entry would report an absent `tailscale_auth_key` by name; the converge job now always supplies it, as an empty string, so whether 3b's diagnostic fires on *empty* as well as on *undefined* is a decision that entry must now make rather than one it can leave implicit. Record it there, not here — a note kept inside this change is archived with it.
- [x] 6.6 `README.md`: two edits, both verified against the file rather than assumed. The paragraph stating that repository secrets hold `HCLOUD_TOKEN` and `HCLOUD_TOKEN_STAGING` is false from migration step 5 onward and takes the new name. And where the README describes the layers and how each reaches infrastructure, bring the host layer's description into line — it is the sentence a reader uses to decide whether this repository means what `AGENTS.md` says about shipping from a local machine.
- [x] 6.7 Run `openspec validate --all` and `pre-commit run --all-files`.

## 7. The operator's work, in the order `design.md`'s Migration Plan gives

- [ ] 7.1 Before the merge: add the repository secret `HCLOUD_TOKEN_PROD` carrying production's existing Read Only token, leaving the repository `HCLOUD_TOKEN` in place so both names resolve.
- [ ] 7.2 Before the merge: generate one `ansible-ci` keypair per environment, append each public half to `root`'s `authorized_keys` on that environment's host, and confirm each key logs in — `ssh -i <key> root@<tailnet name> true` from a tailnet-joined machine.
- [ ] 7.3 Before the merge: add `ANSIBLE_SSH_PRIVATE_KEY` and `ANSIBLE_VAULT_PASSWORD` to each environment's GitHub Environment, and copies of `TAILSCALE_OAUTH_CLIENT_ID` and `TAILSCALE_OAUTH_SECRET` to `staging`'s.
- [ ] 7.4 Confirm each server's Hetzner name is the name `tailscaled` knows that peer by, before anything depends on it: `tailscale ip -4 <server name>` from a tailnet peer returns that host's `100.x.y.z`. Be clear about what this does and does not establish — it is a lookup in the local netmap and would succeed with MagicDNS switched off, so it is evidence about the **name** and about nothing else. That is exactly what 3.5-0's mapping needs, and it is why this command was moved off the claim an earlier draft made for it. If it returns no address the name is wrong, and 3.5-0 needs another source — stop and raise it rather than working around it in the workflow.
- [ ] 7.4a Perform task 1.3 now, if it has not been: the three connection-address checks against the live API, from a workstation with `ansible/.envrc` filled in. It is disclosed under "Not performed" because the authoring environment holds no Hetzner credential, and it is the only evidence this change will ever have for the delta's four *Address Selected Per Run* scenarios. It belongs here rather than in section 1 because this is where someone with the credentials is reading.
- [ ] 7.5 Merge. This is the workflow's **first** run: it cannot be dispatched before it is on the default branch, so nothing has exercised it end to end until now, and staging is what absorbs that. Staging's converge runs unattended; production's waits. Read staging's run in full: the play recap names every role and reports `changed` counts consistent with an already-converged host; the derivation step printed the server's tailnet name and **not** its public IPv4, which is the evidence 3.4a landed; the inventory resolved at all, which is the evidence 3.4b's credential arrived under the name the source reads; and the keyscan obtained a key without exhausting its retry bound. A retry that was exercised at all is worth noting — it means the propagation delay is real on this tailnet and the bound is load-bearing rather than theoretical.
- [ ] 7.6 Approve production's converge only after 7.5, and read its preflight step specifically: it is what establishes that production's unlabelled `1.1` `ghcr_pull_token` block decrypts under `--vault-id prod@…`.
- [ ] 7.7 After the first production converge succeeds: delete the **repository** secret `HCLOUD_TOKEN`. The `production` Environment's `HCLOUD_TOKEN` stays — it is the write token, and `apply.yml`'s guard that the two differ depends on it existing.
- [ ] 7.8 Confirm the effect rather than the deploy: make a trivial, reversible change under `ansible/` — a comment in a role — merge it, and confirm the converge runs and the host is unchanged. That is `ship:confirm`'s observation for this change, and it is performable, so no waiver applies.

## Not performed

Neither of these was declined on its merits: both need something the authoring environment does not have. Each is still owed, and each is named below where someone who can perform it will read it.

- 1.3 Verify the three connection-address behaviours against the live API — the verification half only. The committed shape is asserted by `.github/tests`: a selection exists, it is read per run, its default literal is the public address, and both sources read one variable. That is a weaker proposition than what a run dials, and all three of this task's checks remain unrun.
  Reason: each needs a valid read-only Hetzner token and a live server. The authoring environment holds no credential for either Hetzner project, and `.github/tests` may make no network call and hold no credential — constraints that suite asserts about itself. The third check especially, that an unrecognised selection fails rather than falling back, is the plugin's own option validation and has no committed file whose content decides it. **Perform it before 7.5**, from a workstation with `ansible/.envrc` filled in: it is the only evidence this change will ever have for the delta's four *Address Selected Per Run* scenarios, which `test-plan.md` records as uncovered for this same reason.

- 3.9 Verify the workflow with `actionlint` — the `actionlint` half only. The rest was performed: `pre-commit run` is clean over every file this change touches, and the workflow was checked three further ways the task does not ask for — every `run:` body parsed with `bash -n`, every one linted with `shellcheck -S warning`, and the file parsed with a YAML loader. Shellcheck reported two `SC2296` findings, both fixed by passing the SSH key and the Vault password through `env:` rather than interpolating them into the shell body, which is the better shape anyway.
  Reason: `actionlint` is on no path in this environment, and the task itself records that nothing in this repository installs it — `docs/change-queue.md` entry 6 owns adding it to `.pre-commit-config.yaml` and notes it exits non-zero on two pre-existing findings that must be dispositioned first. Installing it ad hoc was not attempted. What stays unchecked is the Actions-specific layer: expression syntax, `needs:` references and the shape of `uses:` inputs. The first real run, 7.5, is what exercises those — later than it should be, and that is this gap's cost.

## 8. Archive

- [ ] 8.1 Archive this change once 7.8 is confirmed, per `AGENTS.md`'s `ship` stage.
