## 1. The check

- [x] 1.1 Write `.github/tests/test_the_retired_requirement_names_are_gone.py`, in the idiom of `test_the_external_service_names_are_retired.py`: a pure offence-finder plus a thin reader over `tracked_files()`, and exemptions stated with the evidence that required each. **Its discriminating class is not in this module** — see *Not performed* below.
- [x] 1.2 Derive the retired set rather than declaring it (design decision 1): every `### Requirement: <name>` under a `## REMOVED Requirements` heading in `openspec/changes/archive/*/specs/*/spec.md`, minus every `### Requirement: <name>` in `openspec/specs/*/spec.md`.
- [x] 1.3 Flatten each file before matching and keep an offset-to-line map, so a name wrapped across a comment's line break is found and still reported as `<path>:<line>: <name>` (design decision 2).
- [x] 1.4 Suppress a retired name that is a prefix of a live name where the live name is what the file says — *Ansible Configuration Is Verified in Continuous Integration* against *…and Gates the Merge* is the case that exists today. Run the suppression over the **flattened** text, not per line: almost every citation of that live name wraps, so a per-line suppression reports every one of them.
- [x] 1.5 Name the replacement in the failure message, read from the `**Migration**` or `**Reason**` line of the `REMOVED` block, **and only where a specification currently holds it** (design decision 3). Where the recorded replacement is itself retired, follow the chain to the first live name — *Dedicated Hetzner Cloud Project for Prod* → *Each Environment Has a Dedicated Hetzner Cloud Project* → *Each Stack Has a Dedicated Hetzner Cloud Project* is the chain that exists today. Where the chain reaches no live name, or the block records no replacement, omit it.
- [x] 1.6 Exempt `openspec/` by prefix, and this module itself by path. **Do not exempt `.github/tests/`** — decision 5, and the delta scenario *The test suite's own citations are read*.
- [x] 1.7 Exempt `docs/deferred-work.md` by path, with the reason stated at the exemption: its 2026-09-10 paragraph names *Dedicated Hetzner Cloud Project for Prod* as the requirement a change renamed, which is a record of the retirement itself.
- [x] 1.8 Exempt `docs/change-queue.md` by path, with entry 74 named as the reason and its expiry stated (design decision 6). Task 6.4 deletes this exemption in the archive commit; task 1.9's expiry assertion is what forces that.
- [x] 1.9 Assert that each whole-path exemption still contains a retired name, so an exemption cannot outlive what it excused.
- [x] 1.10 Pin sweep anchors across the surfaces and depths this change edits, and assert the derived retired set is non-empty and contains *Each Environment Has a Dedicated Hetzner Cloud Project* (design decision 3).
- [x] 1.11 Confirm the module needs no network, credential, container runtime or Terraform binary, so that `TestEveryModuleInTheSuiteDirectoryNeedsNoPrivilegedResource` in `test_ci_configuration.py` passes over it.
- [x] 1.12 The derived tests for this change's five delta scenarios live in a module of their own, `.github/tests/test_a_retired_requirement_name_is_reported.py`, written by their own author before this implementation exists, per `AGENTS.md`'s independent-test-authoring step and its `.github/tests/*.py` glob. They import the finder from `test_the_retired_requirement_names_are_gone` and exercise it against fixture trees; until 1.1 exists that import fails and the suite is red, which is the expected state while applying. **A separate module because `.github/tests` is both this repository's check surface and its test surface**: 1.1's module is the check — it sweeps the real tree — and this one is the test of that check's reader, so merging them would make the thing under test and the thing asserting it one file with no seam. The discriminating class of 1.1 is **additional** to these and does not stand in for them: it falsifies the reader, they trace to the scenarios. Where both would assert the same thing, the scenario-derived one is the one that stays.

## 2. Sweep the retired requirement names

Measured 2026-09-13 at trunk `80a8ec5`: **47 occurrences across 13 files**, of which 46 are corrected here and one — `docs/change-queue.md:631`, inside entry 74 — is carried by that entry's deletion at archive (task 2.12). Plus one exempt occurrence at `docs/deferred-work.md:223`. Re-run the finder after each task rather than working from this list alone.

- [x] 2.1 `.github/workflows/apply.yml:29`, `.github/workflows/drift.yml:6`, `.github/workflows/pr-validation.yml:14` — *Each Environment Declares Its Own Pipeline Configuration* → *Each Stack Declares Its Own Pipeline Configuration*. Two of the three wrap the name across a comment's line break.
- [x] 2.2 `terraform/stacks/main-staging/versions.tf:48` — *Each Environment Has a Dedicated Hetzner Cloud Project* → *Each Stack Has a Dedicated Hetzner Cloud Project*, wrapped across two comment lines. **Do not touch the sentence two lines below it about prod's read-only secret**: it is knowingly wrong and belongs to `docs/change-queue.md` entry 70.
- [x] 2.3 `.github/tests/test_terraform_stacks_are_the_iterated_unit.py:2953` — the literal that asserts the `versions.tf` comment of 2.2, which moves in the same commit.
- [x] 2.4 `ansible/ansible.cfg:26` — *Dynamic Inventory via hcloud Plugin* → *Dynamic Inventory via the hcloud Plugin, One Source per Stack*, and add the `openspec/specs/iac-host-configuration/spec.md` path the citation form requires and this line omits (design decision 8).
- [x] 2.5 `docs/bootstrap-a-new-host.md:106` — *Each Environment Has a Dedicated Hetzner Cloud Project* → *Each Stack Has a Dedicated Hetzner Cloud Project*.
- [x] 2.6 `.github/tests/test_environment_agnostic_pipeline.py` — thirteen occurrences of *Each Environment Declares Its Own Pipeline Configuration*, all in section banners and `SPECIFIED`/`ADDED` docstrings; ten of the thirteen wrap across lines.
- [x] 2.7 `.github/tests/test_terraform_stacks_are_the_iterated_unit.py` — ten occurrences: eight of *Each Environment Declares Its Own Pipeline Configuration*, two of *Environment and Module Folder Structure* → *Stack and Module Folder Structure*. **Plus the truncated one at `:2951` that the sweep cannot see**: the fixture there claims to reproduce `terraform/stacks/main-staging/pipeline.yml` verbatim as it stands at HEAD, and carries `"Each Environment Declares Its Own Pipeline` where that file says `Each Stack`. Design decision 2 discloses why no predicate here catches it.
- [x] 2.8 `.github/tests/test_host_configuration_names_its_environment.py` — six occurrences of *Dynamic Inventory via hcloud Plugin*.
- [x] 2.9 `.github/tests/test_a_second_environment.py` — five occurrences: two of *Each Environment Has a Dedicated Hetzner Cloud Project*, three of *Environment and Module Folder Structure*.
- [x] 2.10 `.github/tests/test_host_converge_workflow.py` — five occurrences: two of *Each Environment Declares Its Own Pipeline Configuration*, three of *Dynamic Inventory via hcloud Plugin*.
- [x] 2.11 `.github/tests/test_a_stack_and_its_environment_are_named_separately.py:12` — one occurrence of *Each Environment Declares Its Own Pipeline Configuration*.
- [x] 2.12 `docs/change-queue.md:631` — inside entry 74. Left as it stands: task 1.8 exempts the file for the life of this change, and task 6.1 deletes the entry at archive, which is what expires the exemption.
- [x] 2.13 Re-run the finder over the tree and confirm the only remaining occurrences are `docs/deferred-work.md:223` and `docs/change-queue.md:631`, each behind its stated exemption.

## 3. The other stale names

- [x] 3.1 `README.md:25` — the stack folders are `main-production` and `main-staging`, not `prod` and `staging`.
- [x] 3.2 `docs/bootstrap-a-new-host.md:183` and `:1005` — both cite a change named `add-a-staging-stack`, which never existed. The archived records are `add-a-staging-environment` and `configure-the-staging-host`; name whichever each sentence means.
- [x] 3.3 `ansible/roles/swap/defaults/main.yml:14` and `ansible/roles/swap/README.md:28` — the volume is named `main`, not `main-data`. Both sentences are about the volume, not the mount path, so neither becomes `/mnt/main`.
- [x] 3.4 `docs/bootstrap-a-new-host.md:773` — the heartbeat check `<company>-prod alertmanager` carries a company segment the account does not need and a spelling this repository retired. Give it a name consistent with Appendix A's table.
- [x] 3.5 Entry 73, the converge keypair. The literal `~/.ssh/<company>-ansible-ci-<environment>` is at exactly four lines: `docs/bootstrap-a-new-host.md:76` (§0.3's table row, twice on the line — the `-f` path and the `-C` comment), `:716` and `:718` (§6.6's `ssh-copy-id` and its check), and `:726` (§6.6's `gh secret set`). Rename the key to `<stack>` at each.
- [x] 3.6 Then read the prose that describes that key and change it only where it now says something false: §0.3's two paragraphs at `:80` and `:82` (which call it "one per stack" already, and may need nothing), and Appendix A's converge-key row at `:950` (which names the secret, not the filename). Record in the commit message which of these needed no edit, so a later reader does not take the absence for an oversight.
- [x] 3.7 §6.6's note about which argument `gh secret set --env` takes is about the GitHub **Environment** and does **not** move. Confirm it still reads correctly beside the renamed key, since the two now differ on both axes.

## 4. The dead change-queue pointers

- [x] 4.1 `docs/bootstrap-a-new-host.md:1001` (Appendix C) — "log rotation (21), swap and container limits (22, 7)" names two entries that no longer exist: 21 and 22 were delivered by `bound-host-log-growth-and-add-swap`. Only 7 is outstanding. "DNS as code (26)" likewise: entry 26 was declined and is now a `docs/deferred-work.md` section, which §4.4 already cites correctly.
- [x] 4.2 `docs/deferred-work.md:330` — `**Owner:** docs/change-queue.md entry 62`, in *The stack rename left the word in test identifiers and in specification titles*. Entry 62 is archived as `rename-the-stacks-and-their-resources`; say what it did to each of that section's three bullets rather than pointing at it as a future owner. Its "Requirement titles carrying *Environment*" bullet in particular is no longer true as written. Write its *Scenario titles* bullet knowing task 6.2's new queue entry exists, and point the two at each other (task 6.2b).
- [x] 4.3 `ansible/roles/platform_data_volume/tasks/main.yml:191` and `ansible/roles/swap/tasks/main.yml:126` — both cite entry 23 as the change that *would* move the host converge into a workflow. It did, as `apply-host-configuration-through-a-gated-workflow`.
- [x] 4.4 `docs/change-queue.md` entries **27** and 30 — both cite entry 23 in the same forward tense. (The task said 25 when it was written; entry 25 carries no such pointer, having already said "the wait is over". The edits landed in 27 and 30.)

## 5. The GitHub App, and the naming banner

- [x] 5.1 `docs/bootstrap-a-new-host.md` §0.1 — add the GitHub App among the accounts and identities stage 0 assembles, as the identity that opens the hook-update pull request.
- [x] 5.2 §0.4 — add a row to the what-exists-once-and-what-exists-twice table (**1**, shared: one App serves the repository whatever its stack count), because that section ends "Appendix A is this same set seen from the other end … if you change one, change the other", and task 5.4 changes Appendix A.
- [x] 5.3 §3.2 — add its creation: a GitHub App installed on this repository alone, with repository permissions Contents and Pull requests at read and write and no account-level permissions, per *Automated Dependency Updates* (`openspec/specs/iac-safety-hardening/spec.md`) and the scope `README.md:131-146` already records. Say why it exists: a pull request opened with a workflow's default `GITHUB_TOKEN` starts no workflow run, so the required checks never report and it can never be merged.
- [x] 5.4 §3.3 — add `APP_CLIENT_ID` and `APP_PRIVATE_KEY` to the repository-secret block and to that stage's **Secrets created in this stage** table, and correct the section's **Check** line, which currently counts seven secrets.
- [x] 5.5 Appendix A — add the row that makes "Every credential the system uses, in one place" true, naming what breaks when it is wrong: the weekly hook-update pull request stops being opened, and the workflow's heartbeat check reports the **run** rather than the pull request, so nothing alarms.
- [x] 5.6 Stage 9.4 — the dispatch step currently fails at *Mint a token for the App* on a clone that has no App. Say that 5.3's secrets are its prerequisite.
- [x] 5.7 `docs/naming-conventions.md:5-7` — rewrite the banner (design decision 12): the scheme is in effect; the two GitHub Environments are the one exception and entry 75 is why; entries 73 and 74 are outstanding beside it until this change archives. Keep the instruction that whoever archives 75 deletes the banner.
- [x] 5.8 `AGENTS.md`, "Citing this repository's own specifications and change records" — that section states only the path half of the rule and names only `test_ci_configuration.py` as asserting it. After this change it is the stale document about the rule this change exists to enforce. Extend it to the name half and name the new module.

## 6. Records

- [x] 6.1 Delete entries 73 and 74 from `docs/change-queue.md` in the archive commit — not before, per `AGENTS.md`, and see task 1.8.
- [x] 6.2 Record the stale **scenario** titles as a new `docs/change-queue.md` entry (design decision 7): thirty citations naming a scenario no specification holds, measured 2026-09-13. Say that the replacements are not mechanical, and that the change should extend this change's own check to the scenario predicate rather than sweep by hand.
- [x] 6.2a **The entry quotes scenario titles only, and never a retired requirement name** (design decision 6). Write it during implementation, not at archive — `AGENTS.md` records a surfaced change before doing anything else about it, and an entry that exists only in the archive commit is lost with the branch if this change is abandoned. `docs/change-queue.md` is exempt until then, so the constraint is not what makes the entry safe now; it is what keeps it safe in the archive commit, once task 6.4 has removed that exemption. A scenario title is not in the retired set, so the entry loses nothing by it.
- [x] 6.2b Cross-reference `.github/workflows/drift.yml:290` in that entry rather than dropping it, so the entry's count stays the thirty that `proposal.md`, `design.md` decision 7 and task 6.2 all state — and have the entry state the split explicitly: twenty-nine of its own kind, plus this one. It is the **inverse** case. The citation there says *Drift in one stack does not resolve another's report* while `openspec/specs/iac-cicd-pipeline/spec.md:353` still says *…one environment…* — the citation is ahead of the specification, not behind it. That residue belongs to the `docs/deferred-work.md` section task 4.2 rewrites, whose third bullet is *Scenario titles*. Make the two records point at each other rather than describe one thing twice in the two files `AGENTS.md` gives opposite meanings.
- [x] 6.3 Record the declined change-name check (design decision 4) in `docs/deferred-work.md` — its measurement and why a retired *requirement* name is checkable where a retired *change* name is not. `deferred-work` rather than the queue: it is work deliberately not done.
- [x] 6.4 In the archive commit, delete task 1.8's `docs/change-queue.md` exemption together with entries 73 and 74. Task 1.9's expiry assertion turns red in that commit if it is left, which is the mechanism doing the reminding.
- [x] 6.5 Record any further change this one surfaces, per `AGENTS.md`, rather than folding it in.

## 7. Verify

- [x] 7.1 `python3 -m unittest discover --start-directory .github/tests` green, from the repository root.
- [x] 7.2 `openspec validate --all` green.
- [x] 7.3 `pre-commit run --all-files` green.
- [x] 7.4 Establish the new check actually fails: reintroduce one retired name into a swept file, confirm the failure names the file, the line and the live replacement, and restore the file from the committed content — committing before dispatching review, per `AGENTS.md`, so the restore is exact.
- [x] 7.5 Establish the wrapped-name case specifically, by reintroducing a name split across two comment lines rather than one on a single line.
- [x] 7.6 Establish the expiry case: delete a retired name from an exempt file and confirm the check fails naming the now-idle exemption.
- [x] 7.7 Establish the prefix case: confirm a correct citation of *Ansible Configuration Is Verified in Continuous Integration and Gates the Merge*, wrapped across lines as the tree writes it, is not reported.
- [x] 7.8 Confirm `ansible-lint` and the syntax check still pass over the two role files edited in 3.3 and 4.3.

## 8. Ship

- [x] 8.1 Open the pull request for the implementation and merge it. PR #164, merged 2026-09-13 as `18cb9f6`; `validate` and `ansible-verify` both green on it.
- [x] 8.2 **Operator step at the observer, which no check here can hold** — performed as an inspection, and it found the task's premise false. **No check named `<company>-prod alertmanager` exists, and none ever did.** The observer holds *Alertmanager Dead Man's Switch* under the slug `my-first-check`, which is the vendor's own default first-check name: §7.1 prescribed a name and whoever followed it created the check under another, and nothing in this repository can see that divergence. So there was no rename to perform. What §7.1 now says is a prescription for the next deployment, and the existing check's name is a decision for the operator rather than work this change left undone.
  The safety question it was meant to settle is answered, and in the operator's favour: Alertmanager posts to `${DEADMANSWITCH_URL}` (`platform/docker-compose.yml`), a full ping URL held in `PLATFORM_DEADMANSWITCH_URL`, so **nothing addresses that check by slug** and renaming its name or its slug breaks nothing. The four periodic-job checks are the opposite case and must not be re-slugged — `drift.yml`, `pre-commit-autoupdate.yml` and the `image_prune` role all build `hc-ping.com/<key>/<slug>?create=1`, so a renamed slug would silently create a second check and leave the first to alarm.
- [x] 8.3 Confirmed by the operator on 2026-09-13, on the evidence below rather than on a healthy deploy.
  **The check runs inside the required status check.** PR #164's `validate` job log names this change's own module and reports `Ran 967 tests … OK` — so the gate is live in the pipeline, on a runner, not only on a workstation.
  **Both hosts converged `changed=0`.** `main-staging` `ok=85`, `main-production` `ok=87`, each with the three lines §6.6 names and no Galaxy failure.
  **The falsification was established locally rather than on a runner**, and the operator accepted that: tasks 7.4 to 7.7 reintroduced a retired name, the same name wrapped across two comment lines, an emptied exempt file and a correct wrapped citation of the live longer name, and the check reported exactly the first three and not the fourth. What a CI repetition would add is only that the runner reproduces a read over committed files with no environment dependence.
- [x] 8.4 Archive, carrying tasks 6.1 and 6.4 in the archive commit — task 6.2's entry was written during implementation — and open the record's own pull request.
- [x] 8.5 Re-run `python3 -m unittest discover --start-directory .github/tests` over the archive commit's own content, before opening its pull request. That commit deletes two queue entries, deletes the exemption that covered them and adds a new entry in one step, so it is the one commit in this change whose green result cannot be inferred from the previous one. **967 tests, OK** — and the task earned its place twice over. Deleting entry 74 turned `test_no_exemption_is_idle` red exactly as design decision 6 predicted, which is what forced the exemption's deletion in this same commit. And run **before** staging the archive move, the suite reports 20 failures that mean nothing: the archive relocates the change directory on disk while the version-control index still lists the old paths, and every sweep in this suite reads tracked files. Stage first, then read the result.

## Not performed

- **1.1's discriminating class, in the check's own module.** The module carries three classes and all three read the committed tree; none hands the finder fixture material built to falsify it.
  Reason: the sibling `test_a_retired_requirement_name_is_reported.py` is that discriminator, in 31 fixture-driven tests written from the delta scenarios by an independent author before this implementation existed. Task 1.12 anticipated the overlap and settled it — "where both would assert the same thing, the scenario-derived one is the one that stays" — so a second class here would restate the sibling's work in a module that cannot be falsified by it. What the absence costs is stated rather than hidden: the reader is falsified only from outside this file, so deleting the sibling would leave this module's green run meaning less than it appears to, and `KNOWN_RETIRED` is the only thing inside it that fails if the derivation silently empties. The code review that found this is the reason it is disclosed instead of ticked.
