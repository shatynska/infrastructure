## 1. Derive the tests from the approved plan

- [x] 1.1 An author other than the implementer writes the static assertions for the delta's scenarios into a new `.github/tests/` module, from the delta spec rather than from the rule, and records the scenario-to-test mapping plus anything deliberately left uncovered in this change's `test-plan.md`. Verify by running `python3 -m unittest discover --start-directory .github/tests` from the repository root and confirming the new assertions fail against the unmodified rule — a baseline recorded before any implementation exists, so that passing later means something. — done by `ai-toolkit:change-test-writer` against the plan commit `60e17eb`: `.github/tests/test_the_container_alert_names_a_container.py`, 23 tests, plus this change's `test-plan.md`. Baseline recorded and independently re-run: **1550 tests OK before, 1573 with 3 failures after**, all three in the new module, each naming the unrestricted selector it found (`container_oom_events_total{}`, `container_start_time_seconds{}`) rather than failing on an absent target.

## 2. Narrow the rule

- [x] 2.1 In `platform/docker-compose.yml`, add the `{name!=""}` selector to both metric selectors in `ContainerRestartingOrOOMKilled`'s expression — `container_oom_events_total` and `container_start_time_seconds` — leaving the thresholds, the `for:`, the rule's own `labels:` block and the annotations untouched. Verify by reading the diff of that file and confirming exactly those two selectors changed. — done. Both selectors carry `{name!=""}`; thresholds, `for: 0m`, `severity: warning` and both annotations are byte-identical.
- [x] 2.2 Move the `prometheus` service's `platform.config-checksum` label to the value the edited config hashes to. This is not bookkeeping: the content of an inline `configs:` block is outside the digest Compose compares, so without it the deploy recreates nothing and reports success. Verify by running `python3 -m unittest test_shipped_config_reaches_the_container` from `.github/tests`, which fails naming the value the label should hold and passes once it holds it. — done, and it was **not** anticipated by the plan as written: the test author found it, and the artifacts were corrected to say so. The label moved from `921a970cfbc8` to `749ebfe68c6e`, the value `test_shipped_config_reaches_the_container` named. Without it the deploy would have recreated nothing and reported success.
- [x] 2.3 Verify the file still renders and parses as Compose, the way `pr-validation.yml` does it: `cp platform/.env.example platform/.env && docker compose -f platform/docker-compose.yml config >/dev/null && rm platform/.env`, run from the repository root and expected to exit 0. — done: `docker compose -f platform/docker-compose.yml config` exits 0 against `platform/.env.example`.
- [x] 2.4 Verify the `prometheus_rules` entry is still valid Prometheus rule YAML by parsing it the way the new test module does — load the Compose file, undo the `$$` escaping in the entry's `content`, parse it as YAML, and confirm the narrowed rule is among the groups with both selectors present. — done: the entry parses as rule YAML once `$$` is undone, and the rule reads `increase(container_oom_events_total{name!=""}[10m]) > 0 or changes(container_start_time_seconds{name!=""}[10m]) > 3`.

## 3. Verify

- [x] 3.1 Run `python3 -m unittest discover --start-directory .github/tests` from the repository root and confirm the whole suite is green — the new module included, which turns the recorded baseline from failing to passing, and every existing module, which establishes the change broke none of them. — done: **1573 tests, OK**, run from the repository root. The three recorded failures are now passes and no existing module moved.
- [x] 3.2 Run `pre-commit run --all-files` and confirm it passes, having first provisioned this working tree per README's Local setup step 5 (`ansible-galaxy role install -r ansible/requirements.yml -p ansible/roles`), so that an unprovisioned tree's `ansible-playbook --syntax-check` failure is not mistaken for a defect in this change. — done: all seven hooks passed (`terraform fmt`, `tflint`, `terraform validate`, `gitleaks`, `shellcheck`, `ansible-lint`, `ansible-playbook --syntax-check`). This tree was provisioned first with `ansible-galaxy role install -r ansible/requirements.yml -p ansible/roles`.
- [x] 3.3 Commit the implementation, then dispatch `ai-toolkit:change-code-reviewer` over the committed diff and address what it reports. — done over `76806ee..d9e731a`: **no blocking defects**. Two `[MINOR]` findings, both documentation in this change's own artifacts and both applied: `design.md` described the test module as locating the rule by its `alert` name when it locates by the metrics a rule reads, and `tasks.md` 5.1 still explained itself in the superseded reload vocabulary. A third finding was recorded as a latent property needing no action and is now in `design.md`. The reviewer independently recomputed all three config checksums, confirmed the narrowed selector is inside the `prometheus` digest input, and established by probe that the assertions discriminate — a half-applied selector is reported on exactly one side, and a rename carrying the defect is still caught.

## 4. Ship

- [x] 4.1 Open the pull request and verify continuous integration passes on the branch head — `PR Validation` in particular, which runs the static suite unconditionally. — done: PR #252, all four running checks passed (`validate`, `ansible-verify`, two `discover` jobs); the Terraform `plan` and Molecule matrices skipped, neither being touched.
- [x] 4.2 Wait for the operator's confirmation that the pull request merged and that `Platform Deploy` reported healthy for both stacks. Do not infer any of that from a green pull request. — done: the operator confirmed the merge and that Platform Deploy completed. Not inferred from the green pull request.

## 5. Confirm the effect on the deployed staging host

- [x] 5.1 Confirm the deployed rule is the narrowed one: on staging, read it back out of the running Prometheus with `docker exec platform-grafana-1 wget -qO- "http://prometheus:9090/api/v1/rules"` and confirm `ContainerRestartingOrOOMKilled`'s expression carries `name!=""` on both metrics. A deploy that recreated nothing is what this catches — there is no reload path here, so an unmoved checksum would leave the container running the old rule while the deploy reported success. — done: `platform-prometheus-1` was **recreated** at `2026-09-17T12:59:43Z`, and the rule read back out of the running instance is `increase(container_oom_events_total{name!=""}[10m]) > 0 or changes(container_start_time_seconds{name!=""}[10m]) > 3`. This is the evidence that moving `platform.config-checksum` was load-bearing: without it the container would not have been replaced and the old rule would still be running.
- [x] 5.2 Confirm the narrowed expression selects only containers: evaluate its two halves against staging's Prometheus and confirm every series returned carries a `name`, and that the count matches `docker ps -q | wc -l` on that host. Measured before this change on 2026-09-17: 46 `container_start_time_seconds` series of which 10 had a name, and 55 `container_oom_events_total` series of which 10 had a name. — done: 10 of 54 `container_start_time_seconds` series selected, against `docker ps -q | wc -l` = 10. Before the change the counts were 46/10 and 55/10; cAdvisor now exports 54 of each, the difference being ordinary cgroup turnover.
- [x] 5.3 Confirm the negative scenario against the churn that actually provoked the incident: open and close more than three short-lived SSH sessions to the staging host inside ten minutes, which recreates the `/user.slice/user-1000.slice/…` control groups. **Establish that the churn happened before reading the silence**, because "no alert fired" is evidence only if the condition recurred: confirm `changes(container_start_time_seconds{name=""}[10m]) > 3` returns that session control group at that moment, and only then confirm no `ContainerRestartingOrOOMKilled` appears in Prometheus's alerts and none reaches the Slack channel. The sessions must be **sequential** — each closed before the next opens, since an overlapping session keeps `user-1000.slice` alive and the cgroup is never recreated — and spaced past a scrape interval, for the reason 5.4 gives. Four quick overlapping `ssh` invocations produce a clean channel that means nothing and is indistinguishable from a confirmed fix. Before this change that sequence fired the alert; a clean channel afterwards, with the churn shown to have occurred, is the observable effect the operator is asked to confirm. — done, **by controlled comparison rather than by an absence**. Six sequential SSH sessions, each held open 35 s and separated by 35 s. Evaluated at one instant, `13:43:44Z`: the **old** expression returned 8 series — `/user.slice/user-1000.slice` and its seven children, `changes() = 7` against a threshold of 3 — and the **new** expression returned empty. Eight series is the count of notifications the incident produced. Prometheus showed `Watchdog` alone, and the operator confirmed nothing reached the Slack channel between 16:36 and 16:43 Kyiv.

  **The first attempt was a false pass and is recorded because it nearly went unnoticed.** Five sessions 37 s apart produced a silent channel and a non-firing rule — and proved nothing, because the *old* rule would not have fired either: `changes()` reached only 3 against a threshold of `> 3`, since a session cgroup created and destroyed entirely between two 30 s scrapes is never observed. Holding each session open past a scrape is what fixed it. The instruction to establish the churn before reading the silence is what caught this.
- [x] 5.4 Confirm the positive scenario still works: restart one container more than three times in ten minutes, spacing the restarts at least 35 seconds apart so each start is scraped — `fix-cadvisor-containerd-snapshotter` recorded that a faster sequence undercounts `changes()` and proves nothing — and confirm the alert fires naming that container. Use a throwaway container, not a platform service. — done: a throwaway `prom/node-exporter:v1.12.1` instance named `restart-alert-confirm-target`, restarted five times at ~42 s intervals. `changes()` reached 5 on it and 0 on all ten real containers, and the alert fired. The operator confirmed the Slack notification at 16:47 Kyiv reading `Container restart-alert-confirm-target is crash-looping or was OOM-killed` — **both `$labels.name` interpolations filled**, against the same rule's `Container  is crash-looping` of that morning. The container was removed afterwards; `docker ps -a` shows the ten platform and application containers and nothing else.
- [x] 5.5 Report to the operator what 5.1 through 5.4 showed and wait for their confirmation before archiving. — done: reported, and the operator confirmed both halves. They also supplied the cutoff — the last nameless notification arrived at 15:13 Kyiv (12:13 UTC), which Prometheus's own history places at 12:20 UTC on `/user.slice/user-1000.slice`, caused by this session's investigation probes and **46 minutes before the deploy**. Nothing nameless has fired since.

## 6. Archive

- [x] 6.1 Bring the branch back to the freshly fetched trunk, run `openspec archive scope-the-restart-alert-to-real-containers`, stage the result with `git add -A` — the archive moves files without telling git, and the repository's sweep tests fail until it is staged — and open the record's own pull request. — done: this commit.

Removing the branch and this working tree happens after that pull request merges, which is after the commit that writes this file, so it is not a task here. `AGENTS.md` records it as prose for exactly that reason.
