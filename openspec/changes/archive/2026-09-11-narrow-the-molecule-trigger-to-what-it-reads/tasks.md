Verification commands referenced below, from this project's conventions:

- `python3 -m unittest discover --start-directory .github/tests`, from the repository root
- `ansible/scripts/run-molecule test --all`, from a role directory — read the SCENARIO RECAP, not the exit code
- `pre-commit run --all-files`, which carries `terraform fmt`, `tflint`, `terraform validate`, `gitleaks`, `ansible-lint` and `ansible-playbook --syntax-check`
- `openspec validate --all`

Sections 1–5 land through this change's first pull request. Section 6 is the `ship:confirm` observation, and one half of it cannot be made on this change's own pull request — `design.md` Decision 3 says why, and section 6 records what is observed where. Section 7 is the archive. Removing the branch and the working tree happens after this record's own pull request merges, which is after the commit that writes this file, so those steps are recorded in prose in `proposal.md` and in `AGENTS.md` rather than as tasks here.

**Order matters between sections 1 and 2, and in one direction only.** The scans must be running in `.github/tests` before the filter stops selecting the suite on inventory changes. Landing them in one pull request makes the ordering internal, but do not reverse it within the branch: a commit that narrows the filter while the inventory scan still lives in `ops_user`'s verify play is a commit at which a pull request editing `ansible/inventory/group_vars/prod.yml` is scanned by nothing.

## 1. The repository-scope scans move to `.github/tests`

- [x] 1.1 Establish the file set the relocated scans read, per `design.md` Decision 5: the repository's tracked files, enumerated with `git ls-files`. Three behaviours are decided there and all three belong in this helper rather than being rediscovered by whoever writes the scans:
      - Where the enumeration fails — no `git`, or not a repository — the assertion **fails** and names the enumeration as the cause; it does not skip. This is the one place `.github/tests`' existing `require_external_tools` skip-guard must not be copied, and the reason belongs in the docstring: a guarded skip is right for an assertion whose absence leaves another check standing, and wrong for a scan for committed credential material, where a skip reports success for a property nothing examined.
      - The helper returns **bytes**, and the scans match bytes. Decoding every tracked file as text makes an undecodable file — none is tracked today, and nothing prevents one — a hard failure that examined nothing, which is this decision's own failure mode reached from the other side. The markers are ASCII; nothing is lost.
      - A listed path **absent from the working tree fails, naming that path**. A sparse or interrupted checkout cannot establish a property over the committed tree, and it is a state to hear about rather than to scan around.

      Put it, and the three relocated scans, in `.github/tests/test_ci_configuration.py` rather than in a new module. That module is where this repository's conventions record the suite's own constraint assertions as reading — no network call, no credential, no container runtime, no Terraform binary — and the enumeration exception the delta bounds is exactly the kind of thing those assertions must be able to see. A new module would put the one subprocess this suite admits outside the checks that police subprocesses; widening those assertions across modules is `docs/change-queue.md` entry 45's, not this change's.

      Write this helper first; 1.2 through 1.4 all consume it.
- [x] 1.2 Relocate the GitHub/GHCR token-marker scan out of `ansible/roles/deploy_user/molecule/default/verify.yml:419`. It greps for `gh[pousr]_[A-Za-z0-9]{20,}` and `github_pat_[A-Za-z0-9_]{20,}`; the reimplementation reads 1.1's file set, and its failure message names the offending files as the sibling refusals in that module do. Delete the task and its `assert` from the verify play, leaving a comment at the deletion point recording where the assertion went and why — the scenario's own header still lists it, and a reader finding the header without the task must not conclude the check was dropped.
- [x] 1.3 Relocate the `ghcr_pull_token` literal scan from the same file, at line 457. **This is the task where a casual reimplementation loses the property that matters**, and the source comment says so in as many words: both scenario converge files assign the token as a YAML folded scalar, so the assignment line is bare and the `lookup('env', ...)` that makes it acceptable sits on following lines. It is already Python — move it as nearly as-is as the new home permits, keeping the continuation-absorbing loop rather than rewriting it against a YAML parser, and carry across the comment explaining why the two scenario files are deliberately *not* excluded by path. Its file set changes from its own `rglob("*.y*ml")` over the repository to 1.1's tracked files filtered to the same extensions — the YAML restriction is the scan's subject rather than an optimisation, since what it matches is a YAML assignment. Delete the task and its `assert` from the verify play and leave a comment at the deletion point, exactly as 1.2 does; a scan copied rather than moved leaves the suite asserting what `.github/tests` now asserts, on a subset of the pull requests.
- [x] 1.4 Relocate the operator private-key scan from `ansible/roles/ops_user/molecule/default/verify.yml:631`, **widening its root from `ansible/inventory/` to 1.1's file set** per `design.md` Decision 4. Leave the scan at line 618, over the role's own directory, where it is. The assembled-pattern technique carries over unchanged and for the same reason: a file searching for the private-key delimiter matches itself if the pattern is spelled literally, and that is as true in a Python test module as in a verify play. Adjust the surviving `assert` so it no longer references the removed register, and correct the scenario header, which currently describes a scan over both roots.
- [x] 1.5 Assert the negative case for all three scans against `tempfile` fixtures rather than by introducing a marker into the working tree. A real token literal committed even briefly trips `gitleaks` in `pre-commit` and risks being left behind; a fixture makes the red state permanent evidence rather than a one-off observation, which is the argument task 3.4 makes for the premise check and it applies here too. Because the scans read tracked files, the fixture must exercise the matcher over a supplied file list rather than over the repository — so write them to take that list as a parameter, which 1.1's helper supplies in production. **Assemble the fixture payloads at run time, from halves, for the same reason 1.4's search pattern is assembled**: a literal `gh[pousr]_…` or private-key delimiter written into `.github/tests/test_ci_configuration.py` is itself a tracked file, so the relocated scans would match their own module — and `gitleaks` in `pre-commit` would very likely block the commit that introduced it. The pattern being assembled is not enough on its own; the payload it is tested against must be too.
- [x] 1.6 Confirm the scans are clean on the current tree before relying on them. Only one widens — 1.4's private-key scan, from `ansible/inventory/` to every tracked file — so it is the one that could inherit a pre-existing hit; 1.2's keeps the repository root it had and 1.3's keeps its YAML restriction. During planning the token-marker and private-key patterns were each run over all 448 tracked files and matched nothing. Re-run all three and record the tracked-file count in this task's completion note, since it will have moved; a hit is then a real finding rather than something the widening dragged in.
      **Performed 2026-09-11.** 449 tracked files; the token-marker and private-key patterns each matched nothing, and no `ghcr_pull_token` assignment carries a committed literal. The count moved by one from planning's 448 because the derived-test module had landed by then.
- [x] 1.7 **Widen the suite's own spawn audit to admit the enumeration binary.** `.github/tests/test_ci_configuration.py` carries `SPAWNABLE = {"bash", "sh"}`, read by two assertions — one over the module itself and one over every module in the suite directory — and 1.1's tracked-file enumeration adds a third spawnable name. Without this the suite goes red on the commit that introduces the helper, naming the module rather than the cause, which reads as a broken test rather than as an audit doing its job. Add the name **with the reason in place**: it is admitted because it is present wherever the repository was checked out, which is the same bound the delta puts on the exception, and not as a general relaxation. This task was identified by the test author, whose first new module tripped the audit; no earlier review round named it.
- [x] 1.8 Run `python3 -m unittest discover --start-directory .github/tests` and confirm green.

      **Performed 2026-09-11.** This task originally also required `ansible/scripts/run-molecule test --all` from `ansible/roles/deploy_user` and `ansible/roles/ops_user`; that half is disclosed under `

## Nothing not performed

This section replaces a `## Not performed` disclosure, and says so rather than simply vanishing. That disclosure recorded 4.2 — the Molecule suite across all seven authored roles, and the Molecule half of 1.8 — as unreachable, because the environment the change was authored in had no container runtime (`docker info` failed), and a run that cannot reach what it needs reports success rather than failing.

**It was performed on 2026-09-11, in a session whose environment did have one**, and is ticked above with its per-role SCENARIO RECAP. The disclosure is removed rather than kept alongside the tick because leaving both would make the record say two things at once; what it was protecting against — a ticked box meaning either that the work happened or that it did not — is exactly what the evidence under 4.2 now settles.

The judgment in that disclosure was right at the time and is worth keeping in view: what it declined to do was report an unreachable verification as passed. The gap it left was narrow, and it named where the same ground was covered meanwhile — continuous integration on this change's own pull request, which task 6.1 records.
