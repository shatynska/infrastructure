> **Read `test-plan.md` in this directory before starting.** It is not an
> OpenSpec-schema artifact, so it does not appear in `openspec instructions
> apply`'s context and must be opened deliberately. It holds the per-scenario
> accounting (29 scenarios: 14 covered, 10 partial, 5 uncovered with reasons),
> the classified pre-implementation baseline, the exact CI invocation, and four
> questions it deliberately left open.

## 0. Derived tests and their baseline — before any implementation

- [x] 0.1 Confirm `.github/tests/test_ci_configuration.py` is present and that `test-plan.md` records, for each of the 29 scenarios, the test covering it or the reasoned exemption. Run `python3 -m unittest discover --start-directory .github/tests --verbose` from the repository root and record the baseline **by `test-plan.md`'s classification, not by its totals**: 15 failures in state 1 (the assertion ran and discriminated), 8 in state 2 (never executed — `ansible-verify.yml` does not exist), 23 passing of which 1 is flagged vacuous. The totals alone are reachable by a wrong mix, so reproduce the split. This task runs **first**: every later group turns state-1 failures green, and a suite that passes before the implementation exists asserts nothing.
- [x] 0.2 Add an unconditional step to `pr-validation.yml` running the suite: `python3 -m unittest discover --start-directory .github/tests --verbose`, working directory the repository root (the workflow default — do not set `working-directory:`), installing dependencies from `.github/requirements-ci.txt` (design Decision 7). Verify by grepping that the step carries no `if:` condition and no `continue-on-error:`, and that the job still declares no `environment:`.
- [x] 0.3 Add `__pycache__/` to `.gitignore` — this repository has had no Python until now, and running the suite creates one. Verify that after a suite run, `git status --short --untracked-files=all` lists no `__pycache__` path — the tree as a whole is not clean at this point, since 0.1 and 0.2 have already added a file and edited a workflow, so the check is on the ignore rule and not on the tree.
- [x] 0.4 Record the second test command in `AGENTS.md`'s "Testing" section: this project now has two, `terraform test` over `terraform/modules/<name>/tests/*.tftest.hcl` and `python3 -m unittest discover --start-directory .github/tests` over `.github/tests/*.py`. Land this early rather than last — a test-writing dispatch that carries only the Terraform glob cannot place a CI-configuration test, which is exactly what happened while deriving this change's tests. Verify the existing `terraform test` conventions and glob are preserved unchanged.

## 1. Destroy-policy gate fails closed

- [x] 1.1 Rewrite the `Destroy-policy gate` step in `.github/workflows/apply.yml` per design Decision 5: assert `format_version` is present before inspecting; capture `jq`'s exit status explicitly with `if ! has_destructive=$(...)` rather than relying on `set -e`; invoke `jq` without `-e` and without `|| true`; match the result against exactly `true` and exactly `false`, with every other outcome falling to an inspection-failure branch that fails the workflow with a message naming plan inspection as the cause. Verify by running the rewritten script locally against four fixtures — a plan JSON with no destructive action (proceeds, prints the clean message), one containing a `delete` action (reaches the label check), a malformed `plan.json` (fails **with the inspection-failure message**, not a raw `jq` parse error), and valid JSON that is not a plan such as `{}` (fails with the inspection-failure message, and does **not** print "No delete/replace actions"). Keep the four fixture files rather than discarding them, and note in task 7.3's queue entry where they are, so the queued script-restructuring change inherits real inputs instead of re-deriving them.
- [x] 1.2 Confirm the step's untouched parts are byte-identical to before: the `destroy-override` label mechanism, the merge-commit PR-number parse, the search-API fallback and its retry budget. Verify with `git diff` on the file showing changes confined to the inspection block.

## 2. `apply.yml` triggers only on Terraform-affecting pushes

- [x] 2.1 Add a workflow-level `paths: ['terraform/**']` filter to `apply.yml`'s `on: push:` trigger, with a comment recording why this is permissible here and forbidden on `pr-validation.yml` (design Decision 6). Verify with `actionlint` or `yq` that the workflow still parses and the `branches: [main]` constraint is intact.
- [x] 2.2 Confirm no `paths:` filter was added to `pr-validation.yml` at the workflow level. Verify by grepping that file for `paths:` and finding only the `dorny/paths-filter` step inputs.

## 3. Secret scanning becomes unconditional and version-locked

- [x] 3.1 Remove `if: steps.changes.outputs.terraform == 'true'` from both the `Install gitleaks` and `gitleaks` steps in `pr-validation.yml`. Verify by grepping the file: no step whose name contains `gitleaks` carries that condition, and confirm on the diff that both steps remain ordered **before** the `terraform plan` step, which the preserved "PR with a leaked credential fails validation" scenario requires.
- [x] 3.2 Replace the hardcoded `8.24.2` with an extraction of the pinned `rev` from `.pre-commit-config.yaml`'s gitleaks block (design Decision 2), failing the step with an explicit message if the extraction yields an empty string. Verify by running the extraction snippet against the current `.pre-commit-config.yaml` and getting `8.30.0`, and against a copy with the gitleaks block removed and getting a non-zero exit.
- [x] 3.3 Change the scan invocation from `gitleaks detect --source .` to `gitleaks dir . --redact --exit-code 1` (design Decision 1). Verify by running gitleaks 8.30.0 with the new invocation over a clean checkout of this branch: exit code 0.
- [x] 3.4 Verify the new invocation actually detects: run it over a scratch copy of the tree with a known test secret added, and confirm exit code 1 with the finding redacted. Delete the scratch copy afterwards.

## 4. Ansible lint and syntax check become blocking

- [x] 4.1 Add an `ansible` filter (`ansible/**`) to the `dorny/paths-filter` step in `pr-validation.yml`. Verify the step's `filters:` block parses and the existing `terraform` and `platform` filters are unchanged.
- [x] 4.2 Create `.github/requirements-ci.txt` pinning `pre-commit` **and PyYAML** to exact versions, with a comment stating why each pin exists, that `pre-commit-autoupdate.yml`'s unpinned install is queued separately (design Decision 3), and that PyYAML previously reached this repository only transitively via `ansible-core` — which is not a pin (design Decision 7). Pin PyYAML to **6.0.1**, the version `test-plan.md` records the 46 tests as verified against. Verify `pip install -r .github/requirements-ci.txt` resolves to exactly those versions in a scratch virtualenv.
- [x] 4.3 Add steps to `pr-validation.yml`, conditioned on `steps.changes.outputs.ansible == 'true'`: set up Python, `pip install -r .github/requirements-ci.txt` and `pip install -r ansible/requirements-test.txt`, install Galaxy content **into `ansible/roles/`** — `ansible-galaxy collection install -r ansible/requirements.yml` then `ansible-galaxy role install -r ansible/requirements.yml -p ansible/roles`, split because `-p` applies only to roles and silently drops collections, and targeted because `ansible/ansible.cfg`'s `roles_path = roles` replaces the default search list so `~/.ansible/roles` is never consulted — cache `~/.cache/pre-commit` keyed on the runner OS, Python version and `.pre-commit-config.yaml`, then run `pre-commit run ansible-lint --all-files` and `pre-commit run ansible-playbook-syntax-check --all-files` (design Decision 3). The second manifest is what supplies `ansible-galaxy` and `ansible-playbook`: confirm `ansible/requirements-test.txt` pins `ansible-core` exactly (it pins `2.21.3`) and record that, since the tier fails at `ansible-galaxy` if it does not. Verify by running both `pre-commit run` commands locally from a provisioned checkout and confirming they pass — and note the limit of that evidence: `ansible/roles/geerlingguy.docker/` is gitignored but present locally, so a local pass cannot distinguish a correct install path from a broken one. The install path is established by reading `ansible-config dump`'s `DEFAULT_ROLES_PATH` from `ansible/`, not by the local run.
- [x] 4.4 Confirm the job still declares no `environment:` and receives no Hetzner, SSH or registry credential. Verify by grepping the whole job for `environment:` and for `secrets.` and finding only the pre-existing `TF_API_TOKEN` and `HCLOUD_TOKEN` uses inside the Terraform-gated steps.
- [x] 4.5 Confirm every `pip install` added to `pr-validation.yml` names a `-r` manifest and no bare package, so nothing resolves a floating version. Verify by grepping the file for `pip install` and inspecting each hit.

## 5. Molecule runs as an advisory workflow

- [x] 5.1 Create `.github/workflows/ansible-verify.yml` triggered on `pull_request` with `paths: ['ansible/**']` **and** on `workflow_dispatch:`, with `permissions: contents: read` and a `concurrency` group keyed on the PR number (or ref, for a dispatch) with `cancel-in-progress: true`. Carry a comment recording that the workflow-level `paths:` filter is permissible only while the workflow is advisory, and must be removed and moved inside an always-running job before it is made a required check (design Decision 4). Verify it parses with `actionlint`.
- [x] 5.2 Add a `discover` job that emits a JSON array of role names having an `ansible/roles/*/molecule/` directory as a step output, and fails with an explicit message if that array is empty (design Decision 4). Verify by running its shell snippet locally and getting exactly `deploy_user`, `docker`, `hardening`, `ops_user`, `platform_data_volume`; by confirming the snippet names no role literally; and by running it against a scratch tree with no matching directory and observing a non-zero exit rather than an empty array.
- [x] 5.3 Add a `molecule` job consuming that output via `strategy.matrix` with `fail-fast: false`, installing the toolchain from `ansible/requirements-test.txt` and Galaxy content from `ansible/requirements.yml`, then running `molecule test --all` with working directory `ansible/roles/${{ matrix.role }}`. Verify the pinned manifests are the only dependency sources by grepping the workflow for `pip install` and finding no package named outside `-r`.
- [x] 5.4 Confirm the workflow uses no `continue-on-error:` anywhere, so its conclusion is truthful and advisory status comes solely from branch protection (design Decision 4). Verify by grepping the file.
- [x] 5.5 Confirm the workflow declares no `environment:` and consumes no secret. Verify by grepping for `environment:` and `secrets.` and finding neither.

## 6. Dependabot covers every lockfile-bearing directory

- [x] 6.1 Add `/terraform/modules/volume` to the `terraform` ecosystem `directories` list in `.github/dependabot.yml`. Verify by comparing the output of `find terraform -name .terraform.lock.hcl -printf '/%h\n' | sort` against the configured directory list and finding them equal.

## 7. Records and follow-ups

- [x] 7.1 Add an entry to `docs/change-queue.md` for promoting the Molecule workflow to a required status check. It must state four things, not one: the green-run condition; that promotion **first** requires removing the workflow-level `paths:` filter and moving the gating inside an always-running job, because a `paths:`-filtered required check leaves non-matching pull requests permanently unmergeable per the Required Status Checks Report on Every Pull Request requirement; that it is blocked on entry 7.2 landing first, since a floating base image makes "consecutive green runs" evidence about a moving target; and a slot for the first observed per-role outcome and duration, filled in by task 8.7. The baseline belongs in this file rather than in the change's own artifacts, which are archived immediately after — the condition and the evidence it is evaluated against must outlive the change together. Verify the existing entries' content and their dependency ordering are preserved; note that inserting entries ahead of `size-platform-container-resource-limits` renumbers it, and the preamble is reworded because not every entry is blocked on another.
- [x] 7.2 Add an entry to `docs/change-queue.md` for pinning the Molecule platform image: every scenario uses `geerlingguy/docker-ubuntu2204-ansible:latest`, a floating tag, against AGENTS.md's exact-pinning convention. Noticed while implementing this change and deliberately not folded into it. Verify the entry names the affected scenario files.
- [x] 7.3 Add an entry to `docs/change-queue.md` for the two deferred CI items: `pre-commit-autoupdate.yml`'s unpinned `pip install pre-commit`, and moving the destroy-policy gate's inspection logic into a version-controlled script with executable fixtures rather than inline workflow shell (design Decision 5's second rejected alternative). Verify each names its file.
- [x] 7.4 Update `README.md`'s Ansible testing section to state that lint and syntax checks now run in CI on pull requests touching `ansible/`, and that Molecule runs there advisorily. Verify the local `molecule test --all` instructions are preserved unchanged.
- [x] 7.5 **At archive time, not in the implementation pull request**: extend `openspec/specs/iac-cicd-pipeline/spec.md`'s `## Purpose` to name **both** requirements this change adds to the capability — Ansible lint, syntax and Molecule verification, *and* the executable suite that verifies the pipeline's own configuration — alongside the Terraform scope it currently lists. A capability's Purpose cannot be expressed as a delta, so it is hand-edited — but it belongs in the same pull request as the rest of the spec record, so the Purpose does not advertise Ansible verification during the window before the requirements themselves land. Verify by reading the archived `openspec/specs/iac-cicd-pipeline/spec.md` and confirming its Purpose names both new subjects and its requirements include both archived ADDED requirements.

## 8. Verification

- [x] 8.0 Provision the working tree before relying on any result below: create a virtualenv, install `.github/requirements-ci.txt` and `ansible/requirements-test.txt`, run `ansible-galaxy install -r ansible/requirements.yml`, and run `pre-commit install --hook-type pre-commit --hook-type commit-msg`. Verify `pre-commit --version` and `molecule --version` both report the pinned versions. Until this completes, every verification below is reported as **not run, and why**.
- [x] 8.1 Run `pre-commit run --all-files` on the branch and confirm it passes, and that `actionlint` accepts every file under `.github/workflows/`.
- [x] 8.2 Run the local equivalent of the blocking Ansible tier — `pre-commit run ansible-lint --all-files` and `pre-commit run ansible-playbook-syntax-check --all-files` — plus `gitleaks dir . --redact --exit-code 1` at the version `.pre-commit-config.yaml` pins, and record each result. This is the blocking tier's only evidence for this change: its CI steps are gated on `ansible/**` and this pull request touches no such path, so they will report as skipped (design, Migration Plan).
- [x] 8.3 Run `molecule test --all` locally for at least one multi-scenario role (`ops_user`) to confirm the suite the advisory workflow will run is green at this commit, so a CI failure is attributable to the runner rather than to the suite. Record the result; if it cannot be run locally, report it as not run and why.
- [x] 8.4 Re-run the CI-configuration suite now that groups 1-6 are complete, and confirm every state-1 failure recorded in task 0.1 has turned green. Confirm the 8 state-2 tests now **execute their assertions** rather than failing on a missing `ansible-verify.yml` — a green first run is the first evidence those eight discriminate at all, so read it as that and not as coverage already established. Re-check the one pass `test-plan.md` flags as vacuous (`test_the_validation_workflow_installs_only_from_requirement_manifests`): it passes today because `pr-validation.yml` has no `pip install`, and only task 4.3 makes it meaningful.
- [x] 8.5 Open the pull request and confirm on it: the required check reports a conclusion, the gitleaks step ran despite no Terraform change, and the Terraform and Ansible steps skipped for the stated reasons. Confirm no check is left permanently pending, and that the CI-configuration suite step ran and passed — it is the only new check this pull request actually exercises besides gitleaks. `ansible-verify.yml` will not appear on this pull request and cannot be dispatched yet — see 8.7.
- [x] 8.6 After merge, confirm `apply.yml` did **not** run for this pull request and no `production` Environment approval was requested, since it touches no `terraform/**` path.
- [x] 8.7 After merge, dispatch `ansible-verify.yml` manually on `main` — the trigger is only exposed once the workflow file is on the default branch (design Decision 4), so this cannot be done earlier. Record the per-role outcome and duration **into task 7.1's promotion entry in `docs/change-queue.md`**, not only in the run log or this change's artifacts — the artifacts are archived immediately after, and the promotion condition is evaluated by a later session that will read the queue file and nothing else. That record is this change's confirm-gate evidence and the baseline the promotion depends on. If the run is red, record which scenarios failed and how, in the same place, and report that as the change's finding rather than as a defect to fix here.

## Outcomes recorded at archive

- **8.6** — merge commit `c7e516d` triggered only Dependabot Updates. No
  `Terraform Apply (prod)` run and no `production` Environment approval
  request, where the preceding merge `4242127` did produce one. The path
  filter works.
- **8.7** — dispatched on `main` as run `34046099603`, confirming
  `workflow_dispatch` resolves once the file is on the default branch. Four
  roles pass, `platform_data_volume` fails identically to its pull-request
  run; both results are in `docs/change-queue.md` entry 4, and the failure
  itself is entry 8.
- **7.5** — `openspec/specs/iac-cicd-pipeline/spec.md`'s Purpose extended to
  name both added subjects, in this archive pull request.
- **Confirm gate** — the operator confirmed the change working on the evidence
  above and instructed archiving. The destroy-policy gate remains unexercised
  in the pipeline: it fires only on a merge touching `terraform/**`, and this
  change touched none. Its four routes were verified against fixtures locally;
  the next Terraform change exercises it for real.

