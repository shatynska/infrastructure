## 1. Tests

- [x] 1.1 Record the exemption rather than dispatching a test author. This change declares no specification delta — `design.md` Decision 1 substantiates that against the three requirements in `openspec/specs/` that bear on this file, and records the one it knowingly leaves disagreeing with the tree. Verify by confirming this change's directory contains no `specs/` subtree, that its `.openspec.yaml` sets `skip_specs: true`, and that `openspec validate refresh-readme-accuracy` passes on that basis.
- [x] 1.2 Record the baseline: `python3 -m unittest discover --start-directory .github/tests` from the repository root, in this working tree, after provisioning. Note the test count. Verify it is green — a green suite before the change is what makes a green suite after it mean anything.

## 2. Re-verify every row before correcting it

`design.md` names staleness-on-arrival as this change's first risk: the handoff was stale in three ways within two days, and the plan review found three more claims in these artifacts that did not survive checking. Do this against the tree, not against `proposal.md`'s table.

- [x] 2.1 Re-read each row against the file it names — `terraform/environments/prod/terraform.tfvars`, `terraform/modules/*/tests/`, `platform/docker-compose.yml`'s service keys and line count, `ansible/roles/*/molecule/*/molecule.yml`, `.github/workflows/`, `openspec/changes/archive/`, and the three files in the second table. Verify by producing each count or value from a command, not by reading the table.
- [x] 2.2 Confirm the line numbers still resolve. Every one was read at `102a303`; a rebase onto a moved trunk shifts them. Verify by opening the file at each cited line.
- [x] 2.3 Confirm no row has been fixed by another change in the meantime, and drop any that has. Verify by `git log --oneline <trunk-at-proposal>..HEAD -- README.md`.

## 3. Non-goals and Repository layout

- [x] 3.1 Correct the region: state `hel1` and name `terraform/environments/prod/terraform.tfvars` as where it is set, per `design.md` Decision 3. Verify the value against that file, not against this document.
- [x] 3.2 **The trap in 3.1**: the bullet is a *non-goal*. Multi-region deployment stays rejected, and the rejection stays the bullet's first clause; the region's identity is subordinate to it. Verify by reading the edited bullet back and confirming it still reads as a rejection rather than as a description of where the server runs.
- [x] 3.3 Keep the staging framing intact — "a staging environment is *not* a non-goal" is the deliberate distinction the handoff protects, and Status still refers to it. Leave the Non-goals sentence untouched; verify by diff.
  - **Deviated, knowingly.** The Non-goals sentence (`README.md:28-29`) is untouched, so the framing this protects survives. The *Status* staging paragraph was edited: correcting Status to "The bootstrap is done" left the next paragraph saying staging is anticipated "after `prod` is fully stood up" — a condition now met — beside "not yet in scope". Changed to "still anticipated as the next environment", which preserves anticipated-not-rejected. Code review judged this right and asked that the deviation be recorded rather than left silent.
- [x] 3.4 Complete the section against every **committed** top-level directory, per `design.md` Decision 2's closing section. There are seven, not the five a `ls -d */` suggests: the section has `terraform/`, `ansible/` and `platform/`, and omits `.github/` (workflows, `tests/`, `dependabot.yml`, `requirements-ci.txt`), `openspec/` (specifications and change records), `docs/` (`change-queue.md`, `deferred-work.md`) and `.claude/` (the OpenSpec slash commands and skills this repository commits). Verify with `git ls-files | grep / | sed 's|/.*||' | sort -u`, which returns the seven and nothing else, and confirm the correspondence is complete in both directions. The `grep /` is load-bearing: without it the command also emits the seven top-level *files*, leaving "filtered to directories" to the reader's eye — which is `ls -d */`'s failure in a different key.
- [x] 3.5 **The first trap in 3.4**: the membership rule must be stated in the section, not left implicit. "Five top-level directories" was this change's own first answer and was wrong, because `ls -d */` silently drops every dotted directory — including `.github/`, whose contents the README discusses at length. A completed enumeration whose completeness is checkable only by an unwritten convention is not the derivable form `design.md` Decision 2 argues for. Verify the section says which directories it lists and that the stated rule matches the command in 3.4.
- [x] 3.6 **The second trap in 3.4**: the `.claude/` bullet must say that `commands/` and `skills/` are what is committed. The section's stated rule is "every committed top-level directory", and `.claude/worktrees/` — where `AGENTS.md` places working trees — is neither committed nor ignored, so a bullet naming the directory without qualifying it implies working trees are repository content. Describe the directory's role in a clause; do not inventory its contents. Verify against `git check-ignore -v .claude/worktrees/`, which reports it is not ignored.
- [x] 3.7 **The third trap in 3.4**: do **not** add `terraform/modules/volume` to the modules bullet. `design.md` Decision 2's closing section says why — that bullet says "e.g. `terraform/modules/server`", which is an example, marked as one, and correct. Turning it into a list creates the enumeration this change is arguing against, and obliges every future module to edit it. Verify the modules bullet is unchanged by diff.
- [x] 3.8 Correct the `platform/` description to what the stack actually contains: reverse proxy, PostgreSQL, and the monitoring services. Verify against the service keys in `platform/docker-compose.yml`.
- [x] 3.9 Leave the `ansible/` bullet's scope-boundary sentence verbatim. It is a real constraint, listed in the proposal's must-not-undo. Verify by diff.

## 4. Local setup step 5, and the three other copies

- [x] 4.1 Replace `ansible-galaxy install -r ansible/requirements.yml` with the two commands CI uses: `ansible-galaxy collection install -r ansible/requirements.yml` and `ansible-galaxy role install -r ansible/requirements.yml -p ansible/roles`. Verify they are byte-identical to `.github/workflows/pr-validation.yml`'s *Install Galaxy content* step.
- [x] 4.2 State why `-p ansible/roles` is load-bearing, in one or two sentences: `ansible/ansible.cfg` sets `roles_path = roles`, which replaces rather than extends the search list, and every scenario overrides `ANSIBLE_ROLES_PATH` to the same place — so a role in `~/.ansible/roles` is invisible and converge fails on the dependency. Without this a future editor "simplifies" it back. Verify the claim against `ansible/ansible.cfg` and one scenario's `molecule.yml`.
- [x] 4.3 Confirm the split into two commands survives review pressure to merge them: `-p` applies only to roles and is silently ignored for collections, with a warning easy to miss in a CI log. `pr-validation.yml`'s comment states this. Verify by reading that comment.
- [x] 4.4 Correct the same command in `.ansible-lint:3`, `.gitignore:28` and `ansible/requirements.yml:6`, per `design.md` Decision 9. The replacement differs by file, because the comments have different subjects: `ansible/requirements.yml:6` heads a manifest pinning four collections **and** one role, so it takes both commands; `.ansible-lint:3` and `.gitignore:28` are each explaining why one installed *role directory* is excluded or ignored, so they take the role command alone. Change the command only — every one of those comments explains something true about why installed content is excluded, ignored or pinned, and that prose stays. Verify by diff that each file's change is confined to the command.
- [x] 4.5 **The trap in 4.4**: verify with the literal string, `grep -rn "ansible-galaxy install -r ansible/requirements.yml" . --exclude-dir=openspec`, which must return nothing once the four copies are fixed. A looser pattern such as `'ansible-galaxy install -r'` also matches `.github/workflows/pr-validation.yml:232` — which quotes `ansible-galaxy install -r ... -p ...` as a **deliberate negative example**, explaining that `-p` is silently ignored for collections. That comment must survive: task 4.3 depends on it, and it is the reason the two-command split is not "simplified" back. It also matches `docs/change-queue.md:295`, whose copy is prose about the defect and is deleted at 8.5 rather than here. Verify by confirming `pr-validation.yml:232` is unchanged in the diff.
- [x] 4.6 Replace "the eight scenarios this repository owns" — both occurrences — with the durable form from `design.md` Decision 2: "every scenario this repository owns", plus `git ls-files 'ansible/roles/*/molecule/*/molecule.yml'`. State that it runs from the repository root; the neighbouring commands `cd` into a role directory.
- [x] 4.7 **The trap in 4.6**: do not write the `find` form with a `-not -path '*geerlingguy.docker*'` exclusion. It hardcodes into the README the one fact `ansible/requirements.yml` owns, which is this change's own defect class, and `.github/tests/test_ci_configuration.py`'s `galaxy_role_directories` derives that set from the manifest specifically so a hardcoded list fails. Verify the chosen command by running it, confirming it lists twelve files, excludes `ansible/roles/geerlingguy.docker/`, and contains no role name.
- [x] 4.8 Keep the paragraph warning against globbing `ansible/roles/*/molecule/*/`. It explains why the naive glob is wrong, which is still worth knowing — but it is no longer implemented by an exclusion clause, so it must read as the reason the command selects committed files rather than as a caveat on the command given. Verify the digest-pin prose, the bare-digest rationale and the `DOCKER_CONFIG` block are otherwise unchanged, by diff.
- [x] 4.9 Replace the parenthetical naming which roles carry more than one scenario with the same command. Its point — that `-s default` skips most of the suite — must survive. Verify the edited sentence still makes that point without naming a role.
- [x] 4.10 Leave `README.md:99-111`, the paragraph on the Ansible checks and the registered context, unchanged. It is accurate, and `design.md` Decision 5 says why a second registration claim is not added. Verify by diff.

## 5. Environment variables, CI/CD, Testing, Status

- [x] 5.1 Correct the `terraform.tfvars` parenthetical. It lists labels, which are not in the file, and omits the SSH public key, the volume settings and the enable flags. Verify against the file, and confirm the only `labels` block under `terraform/environments/prod/` is in `ssh_key.tf`.
- [x] 5.2 **The trap in 5.1**: this parenthetical is a copy of a table row in *Version Control Excludes State and Secrets* (`openspec/specs/iac-repo-foundations/spec.md`), which carries the same wrong entry. `design.md` Decision 1 chose to correct the README and record the divergence rather than open a delta. Verify the requirement is left untouched, and that task 6.2 records it.
- [x] 5.3 Leave the Read & Write token paragraph verbatim. *Write Credentials Confined to the Gated Pipeline* in `openspec/specs/iac-safety-hardening/spec.md` obliges the README to carry it. Verify by diff, and by re-reading that requirement.
- [x] 5.4 Rewrite CI/CD as one entry per workflow, per `design.md` Decision 4: `pr-validation.yml`, `ansible-verify.yml`, `apply.yml`, `platform-deploy.yml`, `drift.yml`, `pre-commit-autoupdate.yml`. Each names its trigger and what it does. Verify against `ls .github/workflows/` and confirm the correspondence is one-to-one in both directions.
- [x] 5.5 In the pull-request entry, distinguish the two checks that run on **every** pull request — the CI-configuration suite and `gitleaks` — from the ones conditioned on changed paths. The current bullet already names `gitleaks`; what it does not do is say that a documentation-only pull request runs anything, which is the fact a contributor needs. Verify each check's condition against `pr-validation.yml` step by step, rather than assuming the split.
- [x] 5.6 State which contexts gate a merge — `validate` and `ansible-verify` — and point at `gh api repos/:owner/:repo/branches/main/protection`. Do not restate that they are registered; that claim already lives at L101-107 and `design.md` Decision 5 says why once is the right number.
- [x] 5.7 Rewrite Testing to name the three test commands and each one's test-path glob, per `design.md` Decision 6, pointing at `AGENTS.md` for row selection. Delete the "as `terraform/modules/` grows past `terraform/modules/server`" conditional: `terraform/modules/server/tests/` holds five `.tftest.hcl` files and `terraform/modules/volume/tests/` three, so the named module refutes it. Verify each glob by running its command's discovery, and verify the `.github/tests` count against task 1.2's baseline.
- [x] 5.8 **The trap in 5.7**: this section is where a citation is most tempting. `.github/tests/test_ci_configuration.py`'s citation check walks every committed file outside `openspec/`, and `README.md` is one of its four anchors. Every reference must be an `openspec/specs/<capability>/spec.md` path plus a requirement name, or a change named in prose with no path. Verify by `grep -n 'openspec/changes' README.md` returning nothing, and by running the suite.
- [x] 5.9 Rewrite Status: bootstrapping is done, `prod` is provisioned and configured by `ansible/playbooks/host-baseline.yml`, `platform/`'s stack runs on it, twenty-five changes are archived. Keep the staging paragraph. Verify the archive count by `ls openspec/changes/archive | wc -l`.
  - **Deviated, knowingly.** The count was verified (25) and then *not written*: Status says "every change since is recorded under `openspec/`" instead. A literal number in a sentence nothing checks is the rot class Decision 2 rejects, and this one increments with every archive. Recorded rather than left as a silently unmet task.
- [x] 5.10 **The trap in 5.9**: do not name the applications deployed onto the host. `design.md` Decision 7 — which applications exist is not this repository's fact, and it changes with no commit here, which is the exact failure this change is correcting. Name the mechanism instead. Verify by reading the section back.
- [x] 5.11 Delete the sentence claiming `ansible/` and `platform/` hold structure and convention only. Six roles, twelve scenarios and a 607-line Compose stack contradict it.

## 6. Queue

- [x] 6.1 Add an entry for the enforcement this change declines, per `design.md` Decision 8: a `.github/tests` assertion that the README's region agrees with `terraform/environments/prod/terraform.tfvars` and its scenario count agrees with the tree. Record that it needs a requirement of its own, that its scope is a real question — whether the workflow list is also checked, and what that costs — and that it is not blocked on anything.
- [x] 6.2 Add an entry for the specification parenthetical, per `design.md` Decision 1: *Version Control Excludes State and Secrets* describes `terraform.tfvars` as holding labels, which it does not. Record that it is a `MODIFIED` delta on an illustrative list, that this change corrected the README's copy and left this one, and that it is small enough to batch with any other change touching that capability.
- [x] 6.3 Write both to the file's own conventions: a numbered heading, an explicit blocked / not-blocked line with the reason, and prose rather than a bullet list. 6.1's entry is meant to be opened as a change and takes a `## <n>. <kebab-case-change-name>` heading; 6.2's is a correction to batch into whatever change next touches that capability, not a change to open, so it takes a sentence heading — entry 9 is the precedent for that form in this file. Verify by reading both precedents and matching their shape.
- [x] 6.4 Record nothing else. Eleven sections of a stale file will surface more; `AGENTS.md` is one change at a time, and the queue holds identified changes rather than observations. The two entries above are carved out because `design.md` decided them, not because they were noticed.

## 7. Verify and review

- [x] 7.1 Run `python3 -m unittest discover --start-directory .github/tests` from the repository root. Verify green, and that the count matches task 1.2's baseline — a documentation change that moves the test count has done something it did not intend.
- [x] 7.2 Run `pre-commit run --all-files`. Verify green, or that any failure is pre-existing and unrelated — establish that by re-running on a clean checkout of trunk rather than by judgement. Note that 4.4 edits `.ansible-lint` and `.gitignore`, so this run is not a formality.
- [x] 7.3 Run `openspec validate refresh-readme-accuracy`. Verify it passes. Do **not** run `openspec validate --archived` as a gate: three archived changes are red for reasons that predate this change, recorded as `docs/change-queue.md` entry 12.
- [x] 7.4 Read the whole diff against the proposal's *What this must not undo* section, item by item. Verify each of the five is untouched, by diff rather than by recollection.
- [x] 7.5 Re-read `README.md` end to end as a first-time reader, not as a diff. Verify no correction introduced a claim about repository settings, live infrastructure, or an application, that nothing in the repository can check.
- [x] 7.6 Dispatch `ai-toolkit:change-code-reviewer` over the diff. Fix and re-review where the fixes were substantial; bounded at three rounds.

## 8. Ship

- [x] 8.1 Suggest a commit of the corrected `README.md`, the three comment corrections, and the two queue entries. Do not commit without confirmation.
- [x] 8.2 Merge the freshly fetched trunk into the branch and re-run 7.1 before opening the pull request — `strict: true` means the branch must be up to date with `main` to merge, and both required contexts re-run after each trunk update.
- [x] 8.3 Open the pull request. Wait for the operator's confirmation that continuous integration passed and that it merged. Do not infer a merge from a green pull request.
- [x] 8.4 **Confirm the effect.** The observation is performable and is not waivable. On the merged `main`: `grep -n 'fsn1' README.md` returns nothing and the region line names `hel1`; `grep -rn "ansible-galaxy install -r ansible/requirements.yml" . --exclude-dir=openspec` returns nothing, with `.github/workflows/pr-validation.yml:232` — a deliberate negative example quoting the `-p` form — still present and unchanged; the local-setup Galaxy commands, copy-pasted into a fresh clone, leave `geerlingguy.docker` inside `ansible/roles/` and a subsequent `molecule create -s default` in one role resolves the dependency; the CI/CD section corresponds one-to-one with `ls .github/workflows/`. Propose these to the operator and wait for confirmation.
  - **Performed and confirmed 2026-09-08**, against a fresh clone of merged `main` (`48eef02`) — tracked files only, which is the state a new reader gets. `fsn1` absent; `location = "hel1"` present and attributed; the defective command gone from all four files with `pr-validation.yml:232`'s negative example intact; the two Galaxy commands run verbatim landed `geerlingguy.docker` in `ansible/roles/`, ignored per `.gitignore:30`, and `ansible-playbook --syntax-check` then resolved `host-baseline.yml`; CI/CD one-to-one with `.github/workflows/`; the layout list equal to its own stated command's output. Resolution was proved by syntax-check rather than `molecule create`, per `docs/change-queue.md` entry 8 — a colliding run from a concurrent working tree can pass against another session's container. Operator confirmed. Not a waiver: the gate was performable and was performed.
- [x] 8.5 Archive: bring the branch to the freshly fetched trunk, delete `docs/change-queue.md` entry 9 **and** the intro bullet naming `refresh-readme-accuracy` as an opened change, commit the specification record, and open the change's second pull request. Confirm before deleting entry 9 that section 4 actually delivered it in all four files.

## Branch and working tree removed

Task 8.6 asked for the branch and working tree to be removed, and both were:
verified 2026-09-08 by `make-openspec-validation-a-usable-gate` —
`git ls-remote --heads origin` returns `refs/heads/main` alone, no
`refresh-readme-accuracy` branch exists locally, and no such working tree is
registered.

The task is recorded here in prose rather than as a checkbox, and this is **not**
a disclosure of unperformed work: it was performed. It could never have been
ticked, because removal happens after the record's own pull request merges,
which is after the commit that writes this file. A checkbox for it is unticked
by construction and stays that way forever — which is exactly what
`openspec validate --archived` reported, and the only reason this change was red.

The rule that follows from it now lives in `AGENTS.md`'s project conventions: a
change's `tasks.md` ends at the archive commit. This is the only archived change
that put a post-archive step in its task list, so the rule is written narrowly —
the archive step itself belongs in `tasks.md` and is unaffected.
