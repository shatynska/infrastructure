## 1. Derive the tests

- [x] 1.1 Dispatch `ai-toolkit:change-test-writer` with the `.github/tests` row of `AGENTS.md`'s Testing table — test command `python3 -m unittest discover --start-directory .github/tests` run from the repository root, test-path glob `.github/tests/*.py` — this change's `specs/iac-cicd-pipeline/spec.md` as the delta under test, and `openspec/specs/` as the specs root. No scenario in the delta belongs to the Terraform or Molecule rows; say so in the dispatch rather than leaving the author to infer it. Verify a `test-plan.md` exists in this change's directory mapping every scenario to a test or recording why it is uncovered.
- [x] 1.2 Name, in that same dispatch, the scenarios **no test command in this project can reach**, so the author records them rather than reporting a gap or writing a test that implies more than it checked. They are properties of repository settings or of merge outcomes, and `.github/tests` may make no network call: *Direct push to main is rejected*, *Pull request with failing checks cannot merge*, *Every registered context names a literal job* in its registration half, and the merge-outcome clauses of *Documentation-only pull request remains mergeable* and *A failing Molecule scenario blocks the merge*. Each is covered by the ship-stage observation in task 8.5, not by a test. Their **workflow-file** halves — no path filter, a literal job name, a gate that discriminates — are reachable and are what the tests assert. Verify `test-plan.md` records each of these five by name with that disposition.
- [x] 1.3 Require every new test whose subject touches branch protection to say in its docstring what it did **not** establish. The delta obliges this directly: the suite "SHALL NOT be written so as to imply it has established more than that". Verify by reading each new docstring and confirming none can be read as asserting that a context is registered.
- [x] 1.4 Require the gate's derived test to iterate **every** row of design Decision 4's table, not a subset. The table is seven rows: four refusals — discovery did not conclude, skipped-yet-changed, failed-or-cancelled-with-changes, failed-or-cancelled-without — and three passes: succeeded-with-changes, skipped-without-changes, succeeded-without-changes. Verify `test-plan.md` maps each delta scenario to an assertion that **runs** the extracted gate script rather than greps it, and that the test's own case list has seven entries rather than however many scenarios happen to name a row.
- [x] 1.5 Require a **second** extracted-script test, over the change-detection resolution step, which is a different script from the gate and is where the manual-run polarity lives. Its cases are the event name: `pull_request` yields the filter's output unchanged, and any other event — `workflow_dispatch` at least — yields `true`. Delta scenario *A manual run verifies the whole suite* maps here, not to the gate's table, where "Ansible changed" arrives as a given. Verify `test-plan.md` distinguishes the two scripts by name rather than saying "the extracted script".
- [x] 1.6 Confirm the test author recorded obsolete-test entries for the REMOVED requirement. Three existing assertions have a subject that moves and are the implementer's to reconcile, not the test author's to edit — section 4 does that. Verify `test-plan.md` names at least `TestMoleculeDiscoveryAndScenarioCoverage.test_the_workflow_uses_no_continue_on_error` (its docstring cites the dropped scenario), `TestVerificationJobsCarryNoCredential.test_the_molecule_workflow_declares_no_environment` (docstring cites the advisory tier) and `TestRequiredCheckIsNotPathFiltered` (its subject is now a set of workflows, not one).
- [x] 1.7 Record the baseline: run the suite before any implementation and confirm the new tests fail because the workflow is still filtered and carries no aggregating job, rather than by error or by import failure. Verify by capturing the failure output in `test-plan.md`.

## 2. Reshape the workflow trigger and the matrix gate

- [x] 2.1 Remove the workflow-level `paths:` filter from `.github/workflows/ansible-verify.yml`'s `pull_request` trigger and add `branches: [main]`, matching `pr-validation.yml`. Leave `workflow_dispatch: {}` and the `concurrency` block untouched. Verify `TestRequiredCheckIsNotPathFiltered` — once section 4 has widened it — passes over both workflows.
- [x] 2.2 Add a `dorny/paths-filter` step to the discovery job with a single `ansible: - 'ansible/**'` filter. Take the version **from `pr-validation.yml`'s existing usage** rather than choosing one — the argument for reusing the action is the argument for reusing its pin, and two pins of the same action are two things to update. Verify by grepping both workflows and confirming the reference is identical.
- [x] 2.3 Give the filter step `if: github.event_name == 'pull_request'`. It resolves a pull request's changed files and has no diff to resolve against on any other event; conditioning it makes 2.5's polarity trap structurally unreachable rather than merely handled, and keeps the borrowed shape inside the trigger `pr-validation.yml` has actually exercised it on — that workflow runs on `pull_request` only, so its usage says nothing about a dispatch. Verify by dispatching the workflow after merge (task 8.6) rather than by reasoning about it.
- [x] 2.4 Declare a job-level `permissions:` block on the discovery job carrying **both** `contents: read` and `pull-requests: read`. A job-level block **replaces** the workflow-level one rather than adding to it — `pr-validation.yml`'s `validate` job redeclares `contents: read` for exactly this reason — so declaring only `pull-requests: read` would strip the checkout's own scope and fail the newly-required check on every pull request. `pull-requests: read` is what the filter's API read needs, and *Least-Privilege Workflow Permissions* obliges the job to declare what it uses. Put it on the job, not the workflow: the matrix job and the aggregating job need neither. Verify no job gains a write scope, that the discovery job's checkout still resolves, and that `TestVerificationJobsCarryNoCredential.test_the_molecule_workflow_consumes_no_secret` still passes.
- [x] 2.5 Add the step that resolves the gate for both triggers: on `pull_request` it takes the filter's output; on any other event it is `true`, because a manual dispatch is asking for the whole suite. **The polarity is the trap** — defaulting a dispatch to `false` skips the matrix and reports green on the exact trigger used to run the suite against the trunk. Expose the result as a job output alongside `roles`. Pass `github.event_name` in through `env:` so the script body stays free of `${{ }}`; this is the **second** extractable script, and its own test is task 1.5's, not the gate's. Verify by reading the job's `outputs:` block back and confirming both keys are declared.
- [x] 2.6 Leave the **role-discovery** step itself unconditional and byte-identical, per design Decision 2 — including its `${{ }}`-free body, which `test_role_discovery_fails_when_it_finds_nothing` executes against a scratch tree. 2.3's `if:` belongs to the filter step, not this one, and the job carries none either. Verify that test still passes, and that this step and its job are both free of `if:`.
- [x] 2.7 Condition the Molecule matrix job on the change-detection output. Verify a job-level `if:` is present and that no step inside it gained one — the gate belongs to the job, and a step-level condition would leave the job green having run nothing.

## 3. Add the aggregating job

- [x] 3.1 Add a job whose `name:` is the literal `ansible-verify`, with `needs: [<discovery>, <matrix>]` and `if: always()`. This is the job registered as a required context; nothing else in the workflow is. Verify the name contains no `${{`.
- [x] 3.2 Write its single step's gate. Its three inputs — the discovery job's result, the matrix job's result, and the change-detection output — arrive through the step's `env:` block so the `run:` body stays free of `${{ }}` and can be executed standalone, per design Decision 5. Implement design Decision 4's table exactly: check the discovery result **first** and fail if it is not `success`, then accept `success`; accept `skipped` only where nothing under `ansible/` changed; fail everything else — `failure` and `cancelled` included, and regardless of what change detection said — with a distinct message for the skipped-yet-changed case naming it as a green that verified nothing. Verify by running the extracted script by hand across all seven rows before the test does, and confirm the derived test from 1.4 covers the same seven rather than a subset.
- [x] 3.3 Confirm no `continue-on-error:` was introduced anywhere in the file, and that the gate reports by exiting non-zero rather than by annotating. Verify by grepping the file.
- [x] 3.4 Rewrite the file's top comment. It currently states the workflow is advisory, why, and what promotion would require — all three are now wrong or done. Replace with what a reader needs: that this workflow is a required check, that the gating is inside the discovery job because a workflow-level filter would leave non-matching pull requests permanently pending, that the aggregating job exists because a matrix job's context name is generated, and that a skipped matrix is not a success. Cite `openspec/specs/iac-cicd-pipeline/spec.md` and the requirement names — never a path under `openspec/changes/`, per `AGENTS.md`.
- [x] 3.5 Rename the workflow from `Ansible Verify (advisory)` to `Ansible Verify`. Verify this changes no status check context: contexts are job names, and no job name changes.
- [x] 3.6 **The trap:** `test_the_workflow_names_no_role_literally` fails if the workflow's text contains any role directory name as a whole word — and the roles carrying scenarios include one whose name is an ordinary English word that comment prose reaches for naturally. Verify after every edit to this file, not only at the end, that the test still passes.

## 4. Reconcile the existing suite with the moved subject

The test author adds tests and never edits one. These three exist, their subject has moved, and rewriting them is implementation work.

- [x] 4.1 Widen `TestRequiredCheckIsNotPathFiltered` from `pr-validation.yml` to every workflow behind a registered required context, driven by a module-level constant naming them, so that adding a third required check means adding a name rather than a test. Verify it fails when either workflow is given a `paths:` filter, by adding one to each in turn and removing it again.
- [x] 4.2 Update `TestMoleculeDiscoveryAndScenarioCoverage.test_the_workflow_uses_no_continue_on_error`. The assertion is unchanged and stays; its docstring cites the dropped scenario *A failing Molecule scenario does not block a merge* and its reasoning inverts — `continue-on-error` no longer merely destroys a signal, it reports a green required check for a failed suite. Verify the citation names a requirement that exists after archive.
- [x] 4.3 Update the two sites that actually carry the stale wording — `TestVerificationJobsCarryNoCredential.test_the_molecule_workflow_declares_no_environment`'s docstring ("same scenario, advisory tier") and `test_the_workflow_uses_no_continue_on_error`'s docstring and failure message. `TestAnsibleBlockingTier` carries no such wording; what it does carry is the tier **label**, and the delta renames *Blocking tier* to *Lint tier* because both tiers now block. Decide whether the class and its `test_the_blocking_tier_runs_*` methods keep names the specification no longer uses; renaming them is optional, leaving them is a citation naming a label that is gone. The assertions themselves are unchanged either way. Verify no docstring in `.github/tests/` still says "advisory", by grepping.
- [x] 4.4 Confirm no assertion was weakened, deleted or skipped in the course of 4.1–4.3. Verify by diffing the test file and reading every changed line, and by confirming the suite's test count did not fall.

## 5. Update the documentation the change makes wrong

- [x] 5.1 Update `README.md`'s local-setup step 5. It describes the suite as running "in a separate `Ansible Verify (advisory)` workflow that is deliberately **not** a required check", and that a red run "is information, not a blocked merge". Replace with what is true, and keep the paragraph's actual advice — running the suite locally before opening a pull request — which promotion makes more useful rather than less. Verify by reading the whole step back for a second stale statement.
- [x] 5.2 Check `AGENTS.md`'s project conventions for a statement the promotion falsifies. The Testing section names `ansible-verify.yml` as what runs the Molecule suite in CI, which stays true; verify by reading rather than assuming, and leave it alone if it is accurate.
- [x] 5.3 Sweep the whole tree rather than the three sites known in advance. The requirement is renamed, and `AGENTS.md`'s citation rule obliges a committed file to name a requirement by its own name — so the rename can invalidate a citation anywhere, and the executable citation check tests path form only, not requirement names. Grep on the stable **prefix** `Ansible Configuration Is Verified`, not the full name: the test file abbreviates it to "Ansible Configuration Is Verified in CI" in six places, which a search for the full name misses entirely. Grep the tree, excluding `openspec/changes/archive/`, for that prefix and for "advisory" in a Molecule context, and reconcile whatever turns up. Verify by re-running both greps and reading each surviving hit to confirm it is inside an archived change, where it is a correct record of what was true then.

## 6. Record what this change does not do

- [x] 6.1 Delete `docs/change-queue.md` entry 4 — it is this change — at archive, not before. Verify the deletion happens in the archiving commit alongside the specification record.
- [x] 6.2 Add a `docs/change-queue.md` entry for a Molecule matrix over **scenarios** rather than roles, per design Decision 8. Carry what entry 4's first paragraph establishes, because that paragraph goes with entry 4: `molecule test --all` stops at the first failing scenario and silently skips the rest, `--continue-on-failure` is unavailable without `--workers`, and `--workers > 1` requires collection mode these plain roles are not in. Record what the promotion changes about its priority — a red required check that under-reports is slower to diagnose, not weaker — and that it would parallelise the longest role. Verify the entry stands alone without entry 4 present.
- [x] 6.3 Verify no other queue entry was silently invalidated by this change, in particular entry 8, whose subject is local concurrent runs and which continuous integration does not touch.

## 7. Verify

- [x] 7.1 Run `python3 -m unittest discover --start-directory .github/tests` from the repository root and confirm every test passes, including the pre-existing ones and the ones section 4 rewrote.
- [x] 7.2 Run `pre-commit run --all-files` and confirm every hook passes. Note that `actionlint` is **not** among them — `docs/change-queue.md` entry 6 records that nothing installs it — so the workflow's syntax is verified by the suite parsing it and by the run itself, not by a linter.
- [x] 7.3 Confirm the working tree was provisioned before any verification result above was claimed: the pinned Galaxy role installed to `ansible/roles/` and the `pre-commit` hooks installed. Report each check as not run, and why, if it was not.
- [x] 7.4 Do not run the Molecule suite locally for this change. It touches no role, no scenario and no playbook; `docs/change-queue.md` entry 8 records that a local run shares container names, ephemeral directories and cache with any other working tree on this machine. The suite's behaviour under this change is observed on the pull request, which is the subject of section 8. State this as a deliberate exemption rather than reporting the suite as passing.

## 8. Review, ship and confirm

- [x] 8.1 Run `ai-toolkit:change-code-reviewer` over the change's diff once 7.1 and 7.2 pass, and fix what it finds.
- [x] 8.2 Open the pull request. It changes nothing under `ansible/`, so it is itself the first observation of the doc-only path: the matrix must be skipped, no container may start, and `ansible-verify` must conclude success. Read the run's job list rather than the workflow's conclusion.
- [x] 8.3 Wait for the operator's confirmation that the pull request merged and that no deploy was triggered — this change touches no Terraform path, so `apply.yml`'s filter should not fire. Do not infer the merge from a green check.
- [x] 8.4 Ask the operator to add `ansible-verify` to `main`'s required status check contexts, leaving `validate`, `strict: true` and `enforce_admins: true` in place. This is repository settings and cannot be delivered by this change; the workflow must be on `main` first, or the context has never reported and cannot be selected. Provide the exact resulting contexts list. Tell the operator, in the same message, what they will see immediately afterwards: any pull request already open shows `ansible-verify` as expected-but-never-reported, because registering a context does not produce one on a head commit that predates it. `strict: true` forces an update-branch before such a pull request can merge, and that push produces the run. It resolves itself; it reads exactly like the permanent-pending failure this change exists to prevent, which is why it is said in advance rather than diagnosed afterwards.
- [x] 8.5 **Confirm the effect, in two parts.** Both are needed: the throwaway pull request cannot observe direct-push rejection or which contexts are registered, and the API read cannot observe a merge being blocked.
  - Read `repos/<owner>/<repo>/branches/main/protection` directly — **unconditionally, not as a fallback**. This is what covers *Direct push to main is rejected* and the registration half of *Every registered context names a literal job*, which task 1.2 dispositions here rather than to a test. Confirm both contexts, `strict`, `enforce_admins`, `allow_force_pushes` and `allow_deletions`.
  - Propose to the operator: open a throwaway pull request that changes one file under `ansible/` in a way that makes a scenario fail, confirm it is blocked from merging with `ansible-verify` red, then close it without merging. This is what covers *A failing Molecule scenario blocks the merge* and *Pull request with failing checks cannot merge*. If the operator declines it, say plainly which two scenarios remain unobserved rather than reporting the gate as confirmed. Wait for the operator's confirmation. Do not waive this gate — an observation is performable here, which is exactly what the waiver classes exclude.
- [x] 8.6 Dispatch the workflow manually on `main` once it is merged, and read the run. This is the one path no test reaches: task 1.5's test exercises the extracted resolution script, not the action, and `pr-validation.yml` — whose shape this borrows — runs on `pull_request` only, so nothing in this repository has ever observed `dorny/paths-filter` on a diffless event. Confirm the filter step was skipped by its `if:`, the matrix ran in full, and `ansible-verify` concluded success. A red discovery job here means 2.3's condition is wrong or missing, and the gate correctly failed closed on it.
- [x] 8.7 Archive: bring the branch back to the freshly fetched trunk, apply the delta to `openspec/specs/iac-cicd-pipeline/spec.md`, and open the record's own pull request. Four requirements move — one replaced, three modified, the third of which is *Gated Production Apply Applies the Reviewed Plan*, whose only edit is a cross-reference that stops being singular. Update that capability's `## Purpose` paragraph too: it describes "an advisory Molecule suite", which no delta reaches because it is not a requirement. Verify the archived specification contains the new requirement name, no longer contains the dropped scenario, and no longer contains the old requirement name outside `openspec/changes/archive/`.
