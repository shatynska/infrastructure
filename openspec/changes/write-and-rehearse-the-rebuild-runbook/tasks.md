## 1. Record what surfaced, and derive the tests

- [x] 1.1 Add a `docs/backlog.md` entry for the shared-instance maintenance window being unable to announce a rebuild — the declaration file lives on the host the rebuild destroys, so an application's probe reads nothing rather than `window-open`. Verify the entry names the file path, says why the rebuild case escapes the mechanism, and cites no change directory path.
- [x] 1.2 Record the static suite's green baseline on the merge base, by running `python3 -m unittest discover --start-directory .github/tests` from the repository root at that commit and noting the count and that it passed. Verify the number recorded is a green run's, not a count taken mid-change.
- [x] 1.3 Commit the approved plan, then dispatch `ai-toolkit:change-test-writer` against this change with the static row of this project's three test commands — `python3 -m unittest discover --start-directory .github/tests`, test-path glob `.github/tests/*.py` — and with `docs/runbook-rebuild.md` named as the document the assertions read. Verify it produces a `test-plan.md` mapping each of the delta's scenarios to an assertion or recording why that scenario is not statically checkable, that the tests it writes fail against the tree as it stands, and record in `test-plan.md` the count of assertions derived, which task 5.1 reads.

## 2. Write the runbook

- [x] 2.1 Create `docs/runbook-rebuild.md` with the ordered phases from destroyed server to serving host, `main-staging` as the worked example, and production's divergences named inline at the steps where they differ — its reviewer-gated Environment, its applicable `destroy_policy_gate` and the `destroy-override` label the merged pull request must carry, its `delete_protection` and `backups`, and `commerce-ops`'s own unbacked PostgreSQL. Verify every phase heading is followed by a `Credential:` line naming the credential and where it is held, or `Credential: none`.
- [x] 2.2 Write out verbatim for `main-staging` the commands that belong to the sequence itself: the two `terraform.tfvars` merges, the tailnet-peer deletion, the workstation converge, the converge-key install, the two Environment address updates, the `platform-deploy.yml` dispatch with `stack=main-staging`, the prune activation, and the trigger for the application's own deploy. Verify each command's literal values against the file that declares each.
- [x] 2.2a Carry the two recipes the sequence *invokes* as citations rather than copies: the database re-provisioning block, citing `docs/onboard-an-application.md` with the `rotate=yes` assignment named and why a rebuild needs it; and **both** of `platform/README.md`'s manual steps — the `pgexporter` monitoring role and the dead-man's-switch registration — citing that document. What stays in the runbook is what is genuinely sequence: that each is owed, at which point, and with which secret. Verify no fenced block in the runbook carries `CREATE DATABASE`, and verify both manual steps are named, since the count is what makes an omission visible.
- [x] 2.3 Write the "before you destroy anything" section: what is lost and does not come back on this stack, what is lost on production and is unbacked, which alarms the rebuild will raise and for roughly how long, and the pre-state capture that makes "serving what it served before" checkable afterwards. Verify it derives the store classification from *No Store on This Host Holds Data Requiring Backup* rather than restating the table, and that `main-staging-alertmanager` and `main-staging-prune-host-images` are both named as expected-red with the reason each goes red.
- [x] 2.4 Write the observer phase: the checks owed once the host is back, including re-reading each of this host's checks against its intended period and grace, which a check re-created by its own first ping carries wrongly. Verify the phase names the heartbeat observer as the place these steps are performed, names this stack's checks individually, and **cites** `docs/bootstrap-a-new-host.md` Appendix A's register rather than copying its values — `README.md` states they are listed once there, and a second copy would falsify that and go stale.
- [x] 2.5 Write the phase-by-phase failure notes: the stale tailnet peer, the **invalidated `known_hosts` entry** that Appendix B calls the one that bites, the expired or burnt auth key, a converge that wedges partway, a platform deploy landing before the converge, and the re-provisioning block's refusal when `rotate` is wrong. Verify each names the symptom as it presents, not only the cause, and that the `known_hosts` note survives the deletion of the paragraph it comes from.
- [x] 2.6 Add the `## Rehearsal record` section in its **never rehearsed** state — a `Last rehearsed: never` line and no `Partial run:` block — and document the three states and the labelled lines each carries, so a later editor moves the record between states rather than inventing a fourth. Verify the derived assertions pass against that state: the document must be mergeable before it has been rehearsed.

## 3. Reduce the second account, so the two cannot drift

- [x] 3.1 Replace `docs/bootstrap-a-new-host.md`'s Appendix B body with a pointer to `docs/runbook-rebuild.md`, keeping only what the bootstrap document alone can say — that a rebuild's first converge is a workstation converge, and why. Verify no ordered sequence of stages remains in the appendix.
- [x] 3.2 **Correct** Appendix B's claim about staging's data volume rather than deleting it: scope it to the route it holds of, since the volume's `count` turns on the toggles and not the server's identity. Check every other statement the appendix made that the runbook now carries, rather than assuming that one was the only wrong one, by reading each surviving sentence against the file that declares what it asserts.
  - **Performed as a relocation rather than as an in-place correction, which deviates from this task as written.** The scoped, corrected claim is in the runbook's phase 3, where it says the volume goes with the server on the toggle route and that the replace route is unobserved; what stands in Appendix B is the reason a second account was removed. The `known_hosts` half survives in the runbook's phase 7, as task 2.5 required. Recorded because the task asked for a correction and received a move, and a reader comparing the two would otherwise see a deletion.
- [x] 3.2a Record the per-stack Alertmanager checks' period and grace in `docs/bootstrap-a-new-host.md` Appendix A, as a **second** register block addressed by `PLATFORM_DEADMANSWITCH_URL`, taking the values from §7.1, which already states them. Not as rows in the ping key's table: that table is introduced as "the checks **it** addresses", and the secret-inventory row above it states the ping key addresses **four** checks and that "a ping-key rotation touches none of them". Rewrite the existing "Not in this table" paragraph as the pointer to the new block, and reduce §7.1's statement of the settings to a citation of it while keeping its creation step. Verify the ping key's count still reads four, that its rotation sentence is still true, that no check appears in both blocks, and that §7.1 and the register do not both state the values.
- [x] 3.2b Have the register name the requirement that governs it — *A Host's Rebuild Procedure Is Recorded, and Its Rehearsal State With It* (`openspec/specs/iac-server-lifecycle/spec.md`) — so an editor adding a check later finds the obligation where they are standing rather than in a capability they have no reason to open, which is how these checks came to sit outside the register. Then make `README.md`'s "listed once" claim true, which it is not today: the paragraph sits in `### When a periodic job stops working`, whose opening sentence enumerates the three periodic jobs, while the Alertmanager checks are fed by the Watchdog every two minutes and are not periodic jobs at all. Either name the new block there as covering the stacks' continuous dead-man's-switch checks, saying they are not periodic jobs, or place the pointer just outside that section. Verify README's claim is true of both blocks afterwards, **and** that the section's own opening sentence is still true of what it now contains.
- [x] 3.3 Update every other site that names Appendix B's sequence — `docs/bootstrap-a-new-host.md` §6.4, §7.4 and its opening section, `platform/README.md`, and the comment on `platform-deploy.yml`'s `workflow_dispatch` — so each names the runbook instead. Verify with a repository-wide search for "Appendix B" that the sites found are the ones `proposal.md`'s Impact names, and that any further site is added there rather than fixed silently. Verify separately that `README.md`'s "listed once" claim about Appendix A is left true.

## 4. Implement against the derived tests

- [x] 4.1 Run the derived module and make the document satisfy it. Verify with `python3 -m unittest discover --start-directory .github/tests` from the repository root that every previously failing assertion now passes, and that none was made to pass by weakening it.
- [x] 4.2 Check each derived assertion really catches what it claims, by breaking its property in the working tree, reading the failure, and restoring. Verify the restore is exact — do this against committed content, not an uncommitted tree.
  - Done against the committed tree at `c27a2f3`, four breaks, each firing exactly one failure and each restored from the commit: a phase's `Credential:` line deleted; a credential named with no holder (`the DNS provider account.`); `Last rehearsed: never` blanked to `Last rehearsed:`; and a check's name changed in Appendix A's new table. The 51 detector-fires cases cover the matchers; these four cover the property assertions that read committed files.

## 5. Verify and ship the document

- [x] 5.1 Run the verification relevant to what changed — `python3 -m unittest discover --start-directory .github/tests`, `openspec validate --all`, and `pre-commit run --all-files` — and report each result. Verify the static suite's count exceeds the green baseline recorded in task 1.2 by the number of assertions derived.
- [x] 5.2 Commit the implementation, dispatch `ai-toolkit:change-code-reviewer` over the committed diff, and act on its findings. Verify the review's verdict permits proceeding.
  - Three rounds, the bound this project sets, ending **APPROVED**. Eighteen findings applied: twelve from the reviewer and six from a `code-review` skill run it had forked, four of which reached this session only when they were restated on request — they had sat undelivered in the reviewer's transcript while the file they concerned was being edited. Two of the reviewer's own calls were corrected against evidence: a `terraform output` block it reported as replaced was not, and finding A, which it downgraded to a documentation contradiction on the strength of an archived record that `ls ~/.ssh/` shows to be eleven days stale. The review's two closing notes are applied and its backlog recommendation is `guard-the-converge-dispatch-to-the-default-branch`.
- [x] 5.3 Open the runbook's pull request, let continuous integration run, and wait for the operator's confirmation that it merged. Verify from the pull request's own state, not from branch ancestry.

## 6. Rehearse, on `main-staging`, following the merged text

- [x] 6.1 Capture the pre-state the runbook's own section calls for — what is serving, the container set and their health, the active scrape targets, the public IPv4, the volume id, the tailnet name, and **each of this stack's heartbeat checks with its current period and grace, read at the observer** — and record it in the change's own artifacts. Compare that reading against the register task 3.2a wrote: the register is a claim about the observer's configuration until someone has actually looked, and this is the occasion the comparison is owed. Verify `commerce-ops.main-staging.fincci.bike` answers before anything is destroyed.
  - The observer read was performed **by the operator at ~19:45Z, before the destroy**, and reported back — this session holds no credential for that service. It found the prune's grace at 2 hours against the recorded 2 days, the production Alertmanager check's at 2 minutes against 5, and `main-staging-alertmanager` absent altogether. Recorded in `rehearsal-log.md` under phase 1, with the reason its timing is what makes it usable. Everything else in this task was performed by this session.
- [x] 6.2 Confirm the credentials the sequence needs are in hand before it starts: the staging Vault password, a Tailscale auth key that is neither expired nor burnt, and workstation access to both this repository and the application's. Verify each by using it, not by believing it exists.
- [x] 6.3 Open and merge the `server_enabled = false` pull request for `main-staging`. Record the **whole** plan summary from the pull request's plan comment — not only the three resources expected — and verify it destroys the server, its firewall and the volume together while leaving `hcloud_ssh_key.this` alone, which is not count-gated. That output is the first live observation of a coupling nothing has exercised, and it is an observation about the toggle route only.
- [x] 6.4 Open and merge the `server_enabled = true` pull request. Verify the apply created the server and the volume, and record the new public IPv4 and the new volume id.
- [x] 6.5 Perform the remaining phases in the runbook's order, timing each: DNS, the stale tailnet peer, the workstation converge, key-expiry and the converge key, the two address updates, the platform deploy dispatch, both manual steps from `platform/README.md`, the observer phase, the prune activation, the database re-provisioning by `docs/onboard-an-application.md`'s recipe with `rotate=yes`, and the application's own deploy. Verify each phase by the check the runbook gives for it, and record the wall clock and every divergence as each phase completes rather than at the end.
- [x] 6.6 Confirm the host is serving what it served before: `commerce-ops.main-staging.fincci.bike` answers over TLS on a reissued certificate, eight `platform-*` containers are healthy, all five scrape targets are up, and both of the stack's heartbeat checks are green on the period and grace the register records — a check re-created by its own first ping carries the observer's default instead, which is the failure this step exists to catch. Verify each and record which, if any, could not be reached.
  - The heartbeat half was confirmed by the operator at ~21:20Z: both checks green — the prune **on the settings the pre-destroy read recorded**, the Alertmanager check on settings **not yet read**, since it did not exist at that reading. See `rehearsal-log.md`'s phase 12, which keeps the two apart for that reason. The prune reporter's `OK` rather than `Created` at 21:10 is the host-side evidence for its half: a re-created check would have carried the observer's default.

## 7. Fold the rehearsal into the document

- [x] 7.1 Correct every step the rehearsal found wrong, missing or out of order in `docs/runbook-rebuild.md` itself. Verify no correction is left as a note beside the step rather than applied to it.
- [x] 7.2 Move `## Rehearsal record` into whichever state the run reached — **rehearsed**, with `Last rehearsed:` carrying the date beside `Duration:` and `Stack:`, the per-phase durations and what was corrected, and no `Partial run:` block; or **partially rehearsed**, keeping `Last rehearsed: never` and adding the `Partial run:` block with its `Reached:`, `Wall clock:` and `Stopped by:` lines. Verify the derived state assertions pass against real values, and that a partial run carries no rehearsal date.
- [x] 7.3 Add a dated note to `docs/backlog.md` `exercise-the-volume-server-coupling-against-live-state` recording that staging's coupling was observed live on the rehearsal date **via the `server_enabled` toggle route**, that the replace route remains unobserved, and that production's coupling remains unexercised. Verify the entry is not deleted — it is about production's coupling, which this did not exercise.
- [x] 7.4 Where the observer's settings turned out to differ from what the register claims, correct the register against what task 6.1 read — the observer is the authority on its own configuration, and the register is a claim about it until compared. Verify the corrected values against that reading, and where they already agreed, record that they did rather than leaving the comparison unreported.
  - Corrected against the **pre-destroy** reading, which is the direction the runbook's own phase 12 permits: a disagreement found before anything is destroyed is between two claims about a running system, where one found afterwards is most likely the rebuild having reset a check. Both tables had drifted, both in the same direction — the observer held the tighter value in every case.
- [x] 7.5 Where the rehearsal could not complete a step, disclose it under `## Not performed` with a `Reason:` line, rather than ticking it or deleting it. Verify the `Reason:` label is present and non-empty.
- [ ] 7.6 Re-run the verification, commit, dispatch the code reviewer over the committed diff, and open the rehearsal pull request. Verify the operator confirms it merged.

## 8. Archive

- [ ] 8.1 Bring the branch back to the freshly fetched trunk, run `openspec archive write-and-rehearse-the-rebuild-runbook`, `git add -A`, and delete this change's `docs/backlog.md` entry. Verify `openspec validate --archived` passes and the archived record's task list records no outstanding work that is neither ticked nor disclosed.
- [ ] 8.2 Open the record's own pull request and wait for the operator's confirmation that it merged. Verify from the pull request's state.

---

**After the archive record's pull request merges**, and once every other pull request this change opened has merged and nothing uncommitted or unpushed remains, the branch is removed locally and on the remote and the working tree is removed from the repository's main checkout. Those acts happen after the commit that writes this file, so they cannot be tasks in it and are recorded here in prose instead. Read each merge from the pull request's own state rather than from branch ancestry.

The Molecule namespace this change took is not reclaimed by any of that; this change runs no Molecule scenario, so it leaves no container and no loop association behind. `docker ps` and `losetup -a` are what answer that, rather than this sentence.

## Not performed

- 6.1, in part — `main-staging-alertmanager`'s period and grace at the observer were not read.
  Reason: at the pre-destroy read that check did not exist. Both hosts were feeding production's check, which is the defect `give-staging-its-own-dead-mans-switch-check` was opened and closed on the same evening; staging's was created afterwards by its own first ping and so carries the observer's default until someone sets it. The rest of 6.1 was performed. Appendix A's row for that check is labelled a claim rather than a record, and the action is `docs/backlog.md` `set-staging-alertmanager-to-its-register-values`.
