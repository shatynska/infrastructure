## 1. Tests

- [x] 1.1 Record the baseline: `python3 -m unittest discover --start-directory .github/tests` from this working tree's root, with `.github/requirements-ci.txt`'s pins installed. Verify it is green and note the count.
  - 1125 tests, OK, on 2026-09-13, PyYAML 6.0.1 and pre-commit 4.6.2 installed.
- [x] 1.2 Dispatch the independent test author once the plan review permits proceeding and the plan is committed, before any file below is edited, with **all three rows** of `AGENTS.md`'s test table, no verdict about which apply, and design.md decision 9's statement of what is and is not verifiable. Verify by the author's report: new tests and a `test-plan.md`, or a stated gap with its reason.
  - Plan review: FIX REQUIRED in rounds 1 and 2, CONDITIONALLY APPROVED in round 3 with two `[MINOR]` conditions, both applied; plan committed as `2315199`. The author placed 60 tests in `.github/tests/test_the_shared_instance_has_its_first_database.py` and wrote `test-plan.md`: neither Terraform nor Molecule has a subject here. Suite after: 1185 tests, 9 failures, all in the new module and each red on the implementation tasks 2.1 and 3.1 owe; the 1125 existing tests pass. Two scenarios are reported uncovered and three partly, with reasons, in `test-plan.md`.

## 2. The recipe — `docs/bootstrap-a-new-host.md`

- [x] 2.1 Rewrite stage 8.3's recipe to design.md decision 5's block: a `set -eu` subshell; the secret-name check as a refusal unless `rotate=yes`; the password generated with `openssl rand -hex 32` and passed to `gh secret set` and to `psql` only over stdin; `ON_ERROR_STOP`; the four logging settings pinned for the session; the converging `\gset`/`\if` script with double-quoted identifiers; `CONNECT` and `TEMPORARY` revoked from `PUBLIC`. Say that the target's Environment from 8.4 must exist first; that the block is pasted whole, as it stands, once per deploy target, with an independently generated password; that a rotation is the same block with `rotate=yes` in its own assignment line — never a variable set in the shell, which would outlive the paste and disable the name check for the next one — picked up by the application on its next deploy; and what each failure in decision 5's list leaves and how to recover. Verify by reading it back against every bullet of decision 5, and by comparing the committed block with decision 5's line by line.
- [x] 2.2 Replace stage 8.3's deferral sentence with what is now true: the trigger fired on 2026-09-13 with `commerce-ops`, provisioning is manual and the obligation unmet, the mechanism is `docs/backlog.md` `automate-per-application-database-provisioning`. Cite the requirement by name. Verify the citation form against `AGENTS.md`.
- [x] 2.3 Qualify stage 8.3's closing `commerce-ops` paragraph to the production host, say its non-durable half now has a database in production's shared instance awaiting that application's cutover, and keep the paragraph. Verify it no longer reads as describing staging.
- [x] 2.4 Stage 8.4's secrets table: replace the `POSTGRES_PASSWORD` row with the shared-instance password under that target's own value from 8.3, named as 8.3 names it, note that it must not reuse a name the Environment already holds, and note that this row is created by 8.3's recipe rather than by hand in 8.4. Verify it cannot be read as one password for every target, or as reusing `POSTGRES_PASSWORD`.
- [x] 2.5 Appendix B: after a rebuild, re-run 8.3 for each application with a database in the shared instance, before that application's deploy (design.md decision 8). Verify by reading the step order.
  - 2.1–2.5 read back against design.md decisions 5 and 8: the committed block matches decision 5's line for line, the four failure cases and rotation are stated in prose beside it, and Appendix B places the re-provisioning before each application's deploy. The eight derived recipe tests that were red on the committed recipe pass against it; 8.3 carries the only fenced block in the document containing `CREATE DATABASE`, which those tests require. The recipe is not yet run against a host — that is task 4.1.

## 3. Backlog and the sweep

- [x] 3.1 Add `docs/backlog.md` entry `automate-per-application-database-provisioning`: the mechanism and credential path owed since 2026-09-13, why it was not built with one consumer, the secret-name collision decision 5 found as a first data point for it, and that archiving it obliges replacing the divergence paragraph in *Single Shared PostgreSQL Instance, Per-Application Databases*. Verify it says what it waits on or why it is free.
- [x] 3.2 Update `move-commerce-ops-durable-data-to-supabase` with the table division from design.md decision 3, the database and the `SHARED_POSTGRES_PASSWORD` secret that now exist for the queue to point at, the three-step ending, and that the step here is gated on `docker ps` and `docker volume ls` rather than on the migration being reported done. Verify the 2026-09-08 figures are kept as dated.
- [x] 3.3 Update `expose-staging-on-the-web`: its dependency on this change is satisfied; the staging deploy path in the application's repository remains. Verify nothing in it now reads as the database still outstanding.
- [x] 3.4 Add the re-provisioning step to `write-and-rehearse-the-rebuild-runbook`, and a sentence to `upgrade-the-shared-postgres-major` that a volume reset obliges the same. Verify against design.md decision 8.
- [x] 3.5 Run `grep -rn 'commerce-ops' --include='*.md' .` outside `openspec/changes/`, read every hit that describes where that application's data lives, and correct each this change makes false or ambiguous outside `openspec/specs/` — qualify to the production host, delete nothing about the private PostgreSQL while it runs. Hits inside `openspec/specs/` other than the requirement this change's delta modifies are left as they are, for the reason in design.md decision 7's last paragraph. Verify by re-running the grep and giving each data-location hit a disposition in this task's notes, naming every file this task edits.
  - Files edited by 3.1–3.5: `docs/backlog.md` and `docs/bootstrap-a-new-host.md`, nothing else.
  - Dispositions, from `grep -n 'commerce-ops'` over every committed `*.md` outside `openspec/changes/` on 2026-09-13. **Describing where its data lives, and edited:** `docs/backlog.md` `upgrade-the-shared-postgres-major`'s "the divergence this does not cover" and `move-commerce-ops-durable-data-to-supabase`'s opening paragraph, both qualified to the production host; that entry's resolution rewritten (3.2); `write-and-rehearse-the-rebuild-runbook`'s gap sentence, qualified (3.4); `expose-staging-on-the-web`'s outstanding-work sentence (3.3); `docs/bootstrap-a-new-host.md` stage 8.3's closing paragraph (2.3) and Appendix B's rebuild paragraph (2.5). **Describing where its data lives, and left:** `openspec/specs/iac-safety-hardening/spec.md`, the divergence paragraph and the scenario "The stated divergence is not a precedent" — dated 2026-09-08 and describing the host that runs the private container, for design.md decision 7's reason. **Not about where its data lives:** `docs/bootstrap-a-new-host.md`'s DNS table row, stage 8's worked-example sentence, 8.4's workflow item and secrets row; `docs/backlog.md`'s deploy-verification entry, `expose-staging-on-the-web`'s deploy-authorisation paragraph, the staging-banner entry, and entries `record-how-an-application-is-onboarded`, `revoke-an-application-s-deploy-authorisation`, `say-what-the-host-firewall-actually-gates` and `automate-per-application-database-provisioning`; `ansible/roles/docker/README.md`'s log-option sentence.

## 4. Staging provisioning

- [x] 4.1 **Staging, run by the operator** by the recipe as written in 2.1, once `commerce-ops` has a `staging` Environment; until then this task and everything after it is blocked, and the status line says so. Verify from a session on `100.85.219.36`: `\l` shows `commerce-ops` owned by `commerce-ops`; its `datacl` grants nothing to `PUBLIC`; `SELECT has_database_privilege('pgexporter', 'commerce-ops', 'CONNECT')` returns `f`; `docker logs --since <run start> platform-postgres-1 2>&1 | grep -ciE 'create role|alter role'` returns `0`; and `gh secret list --repo fuperia-it/commerce-ops --env staging` lists `SHARED_POSTGRES_PASSWORD`.
  - The operator created `commerce-ops`'s `staging` Environment and ran the recipe from the workstation at 2026-09-13T22:13:44Z, as committed in `e42b14b` with one deviation: `ssh -i ~/.ssh/shatynska-ops`, the workstation having no `staging` alias. Output: `gh`'s `Set Actions secret SHARED_POSTGRES_PASSWORD`, `SET` ×4, `CREATE ROLE`, `CREATE DATABASE`, `REVOKE`.
  - Verified from `ops-claude@100.85.219.36` the same evening: `pg_database` shows `commerce-ops` owned by `commerce-ops` with `datacl` `{"\"commerce-ops\"=CTc/\"commerce-ops\""}`, no `PUBLIC` entry; `has_database_privilege('pgexporter', 'commerce-ops', …)` is `f` for both `CONNECT` and `TEMPORARY`; role `commerce-ops` can log in and is not a superuser; `docker logs --since 2026-09-13T22:13:44Z platform-postgres-1` has zero lines, so zero matching `create role|alter role`; `docker ps -a --filter name=commerce-ops` lists nothing; `gh secret list --env staging` lists `SHARED_POSTGRES_PASSWORD`, set 22:13:46Z.
  - **A recipe defect found while preparing the run, and fixed after it.** The committed block was indented two spaces inside stage 8.3's list, so a copy from the raw file carried the indentation onto the closing `SQL`, which then never terminates the here-document and the paste hangs. The operator was given a column-0 copy; the block is now at column 0 in the document, with a sentence that `host` can be an `ssh` alias carrying a non-default key, and the output a successful run prints. The recipe's content is unchanged, so this run still verifies it.
- [x] 4.2 Record on production, dated and citing the commands: whether `commerce-ops-postgres-1` runs and whether `commerce-ops_commerce_ops_pgdata` exists. Read-only.
  - 2026-09-13, from `ssh prod`: `docker ps --format '{{.Names}}\t{{.Image}}\t{{.Status}}'` lists `commerce-ops-postgres-1  postgres:16-alpine  Up 4 days (healthy)`, beside `commerce-ops-app-1` and `commerce-ops-worker-1`; `docker volume ls` lists `commerce-ops_commerce_ops_pgdata`. The private PostgreSQL runs and its volume exists.

## 5. Verification

- [x] 5.1 `openspec validate provision-commerce-ops-database-in-the-shared-instance --strict`. Verify it passes.
- [x] 5.2 `python3 -m unittest discover --start-directory .github/tests`. Verify green, and the count is 1.1's plus the derived tests.
- [x] 5.3 `pre-commit run --all-files`. Verify it passes or each skip has no files.
- [x] 5.4 `git diff --name-only` against the merge base and `git status --porcelain`. Verify only `openspec/`, `docs/`, `.github/tests/` and the files 3.5's notes name appear.
  - Section 5, run 2026-09-13 from this working tree before the implementation commit. 5.1: valid. 5.2: 1185 tests, OK — 1.1's 1125 plus the 60 derived. 5.3: every hook passed (Terraform fmt, tflint, validate, gitleaks, ansible-lint, `ansible-playbook --syntax-check`). The first run failed the syntax check on `geerlingguy.docker` not found, because this working tree had never installed its Galaxy roles; after `ansible-galaxy role install -r ansible/requirements.yml -p ansible/roles` (README's local setup) it passed, and the role directory is git-ignored. 5.4: against `origin/main`, the committed diff is `.github/tests/test_the_shared_instance_has_its_first_database.py` and this change's own directory; uncommitted are `docs/backlog.md`, `docs/bootstrap-a-new-host.md` and this `tasks.md`. Nothing under `terraform/`, `ansible/`, `platform/` or `.github/workflows/`.

## 6. Ship

- [x] 6.1 Commit, then dispatch the code-review gate over the committed diff. If fixes change the recipe, re-run 4.1 by the changed recipe and section 5 before 6.2.
  - **4.1 re-run by the final recipe (`23f3311`), as a rotation, `rotate=yes`.** The operator's first attempt at 2026-09-14T03:34:25Z replaced the secret (03:35:01Z) and then failed at `ssh: connect to host 100.85.219.36 port 22: Connection timed out`, before `psql` ran: the case design.md decision 5 lists as a host step failing on a rotation, which left the Environment ahead of the role, harmless with nothing deployed on staging. Cause, read rather than assumed: the workstation's Windows Tailscale was signed into another tailnet (`fuperia.co.uk`, 0 peers), while from production `tailscale status` listed `main-staging` online and `tailscale ping 100.85.219.36` answered, and the workstation's own node in the hosts' tailnet was offline for about four hours. The operator added the hosts' tailnet as a second Tailscale profile and re-ran the same block: `Set Actions secret SHARED_POSTGRES_PASSWORD`, `SET` ×4, `ALTER ROLE`, `REVOKE`; secret updated 03:53:03Z. Verified from `ops-claude@100.85.219.36`: `commerce-ops` owned by `commerce-ops`, `datacl` `{"\"commerce-ops\"=CTc/\"commerce-ops\""}`; `pgexporter` has neither `CONNECT` nor `TEMPORARY`; the role can log in and is not a superuser; `docker logs --since 2026-09-14T03:34:25Z platform-postgres-1` has zero lines; no `commerce-ops` container. Section 5 was re-run on `23f3311`: 1210 tests OK, validate valid, every pre-commit hook passed.
  - **Round 1**, over `origin/main...431f101`: no blocking finding; one medium, three low, three nits, all applied. (1, medium) The derived tests accepted an indented here-document terminator, so the defect `431f101` fixed would have passed all 60 — `heredoc_openers` now terminates as bash does, and four detectors with fixtures were added, marked `ADDED AT CODE REVIEW` and recorded in `test-plan.md`. (2) The name check read only the Environment's secrets, while an Environment secret overrides a repository or organisation secret of the same name — the recipe now also reads the repository's and those the organisation shares with it, each in its own assignment so `set -e` sees a failed listing; re-run with `gh`, `ssh` and `openssl` stubbed over ten cases, each listing failing, the name at each level, rotation, and a stale shell variable, all as design.md decision 5 states. Both listings return successfully and empty for `fuperia-it/commerce-ops`, read on 2026-09-13. (3) Text dated production's provisioning to 2026-09-13 before it happened — the delta's divergence paragraph, `upgrade-the-shared-postgres-major` and `automate-per-application-database-provisioning` now date staging only, and 6.2 re-reads them against production's run date. (4) A quoted delimiter, a dropped log setting and `rotate` read from the shell passed every test — covered by (1)'s detectors. (5–7) Stage 8.3's prose after the column-0 fence de-indented so it renders as it reads; "first line" corrected to the assignment line; the re-paste advice after a failed `gh secret set` now covers the secret having been set after all. Suite after: 1198 tests, OK; validate valid; every pre-commit hook passed.
  - **The recipe changed**, so 4.1 is owed again by the changed recipe before 6.2: staging's secret now exists, so that run is a rotation, `rotate=yes`.
  - **Round 2**, over `origin/main...d238870` (a first dispatch ended on an API rate limit with no findings and was run again): no blocking finding; two low-medium, two low, three nits, all applied. (S, low-medium) `rotate=yes` switched the whole name check off, so a name held by the repository or organisation, or any Environment name, passed — in exactly the rebuild and recovery runs where the docs prescribe `rotate=yes`. The check now refuses a repository or organisation name outright, requires `rotate=yes` for a name in the Environment, and refuses `rotate=yes` for a name not in it; the stubbed control-flow run was re-run over thirteen cases, each listing failing, the name at each level with and without `rotate`, a failed `gh secret set` and a stale shell variable, all as design.md decision 5 states. (1, low-medium) Round 1's re-paste advice told the operator to use `rotate=yes` after a failed listing, when a refusal there means the name is taken — rewritten per failure. (2) No test protected the repository and organisation listings — `listing_offences` and its fixtures added. (3) `test-plan.md` called the helper change purely stricter; it is not, and now says what it loosened and why no author test's result changed. (4) Nothing asserts the two recipe copies equal — decided against a test, which would bind the living document to an archived design, and recorded in `test-plan.md`; checked identical, 35 lines each. (5) design.md's failure list named one listing of three. (6) Production's provisioning written in a tense true only after 6.2 — 6.2's re-read widened from dates to every statement of whether production is provisioned. Also found while applying (S): for a repository owned by a personal account GitHub answers the organisation listing with `404`, measured against `shatynska/shatynska-infrastructure` on 2026-09-14, so the block stops having changed nothing; stage 8.3 now says to make that line `org_names=` there. Suite after: 1203 tests, OK; validate valid; every pre-commit hook passed.
  - **Round 3**, over `origin/main...182f8a1` — the last round this project's bound allows: no blocking finding; one medium, one low-medium, four low, one nit, all applied. (1, medium) The name comparison was case-sensitive while GitHub's secret names are not, so `secret=postgres_password` would pass and overwrite `POSTGRES_PASSWORD` — both comparisons are now `grep -qixF`. (2, low-medium) With three refusal branches, one exit or one `rotate` test anywhere satisfied the detectors, so a branch could be removed, inverted or made to exit 0 unseen — `refusal_branch_offences` asserts each branch and the case-insensitive match, with one falsifying fixture per mutation. (3) Stage 8.4 said "the last three rows" take per-target values of a table this change grew to five rows, which read as excluding `DEPLOY_HOST` — now "every row after the first". (4) Backlog entry 19's new staging sentence made "that database" read as staging's — now "production's private database". (5) The advice after a failed listing was wrong for a `rotate=yes` paste and said "unchanged" beside the `org_names=` edit — now "act on what the refusal says". (6) A `404` on the organisation listing also answers a token without access — the `org_names=` workaround now applies only when `gh api repos/<org>/<app> --jq .owner.type` prints `User`. (7) "must not already be used" was false for a rotation — qualified to a first provisioning. Stubbed control flow re-run over sixteen cases, adding lower-case names against GitHub's upper-case listing, all as design.md decision 5 states; the two recipe copies identical, 35 lines each. Suite after: 1210 tests, OK; validate valid; every pre-commit hook passed.
  - **Mutation evidence for the review-added tests**, run on a `git archive` export of `23f3311`, the change's module only: unmodified, 85 tests OK; each of fifteen mutations of stage 8.3's recipe — the block indented under its list, the closing `SQL` indented, the delimiter quoted, a log setting removed, `rotate=no` removed, the repository and organisation listings removed, the organisation listing not fed to the check, two listings merged into one substitution, the repository check conditioned on `rotate`, that check folded into the Environment's, the Environment guard a no-op, the guard inverted, the repository refusal exiting 0, and a case-sensitive comparison — fails at least one test, and in each case the test written for it.
  - **The loop ended at its bound by reporting, not by a clean pass.** Every round found something new in the recipe's name check and none was blocking; all findings of all three rounds are applied. Asked whether to dispatch a fourth round, the operator chose on 2026-09-14 to proceed without one: staging is re-run by the final recipe, then production, then the pull request, where CI and the operator's own review still apply.
- [ ] 6.2 **Production, run by the operator** by the recipe as reviewed, only after 6.1 has cleared, with a password generated independently of staging's, into `commerce-ops`'s `production` Environment under `SHARED_POSTGRES_PASSWORD` — confirming first that `POSTGRES_PASSWORD` is untouched. Verify read-only from `ssh prod` with 4.1's host checks, and `gh secret list --env production` showing both names with `POSTGRES_PASSWORD`'s date unchanged from 2026-08-21. Before 6.3, re-read every statement of when or whether production is provisioned — the delta's divergence paragraph, `docs/backlog.md` `upgrade-the-shared-postgres-major`, `move-commerce-ops-durable-data-to-supabase` step 1 and `automate-per-application-database-provisioning`, and stage 8.3's closing production paragraph — against production's actual run and its date, and correct any the run falsifies. Each is written in the tense true once 6.2 has run, which is before the pull request.
- [ ] 6.3 Open the pull request; wait for the operator to confirm the merge.
- [ ] 6.4 Before staging's `commerce-ops` deploy key is handed over, ask the operator to confirm that what the application's staging deploy renders declares no PostgreSQL service and reads its database password from `SHARED_POSTGRES_PASSWORD`; only then tell the operator the key is released. Verify by the operator's confirmation of both the precondition and the handover, or record which has not happened yet.
- [ ] 6.5 Propose the observation and wait for the operator's confirmation: after staging's first `commerce-ops` deploy, on the staging host, `docker ps --filter name=commerce-ops` shows the application's containers and no PostgreSQL container, `docker volume ls` shows no new volume, and `pg_stat_activity` in `platform-postgres-1` shows a session by `commerce-ops` on database `commerce-ops`. Production's effect is observed by 6.2's checks. Record the confirmation here.
- [ ] 6.6 Re-run 4.2's check. If both are gone, stop and re-enter `plan` per design.md decision 7. Otherwise record the divergence paragraph's deletion under `## Not performed`, naming `move-commerce-ops-durable-data-to-supabase` as where the remainder lives.
- [ ] 6.7 Archive: bring the branch to the freshly fetched trunk, archive the change, and open its record's pull request.
