Verification commands referenced below, from this project's conventions:

- `python3 -m unittest discover --start-directory .github/tests`, from the repository root
- `ansible/scripts/run-molecule test --all`, from a role directory — read the SCENARIO RECAP, not the exit code
- `pre-commit run --all-files`, which carries `ansible-lint` and `ansible-playbook --syntax-check`

Tasks 9 and 10 are the operator's, on their own workstation, against credentials
that exist nowhere in this repository. Everything before them is repository work
that lands and passes before staging is touched.

## 1. Inventory: one source per environment

- [ ] 1.1 Rename `ansible/inventory/hcloud.yml` to `ansible/inventory/prod.hcloud.yml` with `git mv`, and set its `api_token` to `"{{ lookup('ansible.builtin.env', 'HCLOUD_TOKEN_PROD') }}"`. Rewrite the header comment: the block currently explaining that this file sees one project and that whoever gives the repository a second source owns the quiet-failure problem is now describing solved work, and is replaced by what the file *is* — prod's source, reaching prod's project, named for the plugin's `verify_file` suffix rule. Commit the move **before** rewriting the comment: roughly thirty of the file's thirty-seven lines are that comment, and rename detection is a similarity heuristic that will not see a rename across both edits at once. Verify `pre-commit run --all-files` still passes.
- [ ] 1.2 Add `ansible/inventory/staging.hcloud.yml`, identical but for `HCLOUD_TOKEN_STAGING`. Verify the plugin accepts the path at all, before any staging credential exists: `HCLOUD_TOKEN_STAGING=invalid ansible-inventory -i inventory/staging.hcloud.yml --graph` from `ansible/` SHALL emit a line naming `hetzner.hcloud.hcloud` — meaning the plugin was reached and the token rejected, which is the outcome wanted here. Key the check on the **presence** of that line and nothing else: a generic "Unable to parse … as an inventory source" appears alongside it for the same source, so treating that generic line as the failure signal would read a correct outcome as a broken filename. A name the `verify_file` suffix rule rejects produces the generic message *alone*, with no plugin ever named, and that absence is the unambiguous signal.
- [ ] 1.3 Remove the `inventory =` line from `ansible/ansible.cfg`, leaving the rest of `[defaults]` unchanged. Verify from `ansible/` that `ansible-playbook --syntax-check playbooks/host-baseline.yml -e target_environment=syntax-check-only` warns "No inventory was parsed" and exits 0.
- [ ] 1.3a Add `any_unparsed_is_failed = True` under `[inventory]` in `ansible/ansible.cfg`, with a comment naming the setting it is **not**: `unparsed_is_failed`, one word away, fires when *no* source parsed at all and would fail the no-inventory `--syntax-check` path 1.3 just established. Verify both halves, because the two read as synonyms and are not — `HCLOUD_TOKEN_STAGING=invalid ansible-inventory -i inventory/staging.hcloud.yml --graph` from `ansible/` exits **non-zero** naming the source it could not parse, where before this task the same command exits 0 with an empty inventory; and 1.3's syntax check still exits 0. Without this task the delta spec's *An inventory source cannot authenticate* scenario is false, and the guard play reports a bad credential as a destroyed server.
- [ ] 1.4 Add `ansible/.envrc.example` documenting `HCLOUD_TOKEN_PROD` and `HCLOUD_TOKEN_STAGING`, saying that both are Read Only tokens, that direnv loads the nearest `.envrc` and not the parent's, and that Terraform's own `HCLOUD_TOKEN` is a different variable scoped to the environment directory it is planned from. Verify `git check-ignore -v ansible/.envrc` names the existing `.envrc` rule in `.gitignore` and that `ansible/.envrc.example` is not ignored.

## 2. The play: a named target and a guard that refuses

- [ ] 2.1 In `ansible/playbooks/host-baseline.yml`, add a first play `hosts: localhost`, `connection: local`, `gather_facts: false`, `become: false`, carrying two separate `assert` tasks — one that `target_environment` is defined and is a non-empty string, one that `groups[target_environment] | default([])` is non-empty — each with a `fail_msg` naming what was wrong, the environment involved, and that a play matching no host would otherwise exit 0. Two tasks rather than one compound assertion, so the message says which of the two failed.
- [ ] 2.2 Change the baseline play's `hosts: prod` to `hosts: "{{ target_environment }}"`, with no `default()` filter, and update its header comment: it currently says "the prod server" and cites a single environment. Keep every existing role-ordering comment intact — the `swap`-last paragraph and its reference to the play-scope gap are unchanged by this task and are re-examined in 8.1, not here.
- [ ] 2.3 Verify the guard behaves, and record the output in this task's completion note. Both invocations are run from `ansible/` and supply **no `-i` at all**, relying on implicit localhost: `ansible-playbook playbooks/host-baseline.yml -e target_environment=staging` exits non-zero at the first play with the empty-group message, and the same command with no `-e` exits non-zero with the undefined-input message; neither reaches a role. Do **not** use `-i localhost,` — `ansible/ansible.cfg`'s `enable_plugins` names only `hetzner.hcloud.hcloud` and `auto`, which replaces Ansible's default list rather than extending it, so `host_list` is not enabled and a comma-terminated source is one nothing can parse. Under task 1.3a that is now a fatal parse error, and the run dies before play 1 — the guard reads as broken when the harness is. This is the manual verification design.md Decision 10 names as having no automated home; the empty-group path under a source that *did* parse is verified separately in 9.1a, which needs a credential this task does not have.

## 3. Static tooling that compiles the play without running it

- [ ] 3.1 Add `extra_vars: {target_environment: syntax-check-only}` to `.ansible-lint`, with a comment stating that `syntax-check` is an `unskippable` rule, that it fails on a templated `hosts:` with nothing supplied, and that the value names no environment on purpose. Verify `pre-commit run ansible-lint --all-files` passes and fails without the entry.
- [ ] 3.2 Add `-e target_environment=syntax-check-only` to the `ansible-playbook-syntax-check` hook's `entry` in `.pre-commit-config.yaml`, with the same reasoning in its existing comment block. Verify `pre-commit run ansible-playbook-syntax-check --all-files` passes.

## 4. `ansible/inventory/group_vars/staging.yml`

- [ ] 4.1 Write the file from scratch — not copied from prod's and edited. `ansible_user: root`; `hardening_ssh_allowed_cidrs` mirroring `terraform/environments/staging/terraform.tfvars`'s `ssh_allowed_cidrs` exactly; `hardening_web_allowed_cidrs: []` mirroring its `web_allowed_cidrs = []`, stated rather than omitted because the mirror obligation in `ansible/roles/hardening/README.md` is what makes the pair checkable; `ops_user_accounts` with the operator's own entry; `platform_data_volume_subdirs` as prod's, because staging's volume is `main-data` too and `platform/docker-compose.yml` is unparameterised across environments by design. Verify `pre-commit run ansible-lint --all-files` passes over it.
- [ ] 4.2 Add `deploy_apps` with a single `platform` entry carrying **staging's own** deploy public key (task 9.3), and a comment saying why it is not prod's and why `commerce-ops` is absent — design.md Decision 7. Verify the file parses: `ansible localhost -m debug -a 'msg={{ deploy_apps | length }}' -e @inventory/group_vars/staging.yml` prints 1.
- [ ] 4.3 Add `ghcr_pull_username` in plain text and `ghcr_pull_token` Vault-encrypted under the `staging` vault id (task 9.2), and `image_prune_heartbeat_ping_key` likewise. Verify each decrypts without revealing it: `ansible localhost -m debug -a 'msg={{ <variable> | length }}' -e @inventory/group_vars/staging.yml --vault-id staging@prompt` prints a length, and confirm `gitleaks` in `pre-commit run --all-files` is clean.

## 5. Refusal diagnostics that name the environment

- [ ] 5.1 Confirm the two Molecule scenarios currently assert the message being replaced, before changing anything: `ansible/roles/hardening/molecule/absent-ssh-cidrs/verify.yml` and `ansible/roles/image_prune/molecule/absent-heartbeat-key/verify.yml` both assert `'group_vars/prod.yml' in ...failed_msg`. Record that they pass on the current tree — a test that passes before and after establishes nothing.
- [ ] 5.2 Change the `fail_msg` in `ansible/roles/hardening/tasks/main.yml`, `ansible/roles/deploy_user/tasks/main.yml` and both assertions in `ansible/roles/image_prune/tasks/main.yml` to name `ansible/inventory/group_vars/<environment>.yml`, and `platform_data_volume`'s to name `terraform/environments/<environment>/terraform.tfvars`. Change nothing else in any of the five.
- [ ] 5.3 Update the two scenarios' assertions to the new literal exactly — `ansible/inventory/group_vars/<environment>.yml`, not a loosened `group_vars/` substring, which would pass against the message 5.2 replaces. Verify each scenario fails against the pre-5.2 message and passes after it: `ansible/scripts/run-molecule test -s absent-ssh-cidrs` from `ansible/roles/hardening`, and `-s absent-heartbeat-key` from `ansible/roles/image_prune`.
- [ ] 5.4 Sweep the surrounding prose that names prod's file as "where this is set". The files that carry such a reference, confirmed by grep rather than assumed: `hardening/defaults/main.yml`, `hardening/README.md`, `image_prune/defaults/main.yml`, `image_prune/README.md`, `docker/defaults/main.yml`, `docker/meta/main.yml`, `docker/README.md`, and — for the `terraform/environments/prod/` form — `platform_data_volume/README.md` and `hardening/README.md` again. `deploy_user/defaults/main.yml` carries none, despite being the obvious place to look. Leave alone the references genuinely about prod's own file: `deploy_user/molecule/default/verify.yml`'s note on the inline `!vault` form, and the `image_prune` Molecule converge fixtures that cite it as the shape they imitate. Verify by re-running the grep for both `group_vars/prod` and `environments/prod` under `ansible/` and reading every remaining hit.

## 6. Tests

- [ ] 6.1 Land the derived `.github/tests/` assertions for the static properties design.md Decision 10 enumerates: one inventory source per directory under `terraform/environments/`, no two sources naming the same credential variable, no source relying on the plugin's bare `HCLOUD_TOKEN` fallback, the baseline play's `hosts:` not a literal environment name, its first play being a `localhost` guard asserting group non-emptiness, `ansible/ansible.cfg` declaring no default inventory **and setting `any_unparsed_is_failed`** (task 1.3a — the assertion is what keeps a later edit from quietly restoring the empty-environment behaviour, and it should name `unparsed_is_failed` as the wrong neighbour so a reader does not "fix" it into breaking the syntax-check path), and the lint sentinel naming no environment this repository has. Verify `python3 -m unittest discover --start-directory .github/tests` passes from the repository root, and that each new assertion fails against the pre-change tree.
- [ ] 6.2 Run every role's Molecule suite that this change touched, reading the SCENARIO RECAP rather than the exit code: `hardening`, `deploy_user`, `image_prune`, `platform_data_volume`. Confirm the recap names every scenario each role has — `--all` stops at the first failure and silently skips everything sorting after it.

## 7. Documentation

- [ ] 7.1 Rewrite `docs/bootstrap-a-new-host.md`'s "From here on, one host" section. Two of its three bullets are delivered by this change and are removed; the third — `platform-deploy.yml`'s `environment: production` and its single `PLATFORM_DEPLOY_HOST` — stands, and the section's framing changes from "everything from stage 5 to stage 9 configures one of them" to naming what the second host now gets and what it still does not. Keep the "if you decide you do not want it yet" paragraph, updating what deleting staging now also means.
- [ ] 7.2 Rework stage 6 into a stage run once per environment: the inventory check (§6.2) and the playbook run (§6.3) take `-i inventory/<environment>.hcloud.yml` and `-e target_environment=<environment>`, §6.1 names `group_vars/<environment>.yml` and the per-environment vault id, and §6.4's four ad-hoc `ansible prod -m …` commands take `-i`. Add staging's own prerequisites — its tailnet auth key, its deploy keypair, its Vault password — where prod's are listed. Verify every command in the stage is one that has actually been run in tasks 2.3, 9 or 10, or is marked as not yet exercised.
- [ ] 7.3 Update stage 4.3's "It needs no `known_hosts` entry, because nothing converges it" — staging now needs one, at the same moment prod's is recorded. Re-point §4.4's DNS paragraph, which says staging gets its hostnames from "the change that deploys something to it": after 8.3 that is the web-exposure entry, not entry 52, which carries only the deploy mechanism. Sequence this after 8.3 so the entry has a number to cite.
- [ ] 7.4 Update Appendix A's secret inventory with staging's four — Vault password, tailnet auth key, `platform` deploy keypair, and the GHCR and heartbeat values encrypted into `group_vars/staging.yml` — and give `staging-server-prune-host-images` its period and grace beside prod's check. Update stage 9's closing-the-gates checklist for anything now true of two hosts.
- [ ] 7.5 Update `.envrc.example`'s header, which currently says "The same variable also feeds Ansible's dynamic inventory, which sees only its token's project — and a play matching no host exits 0." Both halves are false after this change. Point it at `ansible/.envrc.example`.
- [ ] 7.6 Update the two places that describe the token layout and now describe it incompletely: stage 1.2's token table, whose "Your workstation (the repo-root `.envrc`)" row no longer covers what Ansible reads, and §4.1's token-scoping paragraph, which explains the directory trick for Terraform only. Both are upstream of stage 6 and are read before it.
- [ ] 7.7 Update `README.md`'s local-setup section — including its two `ansible -i inventory/hcloud.yml prod` examples, a path this change deletes — and `AGENTS.md`'s "Host configuration and platform stack" Inventory bullet, which names `ansible/inventory/hcloud.yml` as this project's convention. Verify by following the README section end to end in a shell, and by grepping for `inventory/hcloud.yml` across the tree.

## 8. Records

- [ ] 8.1 Revisit `docs/deferred-work.md`'s "Two gaps in required-input validation that only the play could close" — this change is its named trigger. Record that it was revisited, that a `group_vars` has now been written from scratch, that both gaps stand, and why (design.md Decision 11), including the sentence distinguishing the guard play from the play-scope fix so a later reader does not read one as the other. Set a fresh revisit trigger.
- [ ] 8.1a Add to `docs/deferred-work.md`'s "Four requirements still stated over prod alone" the two requirements that acquire a second subject the moment staging converges with the same role set: *Host Joins a Private Tailnet for Non-Operator SSH Access* ("the prod host") and *Unprivileged Operator Accounts Support Interactive Host Inspection* ("on the prod host"), both in `openspec/specs/iac-host-configuration/spec.md`. That item currently names four requirements in three other capabilities and not these. Generalising them is not this change's work — the same argument that item already makes — but going unrecorded is how a requirement stays narrower than the mechanism it describes.
- [ ] 8.2 Record in `docs/change-queue.md` the missing test layer from design.md Decision 10: a play-level harness, what it would cover that neither Molecule nor `.github/tests` can, and that the guard's behaviour is verified by hand until it exists.
- [ ] 8.3 Open a `docs/change-queue.md` entry for **staging's web exposure**, carrying the two pieces of entry 50 that entry 52 does not claim: opening 80/443 in `terraform/environments/staging/terraform.tfvars`'s `web_allowed_cidrs`, and staging's own hostnames, DNS records and certificates. Take the wording from entry 50 rather than paraphrasing it, including its "must not undo" framing — a firewall rule added by the change that has something to put behind it. Record it as **blocked on 52, or at least pointless before it**, in the idiom entries 23 and 52 already use: entry 50 held both halves and got that ordering for free, and splitting them loses it. An entry picked up first would open 80/443 in front of an empty host, which is the state entry 50 warned against. Without this task the work is deleted along with entry 50 at 11.3 and recorded nowhere.
- [ ] 8.4 Re-point every revisit trigger that names entry 50 before it is deleted, and re-point each at the entry that actually inherits its subject rather than at 8.3's by default. `docs/deferred-work.md`'s "Managing DNS in Terraform" goes to 8.3's new entry, whose subject is DNS. "Four requirements still stated over prod alone" goes to **entry 52**: its trigger is staging acquiring a persistent store — its own wording is "the platform stack and a database" — which the web-exposure entry does not bring. Verify with a grep for `entry 50` across `docs/` that no pointer survives the deletion.
- [ ] 8.5 Update `docs/change-queue.md` entries 23 and 52 where they say they are blocked on 50, and check every other entry that names entry 50 — the block lifts when this change archives, not when it merges. Add to entry 52 the reciprocal of 8.3's ordering line, so the coupling is readable from either end rather than only from the newer entry.

## 9. Operator prerequisites, out of band

- [ ] 9.1 Create `ansible/.envrc` from the committed example with both read-only tokens, and `direnv allow`. Verify `ansible-inventory -i inventory/staging.hcloud.yml --graph` from `ansible/` shows the staging server under `@staging`, and `-i inventory/prod.hcloud.yml --graph` shows the production server under `@prod`.
- [ ] 9.1a With both credentials working, verify the guard's remaining path — an empty group under a source that parsed cleanly, which 2.3 could not reach without a credential: `ansible-playbook playbooks/host-baseline.yml -i inventory/prod.hcloud.yml -e target_environment=staging` refuses at the first play naming `staging`. Confirm the message is the guard's empty-group one and not an inventory parse error — that distinction is what task 1.3a exists to preserve.

      Run it in the same session as 9.1, immediately after its `--graph` showed the production server: **this task cannot establish on its own that prod's source resolved**. A source that parsed cleanly and returned no hosts — a token repointed at an empty project, a label no longer applied, `server_enabled = false` — produces this task's exact message and exit code. The positive counterpart is 10.1, where a non-empty group under the same source proceeds past the guard; 9.1a and 10.1 together cover both scenarios of *A Run Whose Target Group Resolves to No Host Refuses*, where 9.1a alone covers one of them twice.

      The direction is deliberate and safe: `hosts:` resolves to `staging`, which holds no host under prod's credential, so even a guard that failed to fire entirely could not reach production.
- [ ] 9.2 Choose staging's Vault password, store it in the password manager, and encrypt staging's GHCR token and heartbeat ping key with `--vault-id staging@prompt` into `group_vars/staging.yml` (task 4.3). The GHCR token may be the same `read:packages` token prod uses; the ping key is the same project ping key, which addresses a different check because the host name differs.
- [ ] 9.3 Generate a staging-only `platform` deploy keypair on the workstation. Commit the public half into `deploy_apps` (task 4.2); keep the private half in the password manager for entry 52, and put it in no GitHub secret in this change.
- [ ] 9.4 Generate a Tailscale auth key for staging — reusable, not ephemeral, no tags — and keep it in the password manager. It is supplied at the prompt in 10.2 and never committed.
- [ ] 9.5 Record staging's SSH host key in the operator's `known_hosts`, by logging in once: `ssh -i ~/.ssh/<company>-root root@<staging ipv4>`, the address from `terraform output` in `terraform/environments/staging/`. `ansible/ansible.cfg` sets `host_key_checking = True`, so without this the first converge fails at connection time with `Host key verification failed` before any role runs — the mid-run discovery this whole section exists to prevent. `docs/bootstrap-a-new-host.md` §4.3 currently says staging needs no such entry "because nothing converges it"; task 7.3 is what corrects that sentence, and this is what makes it false.

## 10. First converge, and confirming it

- [ ] 10.1 Prove production's invocation still reaches production, before touching staging: `ansible-playbook playbooks/host-baseline.yml -i inventory/prod.hcloud.yml -e target_environment=prod --vault-id prod@prompt --check --diff`. Check mode, not a converge — `AGENTS.md` allows local production credentials for reading and not for applying. Confirm it resolves the host and proposes nothing beyond the known check-mode noise `docs/bootstrap-a-new-host.md` §6.3 already describes.
- [ ] 10.2 Run staging's first converge locally: the same command against `staging.hcloud.yml` with `-e target_environment=staging --vault-id staging@prompt` and the auth key from 9.4. Not `--check` — §6.3 says a first run must not be, because apt tasks report changes they did not make and later tasks then fail against a stale cache. Record the play recap.
- [ ] 10.3 Run it again immediately and confirm `changed=0`. A second run that reports changes means something is not idempotent, and is worth understanding before anything else is built on this host.
- [ ] 10.4 Confirm the host state, as §6.4's checklist does for prod: `ufw status` shows default deny with 22 and the tailnet rules and **no** 80/443 rule, staging carrying `web_allowed_cidrs = []`; `tailscale status` reports Running and the machine appears in the tailnet; `systemctl list-timers` shows `prune-host-images.timer`; `/mnt/main-data` is mounted with `prometheus/` and `grafana/`; the `deploy` account exists with the forced-command entry for `platform` and no other; and the operator account logs in over the tailnet with `docker ps` working and `sudo -n true` refused.
- [ ] 10.5 In the Tailscale admin, disable key expiry for the staging machine, and verify it reads as disabled afterwards. §6.4 step 1 performs this for prod and nothing performs it for staging; without it the host silently drops off the tailnet when the key expires, which is the state that makes a host unreachable with no error naming the cause. The verification belongs here rather than in 10.4's checklist, which runs before this task and cannot assert a state nothing has created yet.
- [ ] 10.6 Trigger one prune activation and read what it reports: it will **abandon** on an empty keep set and report `/fail`, which is design.md Decision 8's stated and accepted outcome on a host with nothing deployed. Confirm the failure is that one and not a different one — the journal should name the empty keep set, not a missing enumeration or an unreachable observer — and that the check `staging-server-prune-host-images` now exists at the observer, distinct from prod's.
- [ ] 10.7 Set that check's period and grace at the observer to the values Appendix A lists for the prune. A check the reporter creates by pinging it carries the **observer's default** period until corrected — Appendix A says so — so until this task the host prunes weekly while the observer calls it overdue in a day. Decision 8's "the window is short and known" is stated over the weekly cadence and is not true of the check as created. Verify by reading the check's configured period back, not by reading the unit's timer.

## 11. Ship

- [ ] 11.1 Open the change's implementation pull request once verification passes on the branch head and the code review has cleared, and wait for the operator's confirmation that it merged and that CI was green. Nothing here deploys: this change ships no workflow and no Terraform.
- [ ] 11.2 The confirmation gate for this change is task 10 — a converged staging host, reachable over the tailnet, with the state 10.4 lists. It is not waivable: an observation can be made, and it has been proposed. Wait for the operator's confirmation rather than inferring it from a green pull request.
- [ ] 11.3 Bring the branch back to the freshly fetched trunk, confirm 8.3 and 8.4 have re-homed everything that pointed at entry 50, delete entry 50 from `docs/change-queue.md`, and archive the change's record with `openspec archive`. Verify `openspec validate --archived` passes and that `python3 -m unittest discover --start-directory .github/tests` still passes, the citation-form check included.

Opening the record's own pull request, and removing the branch and the working
tree once it merges, all happen after the commit that writes this file — so they
are recorded here in prose rather than as tasks that could never be ticked in
the file that contains them. Open that pull request; once it merges, remove the
branch locally and on the remote, and the working tree from the repository's
main working tree rather than from inside the tree being removed; then check
`docker ps` for anything this tree's Molecule runs left behind, since nothing
reclaims a removed tree's namespace.
