Verification commands referenced below, from this project's conventions:

- `python3 -m unittest discover --start-directory .github/tests`, from the repository root
- `pre-commit run --all-files`, which carries `terraform fmt`, `tflint`, `terraform validate`, `gitleaks`, `ansible-lint` and `ansible-playbook --syntax-check`
- `openspec validate --all`

No Molecule scenario is owed. This change touches no role: `deploy_user` already provisions the `deploy` account and its forced command on both hosts, and `ansible/inventory/group_vars/staging.yml` already authorises the `platform` key there. What changes is which stacks a workflow deploys to, which is a static property of committed files and belongs to `.github/tests`.

Sections 1–5 are repository work and land through this change's first pull request. Section 6 is the operator's, in repository settings and against credentials that exist nowhere in this repository; `design.md`'s Migration Plan is what sequences it against the merge, and 6.1 completes **before** the pull request merges. Removing the branch and the working tree happens after this record's own pull request merges, which is after the commit that writes this file, so those steps are recorded in prose in `proposal.md` and in `AGENTS.md` rather than as tasks here.

## 1. The declaration gains its fifth field

- [x] 1.1 In `terraform/stacks/main-production/pipeline.yml`, add `deploys_platform: true` with a comment block in the form the four existing fields use. It states: that the field names what is true when the value is **true**, as `destroy_policy_gate` does, and that `platform_excluded: false` would be the same value with the opposite meaning; that absence defaults to `false`, and that the two optional fields default to **opposite** values for one reason — each default is the safe value when the field was forgotten, which for the gate is "gated" and for this is "deployed to by nothing"; and that this field is the only thing that points the platform deploy at this stack, the GitHub Environment it attaches to being `github_environment` above. Say that the field is about a layer that is not Terraform and that this is not new — `target_environment` is an Ansible group in the same file — because the file is the stack's *pipeline* configuration and is `.yml` precisely so Terraform never reads it.
- [x] 1.2 In `terraform/stacks/main-staging/pipeline.yml`, add `deploys_platform: true`, deferring to production's block for the shared reasoning and stating only what is specific here: that this is the field this change flips, that staging's host has authorised the `platform` deploy key since 2026-09-10 and received no deploy in the interval, and that the interval was deliberate rather than an oversight — which is why the opt-in is a declaration and not a derivation from `deploy_apps`. Note that this stack's Environment requires no reviewer, so the deploy proceeds on merge, and point at this file's `github_environment` block, which already gives the reasoning for that setting.
- [x] 1.3 Verify both files still parse as the discovery step reads them — `sed`-extracted flat mappings, one key per line, value unquoted — and that `pre-commit run --all-files` is unaffected. `terraform fmt`, `tflint` and `terraform validate` all ignore `pipeline.yml`, which is the reason the file has that extension; confirm rather than assume, since a new field is the first opportunity for that claim to stop being true.

## 2. `.github/workflows/platform-deploy.yml`


      **Verified 2026-09-13.** `pre-commit run --all-files` reports `(no files to check)` for `terraform fmt`, `tflint` and `terraform validate` with both `pipeline.yml` files staged, confirming the extension still keeps them outside Terraform's reach. The discovery step's `field()` reader extracts both new values correctly.
- [x] 2.1 Rewrite the workflow header. What is there today explains a single gated deploy; what replaces it says that no stack, GitHub Environment or Ansible group is named anywhere in the file, that which stacks receive the platform stack comes from each stack's own `pipeline.yml`, and that the workflow-level `paths:` filter is permissible **only** because this is not a required status check — the required check is the validation step inside `pr-validation.yml`, where the opposite rule holds. Keep `permissions: contents: read` at workflow level and declare it again on every job: a job-level block replaces the workflow-level one rather than adding to it, and an absent declaration falls back to a repository setting that can change with no commit.
- [x] 2.2 Add `workflow_dispatch` with an optional free-text `stack` input, mirroring `host-converge.yml`'s. Free text rather than a `choice`, because a `choice` list would enumerate stacks in workflow text. Comment that an **absent or empty** input selects every discovered stack and is not refused — every `push` supplies an empty one — and that only a non-empty value is matched, so conflating the two would fail every merge. Say why the trigger exists: `docs/bootstrap-a-new-host.md`'s rebuild sequence redeploys the stack to one rebuilt host, and re-running the last run would redeploy every stack and wake a reviewed Environment's gate for a host that did not change. Say what it does **not** open: the dispatch deploys the committed content at the ref it runs on under the same Environment gate, so nothing here lets a deploy carry content that has not merged.

      **The code-review gate found a HIGH defect in this task's own work, and it was in what the task did not think to say.** A `workflow_dispatch` runs against whatever ref is chosen and `actions/checkout` takes that ref, so as first written this trigger would ship an **unmerged branch's** stack definition to a real host — and to `main-staging`, which requires no reviewer, with no approval at all. `AGENTS.md` says a change reaches production by merging and by nothing else, and `design.md` Decision 8 asserted that this trigger does not open a second path; that assertion did not follow from its own premise. A step in `discover` now refuses any ref but the default branch, before any stack is read, before any Environment is named and before any credential is in reach.

      **It is its own step rather than the discovery body's first lines, and the tests are why.** Put inside that body it needed two event-derived variables, and `.github/tests` executes that body verbatim against a scratch tree which cannot supply them — so all ten discovery tests died under `set -u`. That is the check earning its place: it caught a guard that would have worked in CI and made the body untestable everywhere else.

      Both comments this task asks for are present at the trigger: why it exists, and what it does not open — the second now naming the guard rather than asserting the conclusion.

- [x] 2.3 Add the `discover` job: no `environment:`, `contents: read`, one output carrying the matrix JSON. Its step enumerates `terraform/stacks/*/`, reads `github_environment` and `deploys_platform` from each `pipeline.yml` with the flat-mapping `field()` reader the sibling workflows use, and emits one record per stack that opts in, carrying `name` and `github_environment` and nothing else — the secrets are reached by fixed names through the Environment, so no secret name enters the matrix. Refuse, by name and non-zero, on: a stack directory with no declaration; a declaration that opts in but declares no `github_environment`; a `deploys_platform` value that is neither `true` nor `false`, rather than treating anything unrecognised as one of them; a control character in either field; a dispatched `stack` input naming a stack that did not opt in, reporting those that did; and an empty opt-in set. Echo the excluded stacks by name before emitting the matrix, so a stack omitted by a forgotten field appears in the log of every deploy rather than only in its own absence. Carry a comment saying this body is **deliberately not identical** to the Terraform and host-converge discovery bodies — it reads a field none of them read — and is therefore outside the identity assertion in `.github/tests`, which stays covering the three Terraform workflows exactly as it does today; name what it *does* share with them, and point at the backlog entry that would collapse the five. Take nothing from the event inside the step body, so `.github/tests` can execute it against a scratch tree.
- [x] 2.4 Keep the `diff` job, adding `needs: discover` and a comment saying why there is one of it rather than one per stack: the deployed content is the same for every stack, every per-stack value arriving at deploy time from that stack's Environment and therefore appearing in no diff, so a job per stack would publish the same summary several times and multiply the jobs that run before the gate. Handle the dispatched run, which has no push range: say so in the summary rather than emitting an empty code fence, as `host-converge.yml`'s `publish` job does.
- [x] 2.5 Turn `deploy` into a matrix job: `needs: [discover, diff]`, `environment: ${{ matrix.stack.github_environment }}`, `strategy.fail-fast: false`, matrix over the discovered stacks, and `concurrency` keyed on `platform-deploy-${{ matrix.stack.name }}` with `cancel-in-progress: false`. Delete the workflow-level `concurrency` block and say in its place why it moved: a workflow-level group cannot read a matrix, and two stacks' deploys contend for nothing. Set no `max-parallel`. Comment that `fail-fast: false`, the absent `max-parallel` and the absence of any `needs:` between deploy rows are the three ways promotion ordering would be reintroduced without a dependency edge to read, and cite *Each Stack's Deploy Attaches to the Environment Its Own Declaration Names* (`openspec/specs/iac-platform-deploy-pipeline/spec.md`) for why ordering is the approver's and not the workflow's. Replace the long comment justifying the `main-production` literal with one saying what replaced it: the Environment is resolved per matrix row from discovery, the hazard that comment described — GitHub creating an unprotected Environment for a name no stack declares — is now held by the census asserting no two stacks declare one Environment plus the assertion that the gate varies by row, and the pair is the same substitution the Terraform apply made.
- [x] 2.6 Add the deploy job's first step: refuse an **empty value anywhere in this stack's secret set**, naming the stack, the GitHub Environment and every empty name, before the tailnet join and before anything is written. The set is `PLATFORM_DEPLOY_HOST`, `PLATFORM_DEPLOY_SSH_KEY` and the seven secret-backed values of the **eight** `platform/.env.example` documents — nine, which is the count 6.1 enters. `GRAFANA_BIND_ADDRESS` is the eighth and is **not** in this set: no secret holds it, and the step that derives it runs after this one, so including it would refuse every row of every run. An undefined GitHub secret resolves to an empty string rather than to an error, and the two halves of that fail differently: an empty host reaches `tailscale ping` and `ssh deploy@` as an empty argument naming neither cause, while an empty `.env` value is interpolated by `platform/docker-compose.yml` as `${VAR}` with no `:?` form, rendering an empty assignment. The second is the one worth the step — an empty `GRAFANA_ADMIN_PASSWORD` lets Grafana start, satisfies the deploy's `--wait`, reports the run green, and violates *Metrics Dashboards Are Available* (`openspec/specs/iac-platform-services/spec.md`), which obliges a non-default credential. Mirror `host-converge.yml`'s refusal on an absent read-only credential, cite it, and say that this one reads nine values where that one reads a single credential. Bring all nine in through the step's own `env:` block rather than interpolating `${{ secrets.* }}` into the shell body — that workflow's credential step comments why (an interpolated expression is substituted as **text** before bash parses the line, so a value carrying a quote or a newline is parsed as shell, and one of these nine is a multi-line private key), and an `env:` block also lets the check loop over names instead of spelling each twice. Do not echo any value: report names only.

      **One wording change the tests forced, and it was the message rather than the logic.** `secret_set_offences` treats any step whose `run:` body mentions `.env` as a step that renders it, and the refusal's own `::error::` text named that file in prose -- so the step read as rendering before it had checked, and reported itself as acting too early. The message now says "the rendered environment file". The predicate is a heuristic, but it is the shape this suite uses throughout and the honest repair was on this side.

      **A second question the code-review gate raised, and the requirement moved rather than the code.** `TAILSCALE_OAUTH_CLIENT_ID` and `_SECRET` are scoped to each stack's Environment and consumed by this deploy, so the requirement's opening clause — "every secret that deploy consumes" — reached them while its enumeration did not. Widening the step to eleven was tried and reverted: the ordering check reads any step mentioning `tailscale` as the step that joins the tailnet, so the check became indistinguishable from the act it is obliged to precede. The narrowing is the honest end anyway. This step exists for secrets whose absence does **not** stop the deploy — an empty value rendered into a file, which a tolerant service accepts on its way to a green run. An absent OAuth credential fails the join, loudly, before anything is written, which is the guarantee this step would otherwise be supplying. The requirement now names that class instead of "every secret", and the workflow says why the pair is excluded.
- [x] 2.7 Leave the remaining steps' bodies alone — the tailnet join, the Grafana bind-address resolution, the `.env` render, the SSH key setup and the one-session `tar | ssh` delivery are all already written against `secrets.PLATFORM_*` and resolve per Environment with no edit. Update their comments where they say "production" or name the `main-production` Environment: each now describes the stack the row belongs to. Confirm by inspection that no step introduces a stack name, and that the `.env` render still lists exactly the eight variables `platform/.env.example` documents.
- [x] 2.8 Run `actionlint` if the toolchain has it, and `pre-commit run --all-files` regardless. Verify the workflow parses as YAML by loading it the way `.github/tests` does.

## 3. `.github/tests`

The independent test author writes what the delta specs oblige. The tasks here are the **four** existing assertions, across three modules, that rest on a proposition this change retires — none of which a new module would find, because each is green today and goes red on task 2.5 rather than on anything a delta adds.

- [x] 3.1 Re-point `TestTheDeployGateNamesTheEnvironmentAStackDeclares` in `.github/tests/test_the_external_service_names_are_retired.py`. Its `gate_disagreements` reader requires `platform-deploy.yml` to declare **exactly one** deployment Environment and requires a stack to declare that name — a proposition the specification held until this change and holds no longer. What replaces it asserts what the requirement now says: every Environment the deploy job can attach to is resolved per matrix row from discovery rather than written as a literal, and no job in the workflow names a literal Environment at all. This is **not** weakening a test to reach green — the proposition changed in the specification and the assertion is re-pointed at the new one — and it is recorded as an obsolete-test entry in `test-plan.md`. Keep the discriminator class that feeds the reader a half-renamed pair: the hazard it describes (GitHub creating an unprotected Environment for a name no stack declares) is not retired by this change, only relocated, and the class is what stops the replacement passing over an empty read.
- [x] 3.1a Re-point `TestTheDeployGateNamesTheProductionStacksEnvironment` in `.github/tests/test_the_github_environments_are_named_for_their_stacks.py`. Both of its assertions read the literal: one calls `deploy_gate_disagreements(...)`, which returns an offence unless the workflow's gated Environments are exactly one name equal to the production stack's declaration, and the other asserts `sorted(set(platform_deploy_gated_environments())) == ["main-production"]`. After 2.5 the single value read is the string `${{ matrix.stack.github_environment }}`, so both are red. The replacement proposition is the one the requirement now states: each deploy row's Environment is resolved from discovery rather than written as a literal, and which Environment a given stack resolves to is readable from that stack's own declaration — asserted per discovered stack rather than over one name. Record it as an obsolete-test entry in `test-plan.md`, and keep that module's own fixture-driven discriminators, which exist for a hazard this change relocates rather than retires.
- [x] 3.1b Re-point `TestAProposedImageUpdateIsNotExemptFromTheStacksObligations.test_the_stack_deploy_stays_gated_on_the_production_environment` in `.github/tests/test_ci_configuration.py`, and the module constant `GATED_DEPLOY_ENVIRONMENT` it reads. It asserts that exactly one job in `platform-deploy.yml` declares `environment: main-production`; after 2.5, zero do. This one carries a live obligation of its own and SHALL NOT simply be deleted: it traces to *Automated Dependency Updates* (`openspec/specs/iac-safety-hardening/spec.md`) and its scenario that an automatically proposed image bump is subject to the same gated deploy approval. What keeps that true is now that **every** deploy row declares an `environment:` resolved from discovery, so no route exists by which a merged image bump deploys ungated. Assert that, delete the constant, and record both in `test-plan.md`.

      **Done, and the constant is gone rather than re-pointed.** `GATED_DEPLOY_ENVIRONMENT` held a literal no workflow now contains, and a dead constant asserting a retired rule reads as the rule. The replacement assertion finds every job that sets up the platform deploy key and requires each to declare an `environment:` varying by matrix row -- which is what keeps a merged image bump gated, and fails both on a dropped gate and on a literal one. Corrected at the code-review gate: the finder read each step's `run:` alone, so moving the deploy key into a step's `env:` — the convention task 2.6 applies to its own step — would have reported a deploy with no gate, which is the same message as a gate genuinely removed. It reads the whole step now.
- [x] 3.2 Widen `TestNoWorkflowNamesAnEnvironment` in `.github/tests/test_environment_agnostic_pipeline.py` to reach `platform-deploy.yml`. Its `TERRAFORM_WORKFLOWS` tuple is three files and its docstring already records the narrowing as a decision, naming `host-converge.yml` as the workflow the sweep does not look at. This change makes a fourth, and the requirement it would enforce — *The Platform Deploy Names No Stack* — is one of this change's own deltas, so the sweep must reach it. Decide and record whether `host-converge.yml` comes with it or stays out: it is not this change's requirement, so the honest default is to leave it and say so in the docstring rather than widen it silently. Whichever is chosen, the docstring's account of what the tuple reaches must be true afterwards.
- [x] 3.3 Correct the citations of the retired requirement name, by literal search now rather than by the sweep. *Gated Deploy Reuses the Terraform Production Environment* is renamed by this change, and `.github/tests/test_the_retired_requirement_names_are_gone.py` derives its retired set from the **archive** less what `openspec/specs/` currently holds — so while this change is in flight the name is in neither half and that sweep reports nothing. It goes red for the first time at task 7.2, on the record's own pull request, which is exactly the late failure `AGENTS.md`'s citation section describes. Do the work now: outside `openspec/`, the name appears in three files — `.github/workflows/platform-deploy.yml` (the comment above the deploy job's `environment:`, which 2.5 rewrites anyway), `.github/tests/test_the_external_service_names_are_retired.py` (two docstrings), and `.github/tests/test_the_github_environments_are_named_for_their_stacks.py` (four places, including the two assertions 3.1a re-points). Note that `platform/README.md` cites the **capability** and no requirement name, so 4.1's edit there is about the mechanism rather than about this rename. Re-run the literal search after the edits and record the result in this task's completion note.

      **Done, and the planned search missed three of them.** The literal full-name search found the name in the three files this task predicted. A second search for the opening words alone then found three more, in `.github/tests/test_the_external_service_names_are_retired.py` and `.github/tests/test_the_github_environments_are_named_for_their_stacks.py`, where the name is **wrapped across two comment lines** and no single-line search for the whole string can see it. Worth recording as the method rather than as the incident: a citation sweep by literal must search a distinctive prefix, not the full name.

      **One of those citations turned out to be load-bearing**, which is the second thing this task found. `TestEveryStacksGithubEnvironmentIsOnTheStackAxis` traces its stack-axis relation to a sentence the retired requirement carried -- that each GitHub Environment is named for the stack rather than for the environment axis -- and the ADDED requirement had not reproduced it, so that assertion would have traced to nothing. The sentence is carried into the new requirement and the carry-forward is named at the removal. Its force also grew: with one deploy a name on the wrong axis was untidy; with a row per stack, two stacks of one environment collide on an environment-axis name and share whichever Environment's write token was created first.
- [ ] 3.3a Confirm at archive time, not before, that the sweep is green. Once this change's deltas move into `openspec/changes/archive/`, the retired name becomes derivable and `test_the_retired_requirement_names_are_gone.py` fires for the first time. Run the suite as part of the record's own pull request rather than assuming 3.3 caught everything — the whole reason that module exists is that a citation is correct when written and wrong only once the change it cites has succeeded.
- [x] 3.4 Run `python3 -m unittest discover --start-directory .github/tests` and confirm the suite is green. Read the count, not the exit code: a module that fails to import is reported differently from a module whose assertions fail.

## 4. Documentation

- [x] 4.1 `platform/README.md`, the "Deployed by a GitHub Actions workflow, not Ansible" bullet. It describes one `main-production`-gated job reaching one host. Rewrite it over the stacks that opt in, naming where the opt-in is declared, and say that whether a deploy pauses for a reviewer is the Environment's property. It cites the capability `iac-platform-deploy-pipeline` and no requirement name, so this is not one of 3.3's citation repairs — the edit here is to the mechanism it describes.
- [x] 4.2 `docs/bootstrap-a-new-host.md`, the "From here on, two hosts — but only one of them runs anything" section. Its single bullet says `platform-deploy.yml` "declares `environment: production`" — already stale on the Environment's name and about to be stale on the mechanism — and names this backlog entry as what closes it. Rewrite the section: stage 7 is now run once per stack, what still makes staging different is its web exposure and its DNS (entry 17) rather than its deploy path, and the sentence about the weekly prune reporting failure "until the stack arrives" is retired by this change rather than deferred by it.
- [x] 4.3 `docs/bootstrap-a-new-host.md` §0.3 and §6.4. The platform deploy-key row says staging's key "stays at `~/.ssh/<company>-platform-staging` and in your password manager until staging gets a deploy path" and the `PLATFORM_DEPLOY_SSH_KEY` row says "production's key only". Both are retired. Say instead that each stack's Environment holds its own key's private half, and that one leaked half deploys to one host.

      **Ticked before it was done, and the code-review gate caught it.** The prose paragraph was rewritten and the **table row above it was not** — §0.3 still carried a "Platform deploy key — staging" row saying the private half belongs in "your password manager, and no GitHub secret yet … Do not delete it", ten lines above a paragraph saying the opposite. An operator assembling keys from that table would have left `PLATFORM_DEPLOY_SSH_KEY` out of staging's Environment, and the first merge touching `platform/` would have failed that row at 2.6's check — the check working, but the document causing it. The row is gone; the generalised one above covers every stack. A second sentence in the same section, that staging's key "is never deleted at all", went with it.
- [x] 4.4 `docs/bootstrap-a-new-host.md` §7. Make it per-stack: the nine `PLATFORM_*` secrets are entered into **each** stack's Environment, with values of that stack's own, and the table of what each holds gains the note that a value shared between stacks by way of a repository secret is the defect to avoid. State explicitly that a stack's alert-delivery target should be its own — nothing in the running stack identifies which host an alert came from, so two stacks pointed at one Slack channel produce alerts nobody can attribute, and no check reports it. Carry the reasoning from `design.md` Decision 9 rather than restating the mechanism.
- [x] 4.5 `docs/bootstrap-a-new-host.md` §7.5 and Appendix A. The `pgexporter` role and the dead-man's-switch registration are run once per stack; Appendix A's check table gains staging's Watchdog check. Leave §7.1's period-and-grace text alone — it is wrong for a reason `docs/backlog.md` entry 15 owns, and correcting it here would be scope this change does not have.
- [x] 4.6 `docs/bootstrap-a-new-host.md` §103's "three things sit outside it deliberately" paragraph and the secret-inventory table near the end both scope `PLATFORM_*` to production. Bring both onto the per-stack footing. Then re-read the document for any remaining sentence that says the platform stack is production's alone — `grep -n "production only\|production's alone\|only one of them runs"` is the starting point, not the finishing one.

## 5. Backlog


      **Swept, and three claims survived the section-by-section edits.** A search for `production only`, `production's alone` and `until staging gets a deploy` found: the time estimate at the top of the document ("Stages 7 to 9 are production's alone"), the paragraph at §0.3 headed "Why the two platform deploy keys are stored differently", and the `TAILSCALE_OAUTH_CLIENT_ID` row's "(production only)". All three corrected. The §0.3 paragraph is rewritten rather than deleted: the interval it described was real, and a reader meeting it in the history needs to know why it ended.

      **The sweep was not finished, and the code-review gate found four more sites — including the first thing a reader sees.** This task's own text warned that the grep "is the starting point, not the finishing one", and the finish did not reach: the document's opening "What you will have at the end" list still promised "a shared platform stack **on the production server**" and "a staging server, configured but running nothing … because `platform-deploy.yml` still deploys to one stack"; Appendix B's rebuild procedure opened by saying it covers production alone and that staging "stops there, having no stack to redeploy", two paragraphs above its own sequence that now redeploys staging; and Appendix C said a second host's application stack "waits on `docs/backlog.md` entry 16" — this change. All four corrected, the last cited by name rather than by number for the reason 5.2 gives.

      **The lesson is about the method rather than the count.** Every one of those four states the retired fact in words the grep could not match — "on the production server", "running nothing", "having no stack to redeploy", "entry 16". A sweep keyed on the phrasings a document happened to use finds the sites already known; what found these was reading the document's structural claims — its opening promise, its appendices — against what the change makes true.
- [x] 5.1 Add an entry recording that nothing labels an alert with the stack it came from: `platform/docker-compose.yml`'s Prometheus configuration declares no `external_labels`, so two stacks whose `PLATFORM_SLACK_WEBHOOK_URL` addresses one channel produce identical alert text and no check reports the collision. Record what this change did instead (separate delivery targets, an operator instruction, no enforcement), why labelling was not folded in (a Compose change, a new `.env` variable and a checksum regeneration, all excluded by this change's Non-Goals), and that the entry is not blocked.
- [x] 5.1a `README.md`'s workflow list described the retired shape — "a deploy job gated on the same `main-production` Environment" — while its neighbouring `apply.yml` and `host-converge.yml` entries already used the per-stack phrasing. **Outside every task in section 4**, which named `platform/README.md` and `docs/bootstrap-a-new-host.md` and not the root README; found at the code-review gate and corrected here rather than deferred, because it is the same claim this change retires and *Automated Dependency Updates* treats the repository README's runbook as a place a reader is sent.
- [x] 5.2 Update `docs/backlog.md` entry 17, `expose-staging-on-the-web`, which says it is "blocked on 29, or at least pointless before it" — a number that no longer names this entry, which is the renumbering hazard the file's own header warns about. Re-point it at this change **by name**. Do not renumber the file.
- [x] 5.3 Leave `ansible/inventory/group_vars/staging.yml`'s "THIS FILE IS INCOMPLETE, AND THE HOST IS NOT YET CONVERGED" banner alone, and confirm `docs/backlog.md` entry 18 still owns it. This change cites that file as its evidence that staging's host is ready, and the banner says the opposite; the file's own values contradict it. Entry 18 is not blocked and touches one file, so correcting it here would be scope this change does not have — but meeting it and saying nothing would leave a reader unable to tell which of the two is current.
- [x] 5.4 Delete this change's own backlog entry only at archive time, not here. `AGENTS.md` says an entry is deleted when its change is archived, and deleting it now would leave the backlog claiming the work is done while it is still in review.

## 6. The operator's work

- [x] 6.1 **Before the pull request merges.** Create the nine `PLATFORM_*` secrets in the `main-staging` GitHub Environment — the deploy host, the deploy key, and the seven secret-backed values `platform/.env.example` documents: `PLATFORM_DEPLOY_SSH_KEY` (the private half of the staging platform keypair, from the password manager — the public half has been committed in `ansible/inventory/group_vars/staging.yml` since 2026-09-10), `PLATFORM_DEPLOY_HOST`, `PLATFORM_ACME_EMAIL`, `PLATFORM_POSTGRES_USER`, `PLATFORM_POSTGRES_PASSWORD`, `PLATFORM_POSTGRES_EXPORTER_PASSWORD`, `PLATFORM_GRAFANA_ADMIN_PASSWORD`, `PLATFORM_SLACK_WEBHOOK_URL` and `PLATFORM_DEADMANSWITCH_URL`. Every value is staging's own; none is copied from production's Environment. The Slack webhook addresses a channel of staging's own, for the reason 4.4 records. The dead-man's-switch URL is a check of staging's own, registered with the heartbeat service. `TAILSCALE_OAUTH_CLIENT_ID`, `TAILSCALE_OAUTH_SECRET`, `HCLOUD_TOKEN` and `ANSIBLE_*` are already on this Environment from the host-converge work and are not re-entered. Confirm the count is nine by listing the Environment's secrets, and record it in this task's completion note. A **partially** entered set is the likeliest first-time error and is not harmless: 2.6's refusal catches it, but only after the merge and only as a red run.

      **Performed 2026-09-13 by the operator, verified the same day.** `gh secret list --env main-staging` reports all nine `PLATFORM_*` secrets, created between 16:18 and 16:23 UTC, alongside those already present and deliberately not re-entered -- `ANSIBLE_SSH_PRIVATE_KEY`, `ANSIBLE_VAULT_PASSWORD`, `HCLOUD_TOKEN`, `TAILSCALE_OAUTH_CLIENT_ID`, `TAILSCALE_OAUTH_SECRET` and `TF_API_TOKEN` -- whose timestamps are unchanged from 08:56-09:37.

      **Two values were established rather than generated.** `PLATFORM_DEPLOY_SSH_KEY` came from `~/.ssh/shatynska-platform-staging`, which already existed; `ssh-keygen -lf` on it prints `SHA256:yuIlRxQKZC2+a8Zh4Q9g/+kJp5zEcaMyj/XJRiO0LIM deploy@platform-staging`, equal to the fingerprint of the public half committed in `group_vars/staging.yml`, so it is the key staging's `deploy` account authorises. `PLATFORM_DEPLOY_HOST` is `100.85.219.36`, staging's **tailnet** address, read from `tailscale status` on the production host -- the workstation's own `tailscale` is broken under WSL. A public address would have failed to connect at all, and would have published staging's Grafana on the public interface.

      **Noticed and not acted on:** `~/.ssh/shatynska-platform` -- production's deploy key -- is still on the workstation, where §0.3 says to delete it once stored. Raised with the operator; it is not this change's to remove.
- [ ] 6.2 **After the merge.** Confirm a run started at all, first: the workflow triggers on `paths: platform/**`, and this change's merge matches only because 4.1 edits `platform/README.md`. If the documentation work landed separately and no run started, dispatch the workflow for `main-staging` rather than waiting — that is what 2.2's trigger is for. Then confirm both deploy rows: staging's runs unapproved and reports success; production's waits for approval, and once approved deploys the same content to the same host it deploys to today. Read the run's discovery output and confirm it names both stacks as included and none as excluded.
- [ ] 6.3 **After staging's first deploy.** Create `pgexporter`'s role in staging's shared Postgres instance, with a password matching `PLATFORM_POSTGRES_EXPORTER_PASSWORD` in the `main-staging` Environment — `platform/README.md` carries the two statements. Until this runs, staging's `MetricsTargetDown` fires for the `postgres-exporter` job; that is the alert working, and it is the same interval production passed through.
- [ ] 6.4 **After staging's first deploy.** Confirm staging's dead-man's-switch check is receiving Watchdog pings and set its period and grace to the values Appendix A records — a check created by its first ping carries the observer's default until corrected.
- [ ] 6.5 **On the following Monday.** Confirm `main-staging-prune-host-images` reports success rather than failure. It has reported failure every week since staging converged, because a host with nothing deployed has an empty keep set and the prune treats that as a refusal; the platform stack's images are what end it. This is the observation that confirms the change rather than the deploy, and it is the only one that cannot be made on the day.

## 7. Ship

- [ ] 7.1 Open the pull request once verification passes on the branch head and the code-review gate has cleared.
- [ ] 7.2 Archive: bring the branch back to the freshly fetched trunk, delete this change's entry from `docs/backlog.md`, commit the specification record, and open the record's own pull request.
