## 1. The new document

- [x] 1.1 Create `docs/onboard-an-application.md` with the grid stated once at the top — an application is onboarded per deploy target, the infrastructure entry sits on the environment axis and the application repository's Environment on its own — and a numbered forward path with a command and a check per step. Verify by reading it against `docs/bootstrap-a-new-host.md` stage 8 and confirming every step there has a place in it.
- [x] 1.2 Move the naming rule and the image-namespace coupling it exists for, citing *Superseded Application Images Are Reclaimed at Deploy Time* (`openspec/specs/iac-host-configuration/spec.md`). Verify the coupling is stated in the terms that requirement uses — two path segments after the registry, the last equal to the application name, an empty namespace reclaiming nothing silently.
- [x] 1.3 Move the infrastructure side: generate the application's deploy key (decision 5), add the `deploy_apps` entry to that environment's `group_vars`, open and merge the pull request, let the converge run, and confirm GHCR read access. The key-generation step carries the rule that travels with it — the private half is generated into `~/.ssh/` and never into the checkout, because these keys are passphrase-less and a routine `git add -A` is one command away from publishing one. **The reassurance beside that rule does not travel**: §0.3 says `.gitignore` catches this scheme's extension-less names, and task 4.3 measures that it does not catch the one this document generates. Say what actually stands behind it — gitleaks at commit time, once `pre-commit install` has run — rather than repeating a net that is not there. Verify the order the text gives is the one that avoids a wasted converge — the entry lands before the application repository's deploy path exists, not after — and that the document states the convention task 3.2's widened check asserts, rather than leaving the check as the only place it is written.
- [x] 1.4 Move the database section whole: the decision between durable and non-durable, the fenced recipe exactly as it stands, the expected output, the part-by-part explanation, the failure table and the rotation paragraph. Verify by diffing the moved text against stage 8.3 and confirming the only differences are cross-references.
- [x] 1.5 Add the four answers the recipe raises on first use, beside the block: where the password comes from and that nobody holds it afterwards; that it is one role, database and password per application per host even where the secret's name is reused; that it is never `PLATFORM_POSTGRES_PASSWORD`, the instance superuser's, which stays in this repository; and that a lost or leaked password is the same block with `rotate=yes`, picked up on the application's next deploy. Verify each of the four is answerable from the document alone.
- [x] 1.6 Move the application side as it stands (decision 2): the store-declaration obligation, the Compose file and its Traefik labels, one Environment per deploy target, the deploy workflow's two jobs, DNS, and the per-Environment secret table. Verify no sentence was rewritten beyond a cross-reference.
- [x] 1.7 Write what is reachable before DNS and a certificate exist: `curl -k --resolve '<hostname>:443:<tailnet IP>' https://<hostname>/` from a tailnet peer, why the plain `http://` form answers `301` whatever the `Host` header says, why a browser is not that path, and that what refuses a request from the public internet is the Hetzner cloud firewall rather than UFW, because a container-published port is forwarded and never offered to UFW's `INPUT` chain. Verify the statement is scoped to the ports Traefik publishes and does not restate the two-layer claim `docs/backlog.md` `say-what-the-host-firewall-actually-gates` exists to correct.
- [x] 1.8 Write that an application enumerated in `deploy_apps` before anything deploys behind it contributes nothing to the image prune's keep set, and that this is expected rather than a fault. Verify against `ansible/inventory/group_vars/staging.yml`, which records it, and against *Unreferenced Host Images Are Pruned on a Schedule* (`openspec/specs/iac-host-configuration/spec.md`).
- [x] 1.9 State the key-comment convention — `<app>-deploy-<environment>` and `deploy@platform-<environment>` — and that production's two entries predate it and carry no environment segment, a key being identified by its fingerprint rather than its comment (decision 4). Verify the two spellings named match what `ansible/inventory/group_vars/production.yml` and `staging.yml` actually carry.
- [x] 1.10 Renumber the bare intra-stage-8 references the moved text carries — "(8.4, the Environments item)", "that target's key from 8.2", "the name 8.3 gave it", "Written by 8.3's recipe" — onto the new document's own sections. Nothing catches these: task 2.2's sweep does not match the bare form, correctly, since the bootstrap document uses it nowhere outside stage 8, and the cross-reference checker reads only that document. Verify by sweeping `\b8\.\d` over the new document to an empty result.

## 2. The documents it is extracted from

- [x] 2.1 Reduce `docs/bootstrap-a-new-host.md` stage 8 to a pointer to the new document, keeping only what a reader of the bootstrap sequence needs at that point. Verify literally: every sentence removed from stage 8 is present in the new document, and stage 8 says nothing the new document does not.

  Done, with **one sentence deliberately corrected rather than moved**, recorded here because the literal check above does not accommodate it and a later reader diffing the two documents would otherwise find an unexplained divergence. Old stage 8.2 step 3 read "Open a pull request, merge it, then run the playbook (stage 6.3, the tailnet key may be omitted now that the host has joined)". That predates `apply-host-configuration-through-a-gated-workflow`: a converge now runs from a merge to `main` touching `ansible/`, and telling an operator to run the playbook by hand is what this project's pipeline rule forbids. Corrected against `.github/workflows/host-converge.yml`, whose trigger is `push` to `main` filtered on `ansible/**`, with a credential-holding job per stack on that stack's GitHub Environment.
- [x] 2.2 Sweep the whole tree outside `openspec/` for `§8\.\d` and `[Ss]tage 8\.\d`, case-insensitively as the cross-reference checker matches, and redirect every hit to the new document, naming the file and its section in prose rather than in `§N.N` form — which the checker resolves against the bootstrap document's own headings whatever document the pointer is in. Eight hits exist in two files: three in `docs/bootstrap-a-new-host.md` (Appendix B twice, Appendix C once) and five in `docs/backlog.md`, across **four** entries — `upgrade-the-shared-postgres-major`, `write-and-rehearse-the-rebuild-runbook`, `automate-per-application-database-provisioning` twice (the entry the requirement's own divergence paragraph names), and one inside entry `record-how-an-application-is-onboarded` itself, which goes with that entry when task 4.4 deletes it, so redirecting that one is optional. Verify by re-running the sweep to an empty result and by running `test_the_bootstrap_documents_static_conventions.py`; nothing checks the backlog's cross-references, which is why that half is swept rather than trusted.
- [x] 2.3 Remove the application deploy key's row from §0.3's table and replace it with the line saying that key is generated when an application is onboarded (decision 5). Verify §0.4's note that this key "belongs to stage 8" still resolves, and update it to name the new document.
- [x] 2.4 Sweep §0.3's surrounding prose for what the row's removal falsifies, rather than editing the table alone: the paragraph above the table says "the table below has a row for each", and the paragraph below it says "Every platform and application key here is generated in stage 0 and not stored until stage 6 or 8", which stops being true of a key generated at onboarding time. A third sentence in the same paragraph narrates what the application row "read" before it was corrected, and refers to a row that will be gone. Restate the generation window for the keys that remain. Qualify the same paragraph's claim that `.gitignore` covers "this scheme's own extension-less ones" against the gap task 4.3 measures — it does not cover the application key's current name, nor the platform key's under the production spelling. Verify by reading §0.3 end to end after the edit and finding no sentence that counts, places or refers to a row the table no longer carries, and none that promises a net task 4.3 records as absent.
- [x] 2.5 Resolve the `<stack>`/`<environment>` discrepancy onto the environment for both deploy keys across the **whole** document, not §0.3 alone. The literal `~/.ssh/<company>-platform-<stack>` appears three times — §0.3's table, §0.3's verify-before-storing paragraph, §6.4's secret table — and the key comment `deploy@platform-<stack>` once, in that same table row. The per-stack cardinality wording appears in §0.3's converge-key paragraph ("one per stack for the same reason the platform deploy keys are"), §0.3's closing line ("two keys rather than one… must not be able to deploy to both stacks"), §0.4's *Platform deploy keypair* row, §6.1's `deploy_apps` row and Appendix A. Verify no occurrence of either literal remains, and that each cardinality sentence either moved to the environment or was kept deliberately because it states where the **private half** lives, which stays per stack.
- [x] 2.6 State in one place — §0.3, where the key is described — which properties of the platform deploy key are per environment (the filename, the comment, the authorising `group_vars` entry) and which remain per stack (the private half, in that stack's GitHub Environment as `PLATFORM_DEPLOY_SSH_KEY`, resolved by `platform-deploy.yml`'s deploy job for that stack). Verify §6.4 and Appendix A agree with it rather than restating either half.
- [x] 2.7 Delete the **clause** carrying the discrepancy as an open question naming `docs/backlog.md` entry 51, not the paragraph holding it (decision 3). That paragraph also carries the purposes-and-blast-radius reasoning, which §0.4 cites in as many words — "**Why each count is what it is**, §0.3's paragraph above already says" — so a literal deletion would take text another section depends on. Verify §0.4's citation still resolves to something that says what it claims, and that the paragraph's "the table below has a row for each" sentence was settled by task 2.4 rather than left.
- [x] 2.8 Add the two deploy keys to `docs/naming-conventions.md`, *The workstation*, which §0.3 calls the full list, and sweep that file for the same per-stack wording while editing it. Each new row states that the private half is deleted once stored, as §0.3 instructs, so a table of what a workstation holds is not read as saying a passphrase-less key lives there indefinitely — and the converge-key row already there gets the same note in the same pass, since it is the third key of that kind and saying it of two would read as a distinction. Verify its statement that the converge key is the only per-stack artefact whose name lives on a workstation is now true rather than nearly true.
- [x] 2.9 Redirect `platform/README.md`'s two references to `docs/bootstrap-a-new-host.md` as the holder of the `CREATE ROLE` / `CREATE DATABASE` recipe. Verify the second half of that passage too — it describes the automation as deferred until an application needs a database, which the divergence paragraph in *Single Shared PostgreSQL Instance, Per-Application Databases* (`openspec/specs/iac-platform-services/spec.md`) has since replaced with a due-and-unmet obligation.
- [x] 2.10 Add the new document to `README.md`'s `docs/` enumeration, which names the three documents that directory holds. Verify the description given there is one clause and says who reads it and when, as its neighbours do.
- [x] 2.11 Correct `.gitignore`'s private-key comment block on three counts: it names the retired `<company>-<app>-deploy` spelling, it says §0.3 writes each key into `~/.ssh/` — no longer wholly true once an application's key is generated at onboarding time — and it states a count, "stage 0.3 generates six passphrase-less keypairs", which the row removal falsifies. The **patterns** are not edited here: the gap they leave is task 4.3's backlog entry, and widening a pattern is a behaviour change rather than a correction. Verify the comment names the spellings the two documents now prescribe.

## 3. The tests follow their subjects

- [x] 3.1 Re-point `.github/tests/test_the_shared_instance_has_its_first_database.py` at `docs/onboard-an-application.md`, keeping every assertion it makes of the recipe and every falsification class that proves each detector fires. The module's prose moves with its constant: the docstring names the recipe's document, and the constant itself is called `BOOTSTRAP`, which would leave a module asserting one document while naming another. Verify by running the module and confirming the same tests run and pass, not fewer, and that no surviving sentence in it names the document the recipe left.
- [x] 3.2 Widen `.github/tests/test_the_bootstrap_documents_static_conventions.py`'s key-target check to read the new document as well, so the `ssh-keygen -f` an operator runs per application per environment is still held to generating into `~/.ssh/`, with a per-document anchor so a document that stops being read fails rather than passing silently. Keep the **cross-reference** check scoped to the bootstrap document, for the reason that module's docstring gives. Verify by running the module and by making the new document's command fail the check on purpose before restoring it.
- [x] 3.3 Leave the `ssh-keygen` floor's **number** alone. It is `>= 6` against 13 occurrences across the whole document, so removing §0.3's row leaves 12 and the floor is unaffected; lowering it would loosen a live assertion for nothing. Its failure message narrates the number as "fewer `ssh-keygen` invocations than §0.3's key table has rows", which stops describing the threshold once that table has four rows — restate the rationale without touching the figure. Verify by counting the occurrences after task 2.3 and confirming the module still passes on that assertion with only its message changed.
- [x] 3.4 Run the whole static suite and establish what each other reader of these two documents does with them — `test_the_github_environments_are_named_for_their_stacks.py`, `test_the_retired_requirement_names_are_gone.py`, `test_the_external_service_names_are_retired.py` and `test_the_platform_data_mount_moved.py` reference the bootstrap document or sweep every committed document — and confirm the new document enters the repository-wide sweeps correctly rather than being read as absent. Verify: `python3 -m unittest discover --start-directory .github/tests` from the repository root, with the suite's own dependencies installed from `.github/requirements-ci.txt` first — an unprovisioned run is indistinguishable from a passing one.
- [x] 3.5 **Say which source the requirement's document name is resolved from before the detector is written**, because for a window this change will occupy it is ambiguous. `requirement_sources()` in `.github/tests/test_the_shared_instance_has_its_first_database.py` reads the requirement from **every** live change's delta, and while `provision-commerce-ops-database-in-the-shared-instance` is live its delta names `docs/bootstrap-a-new-host.md` where this one names `docs/onboard-an-application.md`. A detector faithful to the scenario over both sources would demand that two documents each hold the recipe and that no second document hold a copy, which nothing can satisfy. The rule: the document name is resolved from **this** change's delta while that delta is live, and from the main specification once **this** change is archived; a live delta naming a document this change supersedes is not a source for this check. The two clauses are written against this change's own life rather than the other's, because the expected order leaves an interval — the other change archived, this one still live — in which the main specification names the document the recipe has left, and a rule falling back to it there would be red on the trunk for the whole interval. Verify the detector states that rule where it resolves the name, and that it is green on this change's own pull request with the other change still live.
- [x] 3.6 The delta's new scenario — the named document holds the recipe, and no second committed document holds a copy — is a new obligation and its check comes from this change's test-authoring dispatch rather than from re-pointing an existing constant. Verify that dispatch is given the scenario, and that whoever writes the detector is told what this change measured: the scenario's population is committed **Markdown outside `openspec/`**, which is what keeps three classes of deliberate copy out of it — a prose mention is not a copy (`platform/README.md` has one); `openspec/` holds real fenced copies inside change records; and the module task 3.1 re-points carries three fenced recipe fixtures of its own, one an exact copy of the live recipe, whose drift from it is a decision `provision-commerce-ops-database-in-the-shared-instance`'s test plan already took. Tell them too that `platform/README.md` carries a fenced `CREATE ROLE pgexporter` block, a different recipe and the likeliest remaining false positive, which keying on `CREATE DATABASE` rather than `CREATE ROLE` excludes as the existing module already does; and that a live change is located **by name** across the live and archived layouts rather than by a literal `openspec/changes/<name>/…` path, which this repository's citation check forbids — `requirement_sources()` is the idiom to copy.
- [x] 3.7 Widen the same module's **cross-reference** check to the new document, which this change numbers for the first time and which cites its own sections six times. Added after the code review found the gap: the docstring's reason for scoping that check to one document is the risk of going red on an archived record, and a document resolving its own references against its own headings carries none of it. Each document keeps a floor of its own under how many sections it declares, since what counts as implausibly few differs. Verify the check discriminates on the new document rather than passing vacuously — over its committed text it reports nothing, and over an in-memory copy carrying `§9.9` and `stage 7.4` it reports both.

## 4. The specification and the backlog

- [x] 4.1 Confirm this change's `.openspec.yaml` carries no `skip_specs` line — it was dropped when the delta was written, per this repository's convention that the line is only for a change genuinely declaring none. Verify `openspec validate --all` passes.
- [x] 4.2 Record in `docs/backlog.md` the finding of decision 3 — that `deploy_apps` sits in `group_vars/<environment>.yml`, so a second tenant in one environment would authorise one deploy key on two hosts — as an entry of its own, not folded in. Verify the entry states what it waits on and why it was recorded rather than opened.
- [x] 4.3 Record in `docs/backlog.md` the second finding of decision 5: `.gitignore`'s root-anchored private-key block carries `/*-deploy`, written for the retired `<company>-<app>-deploy` spelling, and matches no key named `<company>-<app>-<environment>` — so the most frequently generated passphrase-less key is uncovered by the layer that does not depend on having installed anything. Name the platform key in the same entry: under the environment spelling `<company>-platform-staging` is matched by `/*-platform-staging` and `<company>-platform-production` by nothing, which is a worse shape than either spelling alone because it covers one environment and not the other. Verify the gap by testing the current patterns against each filename, and state in the entry that the application half predates this change.
- [x] 4.4 Delete `docs/backlog.md`'s `record-how-an-application-is-onboarded` entry as part of the archive commit, not before. Verify nothing else in the tree cites it once it is gone, and record here which of its items this change closed and which were already closed by `onboard-commerce-ops-to-staging` (design.md, Context), so the deletion is not read as this change's credit for both.

## 5. Verification

- [x] 5.1 Run the static suite and `openspec validate --all` on the branch head and record both outcomes. Verify both are green before the code review is dispatched.
- [x] 5.2 Read the new document once as an operator who has never bootstrapped this host: every step has a command, every command has a check, and nothing requires opening `docs/bootstrap-a-new-host.md` except the four things decision 8 leaves there. Verify by naming, in this task, each jump the document still asks for.

  Done. Five jumps remain, four of them to the once-per-server material decision 8 leaves in `docs/bootstrap-a-new-host.md`: the repository-settings stage, for how an Environment is created with the reviewer a target warrants; the DNS stage, for the zone and its nameservers; the tailnet stage, for the OAuth client an application's deploy job reuses; and that document's secret inventory, reached only indirectly. The fifth is `platform/README.md`, "Joining the platform network", for the Traefik labels a service declares — which belongs to the stack that owns the network rather than to this procedure. Two further pointers are references rather than jumps, in that the step is complete without following them: `ansible/roles/deploy_user/README.md` for what a GHCR credential failure looks like, and two `docs/backlog.md` entries for gaps the document discloses. The OAuth client is the one item decision 8 did not enumerate; it is once-per-server like the other four and is listed here rather than silently added to that decision.

## 6. Ship

- [ ] 6.1 Commit the implementation and dispatch the code review over the committed diff.
- [ ] 6.2 Open the pull request, let continuous integration run, and wait for the operator's confirmation that it merged.
- [ ] 6.3 Propose the observation and wait for the operator's confirmation: the operator opens `docs/onboard-an-application.md` and confirms it can be followed for the next onboarding without opening the bootstrap document, and that the database block in it is the one they have run. Where the operator judges that no observation can be made until the next real onboarding, record the waiver here, naming the class.
- [ ] 6.4 Confirm `provision-commerce-ops-database-in-the-shared-instance` has archived before archiving this change (decision 7). Verify by reading that change's location in the tree rather than by recalling its state; if it has not, this change waits. Then diff this change's delta requirement block against the block that change merged into `openspec/specs/iac-platform-services/spec.md` and expect **exactly three** differences — the document path, the added paragraph, and the added scenario. Anything else is text this change would silently drop, since a `MODIFIED` requirement replaces its block whole and `openspec validate` cannot see the loss.
- [ ] 6.5 Archive: bring the branch to the freshly fetched trunk, archive the change, and open its record's pull request.

The branch and its working tree are removed after that record's pull request merges — after the commit this file is written in, so they cannot be ticked here and are recorded in prose instead, per this repository's convention on task lists and archived records.
