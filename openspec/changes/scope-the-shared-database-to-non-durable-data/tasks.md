## 1. Tests

- [x] 1.1 Record the baseline before anything is edited: `python3 -m unittest discover --start-directory .github/tests`, run from this working tree's root after provisioning. Note the test count. Verify it is green — a green suite before the change is what makes a green suite after it mean anything. Note that this suite fails when run from the repository's *main* working tree while any `.claude/worktrees/` tree exists; that is a known false failure recorded in `docs/change-queue.md` by task 3.3 and is not this change's doing.
- [x] 1.2 This change declares specification deltas, so it owes derived tests and no exemption applies. Dispatch the independent test author after the plan review permits proceeding and the plan is committed, and before any file is edited. Dispatch with **all three rows** of `AGENTS.md`'s test table and no verdict about which apply: two of the classification's reasons are static reads of `platform/docker-compose.yml` — Prometheus's `--storage.tsdb.retention.time` and `--storage.tsdb.retention.size`, and Grafana's provisioned datasource and dashboards — as are the `grafana_dashboard_provider` config's `allowUiUpdates` and `disableDeletion` keys, which decide how much of the Grafana store the provisioned reason actually covers. Cite these by key rather than by line: an earlier draft of this task cited lines 480-481, which excluded `allowUiUpdates` — the setting the whole argument rests on — and `docs/change-queue.md` already records a line citation rotting inside the change whose subject was citations that rot. Those are the `.github/tests` row's subject and the properties `design.md` decision 4a says must not be able to disappear silently. Verify by the author's own report: new test files plus a `test-plan.md`, or a stated gap with its reason, decided by them rather than pre-authorised here.

  - Baseline: 146 green, from this working tree after provisioning. After the derived tests: 170 green. Both runs from inside the worktree, where the phantom failure task 3.3 records does not arise.
  - The test author placed 24 tests under the `.github/tests` row and reported the other five delta scenarios as gaps with reasons, in `test-plan.md`. It appended to `test_ci_configuration.py` rather than adding a module, because the suite's own no-network/no-subprocess/stdlib-only assertions read that file alone.

## 2. `platform/README.md`

- [x] 2.1 Replace the open statement — "How a new application actually gets its own database/role inside that instance is not yet defined" — with what the instance is *for*: non-critical technical or temporary records. Verify by re-reading the section and confirming it now states a policy a reader can act on rather than an unanswered question.
- [x] 2.2 State plainly, in that same section, that nothing in the shared instance is backed up, and that this is a decision rather than an omission. Cite the requirement by name — *No Store on This Host Holds Data Requiring Backup* — without a path to this change's own directory, per `AGENTS.md`'s citation rules. Verify by checking the citation form against that section of `AGENTS.md`.
- [x] 2.3 Say where an application with durable data goes instead: an external managed service that owns its own backups. Do not name a plan or a vendor tier in the README — that is a fact about someone else's account and will rot. Verify by reading the sentence back and confirming it stays true if the account changes.
- [x] 2.4 **Correct the Boundary section too** (`platform/README.md:9-14`), which states the superseded model twice and more prominently than the section above: "A new application gets a new database inside this instance, not its own PostgreSQL container", and "Every application on the host reuses this stack's reverse proxy and **database** rather than defining its own instance of either" — the second being almost verbatim the scenario this change qualifies. Both are unconditional where the amended requirement is conditional, so neither can stand as written and neither is fixed by editing the database section alone. Verify by reading both bullets back against the two delta requirements and confirming that a reader following the README reaches the same answer as a reader following the specification.
- [x] 2.5 Leave the sections unrelated to the database alone: *Deployed by a GitHub Actions workflow* (the third Boundary bullet), *Monitoring and alerting*, and *Status*. Note that the prose tasks 2.1 to 2.3 edit sits **inside** the *Joining the platform network* section, so that section is in scope despite its heading. Verify by `git diff platform/README.md` and confirming every hunk is one of 2.1 to 2.4.

## 3. Queue and deferred-work bookkeeping

- [x] 3.1 Delete `docs/change-queue.md` entries 19 (`back-up-the-shared-database-logically-and-off-host`) and 20 (`decide-the-database-model-and-define-per-application-provisioning`). Both are resolved here: 19 by the classification, 20 by the scoping plus the deferral in 3.5. Verify by confirming neither heading remains, that no surviving entry refers to either by number without explanation, **and** that the preamble introducing that batch — "Entries 19 to 30 came out of a second full review on 2026-09-08" — is corrected: it names a range whose start these deletions remove and whose end later entries already passed.
- [x] 3.2 Add a queue entry for the commerce-ops divergence: that application keeps durable data on this host in a PostgreSQL container of its own, the requirement says it should not, and the resolution — its data moves to Supabase — is work in that application's own repository. Record what is at risk until then: 12 MB, of which the irreplaceable part is a few hundred curated rows, and roughly 17,000 rows of `procrastinate_*` queue tables that are exactly the non-durable class the shared instance is for. State explicitly that resolving it takes **two** steps, because nothing else in this repository owns the second: the migration in that repository, and deleting the divergence paragraph from *No Store on This Host Holds Data Requiring Backup* (`openspec/specs/iac-safety-hardening/spec.md`), which no change here would otherwise prompt. Verify the figures against the host rather than against this file.
- [x] 3.3 Add a queue entry, unrelated to this change's subject, for the finding that surfaced while provisioning: `.github/tests`'s Dependabot-coverage assertion walks into `.claude/worktrees/`, so running the suite from the repository's main working tree fails with three phantom `.terraform.lock.hcl` directories whenever any worktree exists — which `AGENTS.md` mandates one of, per change. It passes from inside a worktree. Recorded here rather than fixed, per `AGENTS.md`'s "A second change surfacing". Verify by reproducing the failure from the main working tree and confirming the same suite is green from inside this one.
- [x] 3.4 Sweep the committed tree for two classes: statements of the superseded model, and pointers to what this change deletes. Search **case-insensitively**, with patterns wide enough to reach prose naming neither "PostgreSQL" nor "shared": at minimum `own database`, `new database`, `own instance`, `shared instance`, `per-application database`, `reuses this stack`, `reverse proxy and`, `shared postgres`, `logical database backup`, `entry 19`, `entry 20`, `entries 19`, `queue 19`, and `queue 20` — the last two because `docs/review-2026-09-08-host-readiness.md`'s gap table records where each finding went in a *Recorded* column whose cells read `Queue 19` and `Queue 20`, which no pattern shaped around the word "entry" reaches. Four holders are known and each needs a stated disposition, so do not treat the search as the whole task:
  - `platform/README.md` Boundary bullets — corrected by task 2.4.
  - `docs/bootstrap-a-new-host.md` §8.3 *Database*, which tells an operator to "Decide per application until `docs/change-queue.md` entry 20 settles it" and offers "Own instance ... the application owns its backup" as a live option. This change settles it; that step becomes wrong rather than merely stale, and it is operational guidance someone will follow. Replace the choice with the decision: non-durable technical or temporary relational data goes in the shared instance by the recipe already given there, durable data goes to the external managed service, and for **durable** data an application's own PostgreSQL container is not an option this repository offers unless the logical backup and rehearsed restore that *No Store on This Host Holds Data Requiring Backup* demands are already in place — which is a bar, not a footnote, and no application has cleared it. For non-durable relational data the shared-instance mandate is unconditional, and no backup licenses a container of its own. Its closing summary also cites "entries 19 to 31" and names 19 and 20 as work to do; correct both.
  - `docs/review-2026-09-08-host-readiness.md`, which cites the same range and records the backup gap this change decides is not one. It is a **dated** record of what was read on 2026-09-08, so it may legitimately stand as an observation — but say so in the document rather than leaving a reader to infer it, and fix the citation of a range whose start no longer exists.
  - `platform/.env.example`, whose superuser comment points at "proposal.md's noted follow-up work" for per-application provisioning — a pointer this change relocates.
  Correct what is now false, or record why a statement stands. Verify by re-running the search and confirming every remaining hit is either correct under the new scoping, dated as a historical record, or explained.
- [x] 3.5 Add a `docs/deferred-work.md` entry for **automated** per-application provisioning inside the shared instance — the mechanism and its credential delivery — deliberately not built, because no application needs a database there today and a mechanism designed against no consumer is guesswork. Word it as automation deferred rather than provisioning undefined: `docs/bootstrap-a-new-host.md` §8.3 already carries a working manual recipe and `platform/README.md` uses the same pattern for `pgexporter`, so an entry claiming the thing is undefined would contradict two documents in this repository. Give it the revisit trigger that matters — the first application that actually wants technical storage in the shared instance. The requirement itself carries the same deferral and trigger, so this entry is the tracker rather than the record; that is deliberate, because this file's own header says an entry is deleted when it stops being true. Verify by checking the entry states both the reason and the trigger, which is what that file's header requires.

- [x] 3.6 Re-verify the store census the classification rests on, from the host and against no expected answer. Enumerate `docker volume ls` and every running container's mounts, and list what is persisted outside a container's writable layer; then check that list against the table rather than checking the table against the list. Reading `platform/docker-compose.yml` is not sufficient and is how the first draft of this change missed Alertmanager's store: `prom/alertmanager` declares `VOLUME /alertmanager` in the image, so the container holds an anonymous volume the stack definition never mentions. The census taken on 2026-09-08 found five platform stores and one application store; any store the re-run finds that the table does not name has to be classified, or named as a divergence, before this change ships. The read-only host paths cAdvisor and node-exporter mount — `/`, `/proc`, `/sys`, `/var/lib/docker`, the Docker and containerd sockets — will show up in that enumeration and are **not** stores: nothing persists data into them, which is what the requirement's opening clause turns on. The stack's eight `configs:` mounts — the Prometheus scrape config and rules, the Alertmanager routing tree, the Grafana datasource and provider, and the three dashboards — will surface too, and are likewise not stores: nothing persists into them, and every deploy recreates them from `platform/docker-compose.yml`. Expect all of these and pass over them.

- [x] 3.7 Update the Purpose line of `openspec/specs/iac-safety-hardening/spec.md`, which enumerates that capability's subjects — "deletion protection, backups, network baseline, resource labeling, and automated dependency updates" — and does not mention the classification of data stores on the host that this change adds to it. A delta cannot carry a capability's Purpose, so this is a direct edit of the main specification, which is what OpenSpec prescribes for that one field. Verify by reading the line back and confirming it names what the capability now governs. Note the interval this creates and accept it deliberately: the Purpose lands on the trunk with this change's own pull request, while the requirement it advertises arrives only when the change is archived. That is bounded by this change's own life, in the same way `AGENTS.md`'s citation rules accept a new capability's path not resolving until archive.

  - **3.6 evidence.** Census re-run on the host on 2026-09-08, list first and table second: `docker volume ls` returns four volumes (`platform_postgres_data`, `platform_traefik_letsencrypt`, `commerce-ops_commerce_ops_pgdata`, and Alertmanager's anonymous `8bf9b6b4…`), and the writable mounts across all eleven running containers add two host binds (`/mnt/main-data/prometheus`, `/mnt/main-data/grafana`). Six stores: the five the table classifies, plus the one named as a divergence. Nothing unaccounted. The read-only binds and the `configs:` mounts appeared as expected and were passed over.

## 4. Verification

- [x] 4.1 `openspec validate scope-the-shared-database-to-non-durable-data --strict`. Verify it passes.
- [x] 4.2 `python3 -m unittest discover --start-directory .github/tests` from this working tree. Verify the count matches task 1.1's baseline plus whatever the test author added, and that it is green.
- [x] 4.3 `pre-commit run --all-files`. Verify it passes, or that each skip is a hook with no files to check.
- [x] 4.4 Confirm the change touched nothing that runs. Verify by `git diff --name-only` against the merge base **and** by `git status --porcelain`, since an untracked file appears in neither the diff nor the commit and would ship unreviewed or be lost. The combined list must contain only `openspec/`, `platform/README.md`, `docs/change-queue.md`, `docs/deferred-work.md`, whatever task 3.4 corrected, and `.github/tests/test_ci_configuration.py` — the last because code review found two of this change's own derived tests unable to fail, one silently absent from a direct run and one passing whether or not the code under test existed. Fixing a verification this change introduced is this change's, not a follow-up's. Any path under `terraform/`, `ansible/`, or `platform/docker-compose.yml` means the change grew beyond what it proposed — including a tempting one-line fix to `allowUiUpdates`, which belongs to a change that can deploy it.
- [x] 4.5 Re-read all four delta requirements against the specifications they modify, in `openspec/specs/iac-platform-services/spec.md` and `openspec/specs/iac-safety-hardening/spec.md`, and confirm no *other* requirement in either file now contradicts them. Three are known to bear on this: *Shared PostgreSQL Instance Metrics Are Collected* refers to the narrowed requirement by name; *Data Durability for Stateful Resources* is itself modified here so the two accounts of `backups = true` agree; and *Monitoring Data Has Bounded, Dedicated Storage* is what makes the Prometheus row's reason durable rather than incidental, and is the requirement the Grafana row has to be read against. Verify by reading them, not by grep alone.
- [x] 4.6 Confirm the two delta specs do not contradict **each other** on what an application may persist on this host: the platform-services scenario governs non-durable relational data, and every other store falls to the safety requirement. Verify by reading both scenarios in sequence and answering, in writing, what each tells an application that wants a Redis cache, one that wants an upload directory, and one that wants **durable relational data on this host with a backup pipeline of its own** — the case where the two deltas most recently disagreed, one forbidding it outright while the other permitted it subject to a rehearsed restore. For the first two, an answer of "a logical backup and a rehearsed restore" means the classification's derived-data reason is not doing its job. For the third, two different answers mean the reconciliation did not hold, and either way the requirement needs another look before this change ships.

  - **4.5, read rather than grepped.** *Shared PostgreSQL Instance Metrics Are Collected* refers to the narrowed requirement by name and the name is unchanged, so the reference resolves. *Monitoring Data Has Bounded, Dedicated Storage* is compatible in both directions: "needs no backup" is not "may be discarded", and its persistence-across-recreation clause is a property of the mount rather than a backup claim; note that Alertmanager's store is neither metrics storage nor dashboard state, so its being off the dedicated volume is outside that requirement rather than in breach of it. *Data Durability for Stateful Resources* is modified here and now gives the same account of `backups = true` as the new requirement.
  - **4.6, answered in writing.** *A Redis cache*: the platform-services requirement excludes non-relational stores from its scope explicitly, so it says nothing; the safety requirement classifies it under the derived-and-regenerated reason, whose source must be named. No backup owed — the derived-data reason does its job. *Durable relational data on this host with a pipeline of its own*: both deltas now answer identically — never in the shared instance, under any circumstances, and elsewhere on the host only with the logical backup and the rehearsed restore in place first. One answer, so the reconciliation holds.
  - **4.6, one probe answered against its stated expectation, deliberately.** *An upload directory* was grouped with the cache as a test of the derived-data reason, but the two are not alike. Thumbnails or any regenerable derivative fall to that reason and owe nothing. Originals a user supplied are durable by definition, and for those the answer *is* "a logical backup and a rehearsed restore, or an external service" — which the task's pass condition reads as a failure. It is not one: it is the requirement working. The probe conflates a derived store with a durable one, and the right reading is that an upload directory has to be classified by which of the two it holds. Recorded rather than quietly passed.

  - **One code-review item was considered and not applied.** The census keys an anonymous volume on its target alone, so two services each declaring `- /data` would collapse into one entry. Closing it changes that identifier's shape, which a derived test asserts by value — and editing an independent author's expectation to match an implementation change is the move this project's testing standard exists to refuse. The hole is unreachable while the stack declares no anonymous volume at all, and it is now documented at the line that carries it. Left for whoever adds the first one.
## 5. Ship

- [ ] 5.1 Dispatch the code-review gate over the diff once 4.1 to 4.6 pass.
- [ ] 5.2 `ship:confirm` cannot be answered by observation: the change alters no running service, no host state and no deployed artifact, so there is nothing to look at that would differ. Name the class — no observation can actually be made — and ask the operator to waive it. Do not waive it here. Verify by recording the operator's waiver in this file before archiving.
