## Purpose

Defines how a Terraform-provisioned Hetzner host is configured via Ansible — inventory provenance, container-runtime installation, the secrets boundary, and the host-level vs. cloud firewall split — and the scope boundary that stops this layer short of application-runtime territory.

## Requirements

### Requirement: Dynamic Inventory via hcloud Plugin
Ansible SHALL discover target hosts via the `hcloud` dynamic inventory plugin rather than a static or hand-maintained inventory file. Hosts SHALL be grouped by the `environment` label already applied to Hetzner resources. The plugin SHALL authenticate using the existing read-only `HCLOUD_TOKEN`, not a separate token.

#### Scenario: Inventory resolved live from Hetzner
- **WHEN** a playbook run targets the `prod` group
- **THEN** inventory SHALL be resolved live from Hetzner Cloud's API via the `environment` label filter, not from a committed hosts file

#### Scenario: Disabled server yields no stale inventory entry
- **WHEN** the prod server's lifecycle toggle is set to disabled and the server no longer exists
- **THEN** the `hcloud` dynamic inventory SHALL return no host for that environment, rather than a stale or unreachable static entry

### Requirement: Container Runtime Installed via Pinned External Role or Equivalent
Docker Engine (or equivalent) installation SHALL use either a pinned external Galaxy role/collection recorded in a committed `requirements.yml`, or hand-rolled tasks. Neither mechanism is mandated over the other, but if an external role or collection is used for this or any other purpose, its version SHALL be pinned.

#### Scenario: External role version is pinned
- **WHEN** the Docker installation role is resolved during a run
- **THEN** its version SHALL come from an exact pin recorded in `requirements.yml`, not an unbounded or floating range

### Requirement: Secrets Never Committed in Plaintext and Never Left World-Readable on Host
Ansible Vault SHALL be the default mechanism for secrets used during host configuration. Any file a secret is rendered into on the host SHALL have restrictive permissions and SHALL NOT be committed to version control.

#### Scenario: Secret-bearing variable is encrypted at rest in the repository
- **WHEN** a secret-bearing variable is stored in this repository
- **THEN** it SHALL be encrypted via Ansible Vault, not stored in plaintext

#### Scenario: Rendered secret file is restricted and untracked
- **WHEN** a task renders a Vault-protected value into a file that will exist on the host (for example an environment file)
- **THEN** that file SHALL be created with restrictive permissions and SHALL be excluded from version control

### Requirement: Host-Level Security Owned by Ansible, Cloud Firewall Owned by Terraform
Ansible SHALL own host-level security controls: the host firewall, intrusion prevention, SSH hardening, and unattended security updates. Ansible SHALL NOT manage the Hetzner cloud firewall or other external network exposure — that remains Terraform's responsibility. For any given port, exactly one layer SHALL be the documented access gate.

#### Scenario: Host firewall rules are Ansible-managed
- **WHEN** host-level firewall rules are defined for the server
- **THEN** they SHALL be managed by Ansible tasks, not by Terraform

#### Scenario: External exposure changes go through the cloud firewall
- **WHEN** a port's reachability from the internet changes (for example, opening a new port)
- **THEN** the change SHALL be made via the Terraform-managed Hetzner cloud firewall, not introduced solely as a host-level rule

### Requirement: Configuration Scope Stops at the Container Runtime
Ansible's responsibility SHALL end once the container runtime engine is installed and ready to run containers. Ansible content SHALL NOT template an application service-definition file (for example, a Compose file) and SHALL NOT invoke a runtime's application-lifecycle commands (starting, stopping, or restarting an application stack).

#### Scenario: A completed run starts no application
- **WHEN** a playbook run completes successfully
- **THEN** no application container SHALL have been started as a result of that run, and no application service-definition file SHALL have been generated by it
